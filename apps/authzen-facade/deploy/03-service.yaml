apiVersion: v1
kind: Service
metadata:
  name: authzen-facade
  namespace: app
  labels:
    app.kubernetes.io/name: authzen-facade
    secforge.platform/component: authzen-facade
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: authzen-facade
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
