# Leaf certs for openbao-seal and main openbao, both issued from the shared
# openbao-ca-issuer (see 03-ca.yaml) so the trust chain is consistent.

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openbao-seal-tls
  namespace: openbao
  labels:
    app.kubernetes.io/name: openbao-seal
    secforge.platform/component: openbao-seal
spec:
  secretName: openbao-seal-tls
  secretTemplate:
    labels:
      app.kubernetes.io/name: openbao-seal
      secforge.platform/component: openbao-seal
  duration: 8760h
  renewBefore: 720h
  privateKey:
    algorithm: ECDSA
    size: 256
    rotationPolicy: Always
  commonName: openbao-seal.openbao.svc.cluster.local
  dnsNames:
    - openbao-seal
    - openbao-seal.openbao
    - openbao-seal.openbao.svc
    - openbao-seal.openbao.svc.cluster.local
  usages: [server auth, digital signature, key encipherment]
  issuerRef:
    name: openbao-ca-issuer
    kind: Issuer

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openbao-tls
  namespace: openbao
  labels:
    app.kubernetes.io/name: openbao
    secforge.platform/component: openbao
spec:
  secretName: openbao-tls
  secretTemplate:
    labels:
      app.kubernetes.io/name: openbao
      secforge.platform/component: openbao
  duration: 8760h
  renewBefore: 720h
  privateKey:
    algorithm: ECDSA
    size: 256
    rotationPolicy: Always
  commonName: openbao.openbao.svc.cluster.local
  dnsNames:
    - openbao
    - openbao.openbao
    - openbao.openbao.svc
    - openbao.openbao.svc.cluster.local
    # Per-replica DNS names for Raft TLS handshake (3-replica HA).
    - openbao-0.openbao-internal
    - openbao-0.openbao-internal.openbao.svc.cluster.local
    - openbao-1.openbao-internal
    - openbao-1.openbao-internal.openbao.svc.cluster.local
    - openbao-2.openbao-internal
    - openbao-2.openbao-internal.openbao.svc.cluster.local
  usages: [server auth, client auth, digital signature, key encipherment]
  issuerRef:
    name: openbao-ca-issuer
    kind: Issuer
