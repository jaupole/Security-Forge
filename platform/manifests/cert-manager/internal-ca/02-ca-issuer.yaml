# The real internal CA issuer, signed by the cert above.
# Used by Keycloak (internal HTTPS), future service-to-service mTLS.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: secforge-internal-ca
spec:
  ca:
    secretName: secforge-internal-ca-tls
