# DENIED: legacy-secret-env annotation set but legacy-secret-env-expires missing.
apiVersion: v1
kind: Pod
metadata:
  name: denied-legacy-missing-expires
  namespace: app
  annotations:
    secforge.local/legacy-secret-env: "JIRA-1234"
spec:
  containers:
    - name: app
      image: example.com/app:1
