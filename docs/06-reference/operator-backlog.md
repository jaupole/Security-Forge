# Operator Backlog

> Canonical index of post-phase follow-up items the operator must (or may) clear. Phase prompts open these; this file is the place to find them after that phase has closed.
>
> When you open a new backlog item, append a row here AND link the row from the closing phase block in PLAN.md. When you close one, mark it ✅ here AND remove the open reference from PLAN.md prose.
>
> Last updated: 2026-06-05 (#79 — k3s secrets-encryption **RE-ENABLED + reboot-validated**; the May-28 "key-lost-on-reboot" incident is permanently closed. Method (offline clean-slate, k3s stopped behind a verified full state.db backup): the corrupt empty-key config was baked into the **datastore bootstrap** (`/bootstrap/<hash>` kine row, `ControlRuntimeBootstrap.EncryptionConfig`) and disk-only edits can't win (reconcile is datastore→disk; disk-newer is fatal; `--cluster-reset` is etcd-only). Fix: moved the stale `cred/encryption-{config,state}.json` aside, tombstoned the bootstrap row (`UPDATE kine SET deleted=1`), uncommented `secrets-encryption: true` → k3s minted a fresh AES-CBC `aescbckey` + rebuilt the bootstrap; verified Enabled + at-rest probe (`k8s:enc:aescbc:`) + a clean `systemctl restart` persistence test. **A subsequent Hetzner hardware reset proved the key survives a full power-cycle.** ⚠️ **Reboot-storm gotcha:** the ≥3 rapid k3s restarts stacked SPIFFE re-mount storms that OOM-looped the **spiffe-csi-driver** at 512Mi → cluster-wide `MountVolume.SetUp failed` → single node compute-starved (SSH refused, tailnet still ping-able) → required the hardware reset. Remediated: csi-driver bumped to **1Gi/256Mi** in `platform/values/spire.yaml` (fresh pod rode out the post-reboot storm at 0 restarts). Post-reboot recovery: operator unsealed openbao-seal 3/5 (main auto-unsealed via Transit), ambient drift cleared via `rollout restart ds/istio-cni-node` then `ds/ztunnel` (control was crash-looping on its FORCE-RLS DB assertion), stale outage-window failed Jobs deleted. **Residual RESOLVED 2026-06-05:** 209/211 secrets now encrypted at rest — the ~198 plaintext *mutable* secrets re-written through the apiserver (`kubectl get secret -o json | kubectl replace`; validated one, then batched — 0 failures, **zero k3s restarts → no mount-storm**) behind a fresh Velero backup + an online `state.db` snapshot. 2 *immutable* secrets stay plaintext (`kube-system/secforge-prod.node-password.k3s`, `vault-secrets-operator/vso-cc-storage-hmac-key`): apiserver-replace can't touch them; only the prepare→rotate→reencrypt restart-dance (2 restarts, §3 storm risk) or delete/recreate can encrypt them — deferred as low-value. NB `k3s secrets-encrypt reencrypt` is NOT in-place (errors `incorrect stage: start`); it only runs as the tail of a key rotation. Runbook: `docs/03-runbooks/k3s-encryption-reenable.md`). Previous: 2026-06-04 (opened #77 — VSO 1.4.0 wedges VaultStaticSecrets after an OpenBao restart/restore; 9 secrets stuck since the 05-19 restore were cleared by delete+recreate of the CR (controller restart / watchdog bounce / force-sync all ineffective). VSO 1.4.0 is already the latest release so an upgrade is a no-op; durable follow-up = pin+Renovate-track VSO. Surfaced verifying the OpenBao 2.5.4 upgrade). Previous: 2026-06-04 (closed #75 — OpenBao 2.5.3→2.5.4 security upgrade executed end-to-end: rehearsed the Shamir seal restart+unseal on 2.5.3 first to validate keys, fresh Velero backup, helm-upgraded both OnDelete releases, rolled main followers-first/leader-last (each auto-unsealed via Transit + rejoined Raft), seal rolled last + operator-unsealed 3/5. All 4 pods on 2.5.4, Raft + VSO verified healthy. Commit `55525b2`). Previous: 2026-06-04 (opened #76 — CNPG Postgres Tier B resolved as **accept + monitor**: the image bump is a no-op (all 5 DBs already on the latest `17.6-bookworm` digest `f785551`; 17.7+ doesn't exist; the reachable libssl3/gnutls CVEs are upstream-stale-awaiting-rebuild, the perl/python ones dormant — no plperl/plpython on any DB). No DB rolled. Added a renovate.json customManager tracking the CNPG `Cluster.spec.imageName` (was a blind spot for keycloak+spicedb) + CNPG roll steps in base-image-cve-cadence.md, so the upstream rebuild surfaces a reviewable PR → roll one-at-a-time. Also opened #75 — OpenBao 2.5.3→2.5.4 watched upgrade, held out of Renovate group PR #83 during the Tier-C CVE pass; #83 split so its Go deps landed via #42/#43 and its velero-plugin 1.14.1 + alpine/k8s 1.36.1 cherry-picked to main in `4049797`, leaving #83 as the OpenBao-only tracking PR. Also this session: portal rebuilt to clear Alpine CVE-2026-31789 (libcrypto3/libssl3 → 3.3.7-r0), deployed + verified with CSP intact; wazuh-agent DaemonSet digest-refreshed (#82) + deployed, agent reconnected Active). Previous: 2026-05-22 (closed #48 — the Keycloak signed-image cutover landed. The IdP now runs the GHA-built, cosign-v2-signed, CVE-refreshed image (`ghcr.io/jaupole/keycloak@sha256:90bbcd9d…`, Keycloak 26.3.3 on base `6a7217a`), verified at admission by Kyverno. Took 3 deploy attempts: two latent GHA-pipeline gaps found + fixed — the Keycloak Operator does not propagate `imagePullSecrets` from the CR podTemplate (moved to the ServiceAccount), and the Dockerfile did not bake the `health/metrics/tracing` build-time options (baked to match the retired kaniko recipe); both failed attempts recovered cleanly via the pre-cutover CR snapshot. Also: cosign pinned to v2 so Kyverno can verify `.sig` signatures, and the Keycloak-version Renovate customManager made functional. Opened+closed #52 (`apply-manifest.sh` client-side apply was broken for the keycloak CR — corrupt last-applied-configuration annotation; fixed — apply-manifest.sh gained an opt-in `--server-side` mode, 03-keycloak.sh uses it, the stale annotation was stripped) and opened #53 (post-soak cutover cleanup). Also closed #39 — flipped the two ADR-0013 outbound-secrets Kyverno policies Audit→Enforce after a green pre-check (live workloads clean; 4 stale 0-replica ReplicaSets deleted); opened #54 for a weekly guardrail-verification CronJob. Closed #51 — the 9 per-namespace Layer-A egress policies relocated into their ns dirs + wired into the 5 owning component scripts; dry-run confirmed repo == live. Closed #28 — Wazuh rule 100301 field path fixed `response.error`→`error`; wazuh-logtest confirms it fires at level 8. Closed #54 — built the weekly guardrail-verification CronJob (07-guardrail-selftest.yaml); a one-off run verified it: the probe Pod is denied by no-secret-shaped-env-vars). Previous: 2026-05-21 (#48 — folded in a sub-task: make the Keycloak base-image customManager functional so Renovate can apply + surface keycloak version bumps. Renovate background: the all-minor-patch group kept failing because that customManager can't apply a version change; keycloak version updates are now disabled in `renovate.json` until #48 fixes the customManager. Also done today: Docker Hub `hostRules` added to `renovate.json` (cleared the anonymous rate-limit lookup failures), jwx majors set to `enabled:false`, `ghcr.io/authzed/spicedb-operator` added to `ignoreDeps`, the wazuh-agent image tagged so Renovate can track it.). Previous: 2026-05-21 (closed #49 — pending `platform/values` deploys: velero helm release-state fixed (the live Deployment already carried the 2Gi OOM hotfix); ingress-nginx `secforge-default-headers` ConfigMap codified as a repo manifest + the chart-ignored dead `extraObjects` block removed; kps/trivy/loki/tempo/minio assessed as minor or inert — no deploy needed). Previous: 2026-05-21 (closed #47 — box-vs-repo drift audit: all 22 `platform/values/*.yaml` overlays scanned against their live helm releases, 13 clean / 9 drifted; the one repo-stale danger — `spire` spiffe-csi-driver memory — fixed + box-synced, the other 8 are repo-ahead or inert. Opened #49 — deploy the pending repo-ahead `platform/values` changes (`velero`'s undeployed 2Gi OOM fix is the priority). Also: SpiceDB/control/keycloak Postgres all upgraded to 17.6 — #46 closed earlier today). Previous: 2026-05-21 (opened #48 — Keycloak CR cutover blocked by three stacked issues found while attempting it: image-name mismatch (the workflow pushes `ghcr.io/jaupole/keycloak`; the CR / Kyverno policy / README / renovate all say the nonexistent `ghcr.io/secforge/keycloak`), a cosign-v3 signature-scheme mismatch that makes Kyverno reject the image at admission, and bidirectional CR drift. Logged as a planned task; the IdP stays on its current image, no change made). Previous: 2026-05-21 (closed #45 — Wazuh SIEM upgraded 4.14.4 → 4.14.5, a security patch (helm revision 18 on the box); indexer / manager / dashboard + the agent DaemonSet all verified at 4.14.5 and the agent reconnected. The deploy surfaced stale repo drift in `platform/values/wazuh.yaml` — 7 production-config items hand-applied to the box and never committed; reconstructed from the live release + verified, and #47 opened to audit the other values overlays for the same drift). Previous: 2026-05-21 (opened #44 — jwx major-version migration parked: Renovate PR #15 closed; jwx v4.0.2 is a ~2-week-old double-major on security-critical JWT/DPoP code; `renovate.json` now parks jwx majors behind dashboard-approval + a 60-day soak; revisit ~2026-07-21. Opened #45 — Wazuh SIEM 4-image upgrade parked, given its own Renovate dashboard group, to be handled before #46. Opened #46 — CloudNativePG Postgres 16→17 major upgrade parked. Renovate "safe wave" otherwise cleared — 14 PRs merged (3 digest pins + 10 GitHub-Action majors + the spicedb-operator pin) and the Keycloak digest refresh approved.). Previous: 2026-05-21 (updated #43 — the Renovate digest-pin half landed: Renovate wired up + made operational, pin PR #2 merged (`8bdcc12`), `gate-iac` 15 → 9; the #43 residual is now 9 findings, all member-hub-deploy-time or app-tier). Previous: 2026-05-21 (opened #42 — helloworld residual prose cleanup, after the Phase 9 helloworld demo was decommissioned (commit `e103f10`); opened #43 — checkov IaC gate residual findings, after the gate was repaired + P1/P2/P3-triaged down to 15 non-blocking findings). Previous: 2026-05-21 (opened #41 — third-party vendor images that ship no cosign signature; `verify-image-signature` was split into `-secforge` (Enforce) + `-vendors` (Audit), 6 of 10 vendor images carry no signature so the vendors policy is permanently Audit). Previous: 2026-05-20 (repointed #28 + #30 after the `infrastructure/wazuh/` retirement — the OpenBao/Keycloak audit rules moved to `platform/manifests/wazuh/local-rules/openbao-keycloak-audit.xml` and were renumbered into the 100300-range to clear the Trivy collision; #28's target rule is now `100301`, #30's Project Tracker allocation moves to the 101xxx per-app range. Opened #38 — the wider `infrastructure/` retirement campaign, audited at `docs/06-reference/infrastructure-retirement.md`. Opened #39 — flip the just-migrated ADR-0013 Kyverno policies from Audit to Enforce. Opened #40 — residual Local-Edition docs/apps cleanup). Previous: 2026-05-19 (opened+closed #36 — `wazuh-manager` livenessProbe crash-loop: the #20 fix used `pgrep -x wazuh-apid`, which never matches because `wazuh-apid` runs as a python process (`python3 .../wazuh_apid.py`), so its process name is `python3`; the probe failed every cycle and kubelet killed the manager every 3m30s — 52 restarts in ~5h — repeatedly dropping the Wazuh dashboard's API connection. Probe rewritten to a `wazuh-control status` grep; applied live via `kubectl patch` + mirrored to the vendored chart). Previous: 2026-05-19 (closed #35 — Keycloak admin-UI cache-mismatch. Investigation found the caching already correct: Keycloak emits `Cache-Control: no-cache` on the `/admin` console HTML itself, and `/resources/<version>/*` assets are content-versioned by path. The prescribed ingress `configuration-snippet` fix was rejected as redundant + blocked (ingress-nginx 1.15 ships `allow-snippet-annotations: false`; enabling cluster-wide re-opens an injection surface). Delivered instead: `platform/manifests/keycloak/06-ingress-admin.yaml` codifying the previously-undeclared live `keycloak-admin` Ingress (drift fix), + theme-cache `additionalOptions` pinned declaratively in `04-keycloak-cr.yaml`. Residual open-tab-during-upgrade blip is inherent SPA behaviour, self-heals on reload.). Previous: 2026-05-19 (closed #33 — all 4 parts for Member Hub: hash-chained audit.event (migration 039 + lib/audit.ts), tamper_flag + verifier CronJob + clear script (migration 040 + workers/audit-verifier/), nightly Ed25519-signed anchors committed to <https://github.com/jaupole/secforge-audit-anchors> (Transit key + audit-signer policy live in OpenBao, CronJob in member-hub ns), SpiceDB `auditor` relation + `read_audit` permission gating audit routes. Runbook at docs/03-runbooks/audit-anchors.md.). Previous: 2026-05-19 (closed #32 — email-anchored auto-reconcile in /me self-heals Keycloak sub-drift; migration 033 + INSERT-site updates + spicedb-sync hooks. Backfill script at src/scripts/backfill-user-email.ts. Lab-tested with `pnpm tsc`; live verification on next Keycloak reset.). Previous: 2026-05-19 (closed #20 — replaced wazuh-manager livenessProbe with `pgrep` exec across the three load-bearing daemons. apid death now auto-recovers via pod restart.). Previous: 2026-05-19 (closed #34 — OpenBao admin recovery via Velero+kopia restore from 2026-05-17 backup. Velero PVR stalled on openbao-0 + openbao-seal-0 (stale-pod-UID issue); worked around with one-shot kopia-CLI Job (mirror.gcr.io image, hostPath PV dirs) for the two volumes. Operator OIDC `admin` role restored from backup; `jason.upole` login lands with admin policy. Created `auth/kubernetes/role/admin-break-glass` (SA `openbao/openbao` → admin policy, 1h ttl) and folded the creation into 05c-openbao-configure.sh so the runbook claim is now load-bearing on cluster rebuild. Velero memory bumped 512Mi→2Gi in platform values after controller OOMKilled mid-restore. Lost ~2657 Raft entries from after 05-17 02:00Z — nothing load-bearing.). Previous: 2026-05-18 (opened #35 — Keycloak admin UI cache-mismatch causes recurring "Failed to fetch dynamically imported module" errors after restarts; workaround is incognito window every time; fix is ingress no-cache header on admin HTML + theme cache env on the StatefulSet; hit twice in one Member Hub dev session). Previous: 2026-05-18 (opened #34 — 🚨 BLOCKING: OpenBao has no working admin recovery path; admin-break-glass k8s role doesn't exist despite runbook claiming it does, generate-root returns 405 on Transit-unsealed setups, all OIDC roles narrow-scope. Member Hub Track C deferred to laptop-hybrid-dev mode until admin restored via Velero recovery or original init root token located). Previous: 2026-05-18 (opened #33 — audit log hardening (chain + sign + git-anchor) deferred from Member Hub's minimal-tier landing today; spec exists in `Member Hub/.claude/skills/audit-logging/SKILL.md`; trigger is first production tenant onboarding where audit is load-bearing for compliance). Previous: 2026-05-18 (opened #32 — Keycloak → Ecosystem Control user-sync gap; surfaced during Member Hub dev when a freshly-signed-in user saw "Active org: none" despite Control having an org; manual fix today via direct DB UPDATE + inline WriteRelationships to SpiceDB; fix candidates range from admin-events SPI listener to a one-shot operator remap endpoint; blocking once Portal opens to multiple users). Previous: 2026-05-05 (opened #31 — codify the PT schema-evolution workflow (PT-repo Prisma migration → platform-repo migration SQL + RLS policy → cluster apply) before the first post-cutover schema change; three deliverables (runbook, `prisma migrate deploy` cutover, tenant_id+RLS lint), small total effort, opens proactively while the workflow is fresh from 10.1.3.) Previous: 2026-05-05 (opened #29 watching-brief — Wazuh agent on operator's Windows host, conditional on threat-model gap firing; opened #30 — PT-specific secforge audit-rule range 100240-100259 to be added once PT emits audit events from 10.1.4 onward, mirroring the per-app convention used for OpenBao + Keycloak.) Previous: 2026-05-05 (opened #28 — secforge rule 100201 (added in #18 closeout) has never fired. Field constraint `<field name="response.error">\.+</field>` targets the wrong path; OpenBao audit events have the error string at top-level `error`, not nested under `response`. Surfaced during #27's slot-picking pass when 100201 was discovered taken-but-dormant. Fix is small: change the field-name path. Worth confirming WHAT the rule was meant to catch when it was originally written — may need wider rework if the original semantics were different from what the field-name implies.) Previous: 2026-05-05 (closed #27 — added secforge rule 100206 (first free slot in 100200-299 — 100201/100203/100204/100205 all taken from #18 + #24). Two design decisions surfaced beyond the prompt's plan: (a) parenting on 100200 doesn't work because failed-auth events lack `auth.display_name`; (b) stand-alone level-5 rule loses to 100001's level 10 because Wazuh fires the highest-level match. Solution: `<if_sid>100001</if_sid>` parenting overrides the catch-all only for events that also match the OpenBao read-denial pattern. `wazuh-logtest` confirms 100206 fires at level 5; regression-checked 100200/100202/100221 still fire on their respective inputs. **Bonus latent-bug finding** (not in #27 scope): rule 100201 added in #18 closure has never fired — its `<field name="response.error">\.+</field>` constraint targets a field path that doesn't exist in OpenBao's audit shape (actual location: top-level `error`). Worth opening a follow-up.) Previous: 2026-05-05 (closed #26 — Path A confirmed: the periodic metrics token aged out under the renew-on-USE pattern despite Prometheus running, because sustained scrape failure means USE doesn't refresh the period. Policy + ServiceMonitor wiring were intact. Re-ran idempotent `configure-metrics-auth.sh`; Prometheus picked up the rotated Secret on next scrape without restart. Verified end-to-end: audit log shows `error: null`, `vault_core_unsealed=1` flowing into Prometheus, Wazuh data.error count on sys/metrics dropped 10→0 across the equivalent window, rule 100001 firings dropped 10→0. Subtle gotcha codified in `openbao-recovery.md`: `lookup-self` is not a useful liveness probe for `-no-default-policy` tokens — must lookup-by-id from admin perspective.) Previous: 2026-05-05 (opened #26 + #27 — Prometheus → OpenBao `sys/metrics` scrape failing with `permission denied` every 30s, surfaced via Wazuh ingestion of the failed audit events; chart's catch-all rule 100001 fires at level 10 instead of secforge ruleset because secforge rule 100200 doesn't cover read-permission-denied events. Two issues, ordered: #26 fixes the underlying auth fault, #27 widens secforge rule coverage.) Previous: 2026-05-05 (opened #25 — Wazuh dashboard OIDC cutover. Phase 7d Item 7 scaffolding has been in place since 2026-05-02 (Keycloak `wazuh-dashboard` client + OpenBao client_secret path + VSO binding + `configure-wazuh-oidc.sh` flip script) but the actual flip from local admin → Keycloak SSO has never been run. Surfaced 2026-05-05 by operator question "why doesn't Wazuh use Keycloak/OpenBao for login like Grafana?".) Previous: 2026-05-05 (closed #24 — `infrastructure/wazuh/03-decoders-configmap.yaml` extended with two literal-prefix `<decoder name="json">` blocks (`^F ` / `^P `) handling the kubelet-wrapped pod-log shape. Wazuh's syslog pre-decoder consumes both the timestamp AND the `stdout`/`stderr` token, so the residual content reaching decoders is `F {"...":...}` not `stdout F {...}`. Live-event probe shows 12 hits in rule.id 100200-100299 range across rules 100200/100202/100221, with `actor` / `path` / `loggerName` fields populated. Annotated #18 with #24-verified note. Bumped runbook with new "kubelet pod-log prefix handling" section + corrected verification recipe.) Previous: 2026-05-05 (closed #22 — added `allow-postgres-operator-to-secforge-app-db` NetworkPolicy at `infrastructure/cloudnativepg/clusters/app-db-netpol-operator-status.yaml` permitting postgres-operator ns → app/secforge-app-db:8000 (CNPG status-extraction endpoint). `Cluster.status` flipped to Ready=True within seconds; `Cannot extract Pod status` reconcile errors stopped immediately.) Previous: 2026-05-05 (closed #23 — root cause was sed-delimiter conflict between `|` in randomly-generated `INDEXER_PASSWORD` and the image's `/etc/cont-init.d/1-config-filebeat`; with `set -e`, the sed-substitution aborted mid-script leaving SSL directives commented + password empty. Fix: `apply.sh` now generates passwords without `|`/`&` (sed-meta safe). `wazuh-alerts-*` index now populated end-to-end. Annotated #17/#18 closure rows as live-verified. Opened #24 (decoder pre-match gap — `stdout F` prefix not stripped; secforge custom rules don't fire on real pod-log lines))

## Legend

- ⬜ Open — not started
- 🟨 In progress
- ✅ Closed
- ⏸️ Blocked — note the blocker
- 👁️ Watching brief — keep an eye on the trigger condition

## Index

| # | Opened | Phase | Topic | Effort | Status | Blocking? |
|---|--------|-------|-------|--------|--------|-----------|
| 4 | 2026-04-30 | 7d.3 | OpenBao Transit unseal token TTL strategy — switch to `-period=720h` periodic tokens | small | ✅ Closed 2026-05-02 | — |
| 12 | 2026-05-02 | 7b.2 | Promtail verify phase-2 — flip Loki `compactor.retention_enabled` and confirm dashboard panels populate | medium | ⬜ Open | No (panels stay empty until flipped; not blocking apps) |
| 13 | 2026-05-02 | 7b | Migrate `BFF_VALKEY_PASSWORD` from K8s Secret env var to OpenBao via `apps/lib/secrets/`. Currently rides on `secforge.dev/legacy-secret-env: OPS-MIGRATE-VALKEY-PW` annotation, expires 2026-07-31 | medium | ✅ Closed 2026-05-05 | — |
| 14 | 2026-05-02 | 7b.5 | Live verification of OTel scrubber sink — blocked on Docker Desktop containerd image-load quirk (`docker save \| ctr import` reports `total: 0.0 B`) | small | ⬜ Open | No (code change compiles + tests pass; live verify deferred) |
| 15 | 2026-05-02 | 7c | Istio Ambient AuthZ-denial observability — original log-level finding closed; ztunnel emits per-policy + per-clause RBAC trace at `RUST_LOG=trace` | n/a | ✅ Closed 2026-05-03 | — |
| 17 | 2026-05-02 | 7d.5 | Wazuh agent `client.keys` doesn't persist across pod restarts (enrollment-loop quirk) | ~30–60 min | ✅ Closed 2026-05-05 (live end-to-end verified 2026-05-05 after #23 closeout — agent → manager pipeline confirmed delivering events at ~30s cadence to `alerts.log`, surviving the 2026-05-05 indexer + manager rotation without re-enrollment) | — |
| 18 | 2026-05-02 | 7d.6 | Wazuh manager-side custom decoders for OpenBao + Keycloak JSON formats not loaded | ~1 hr | ✅ Closed 2026-05-05 (synthetic-event rule firing via `wazuh-logtest` re-verified 2026-05-05 after #23 closeout — rule 100200 fires on a synthetic OpenBao audit JSON. Real pod-log format coverage verified 2026-05-05 via #24 — 12 hits in rule.id 100200-100299 range across rules 100200/100202/100221 from live OpenBao + Keycloak probes, with `actor`/`path`/`loggerName` fields populated. End-to-end SIEM coverage for the secforge ruleset is now complete) | — |
| 19 | 2026-05-04 | 10.1.1 | MCP server reachability after Project Tracker moves to cluster — PT's Week-5 MCP server runs over stdio for Jason's local Claude Code → PT data; cluster deployment breaks the stdio model. Decide between (a) keep a local-only MCP shim that proxies to the cluster API, or (b) expose a bounded set of MCP-over-HTTP endpoints. Surface a small ADR. | small (decision) + medium (impl) | ⬜ Open | Soft — only blocks Week-5 PT work, not the Phase-10 cutover |
| 20 | 2026-05-05 | 9 (retro) | Wazuh-apid daemon stops after `wazuh-manager` pod restart and is not auto-recovered; Wazuh dashboard's "wait for dependencies" init then blocks indefinitely. Manual fix today: `kubectl exec -n wazuh wazuh-manager-0 -- /var/ossec/bin/wazuh-control restart`. Durable fix likely a sidecar/liveness/probe pattern that watches `wazuh-apid` PID. See `docs/99-archive/05-claude-code-prompts/phase-09-retrospective.md` § "Wazuh-apid recovery (operational gotcha)". | medium (~2-4 hr) | ✅ Closed 2026-05-19 (replaced the manager livenessProbe with `exec pgrep -x wazuh-apid && pgrep -x wazuh-remoted && pgrep -x wazuh-analysisd`. Any of the three load-bearing daemons dying now triggers pod restart and auto-recovery — no operator action required. Applied via helm upgrade against vendored chart at `platform/manifests/wazuh/vendor-chart/templates/manager/statefulset.yaml`. Commit `1109ffb`. **DEFECTIVE — superseded by #36 (2026-05-19): `pgrep -x wazuh-apid` never matched the python `wazuh_apid.py` process, so this probe failed every cycle and crash-looped the manager (52 restarts). Corrected via a `wazuh-control status` check.**) | — |
| 21 | 2026-05-05 | 7c-2 | Phase 7c-2 — SPIRE-as-CA cutover + multi-ns STRICT expansion + trust-domain unification cluster.local → secforge.platform. Pre-req: validate the helm-values diff in `infrastructure/istio/authzpol-strict-7c2-draft/helm-values-spire-ca.draft.diff` against the upstream Istio + SPIRE compatibility matrix at https://istio.io/latest/docs/ops/integrations/spire/. Existing 7c-2 prep (AuthorizationPolicy templates, baseline capture, draft diff) parked at `infrastructure/istio/authzpol-strict-7c2-draft/`. Scope: (a) flip Istio to use SPIRE as external CA (disable Citadel), (b) ambient-enroll keycloak / spicedb / openbao / observability namespaces, (c) rewrite `app`-ns AuthorizationPolicy principals from `cluster.local/...` to `secforge.platform/...`, (d) flip mesh-wide PeerAuthentication PERMISSIVE → STRICT staged ns-by-ns, (e) **removes the CNPG-PERMISSIVE workload-scoped override (`infrastructure/istio/05-peer-auth-app-cnpg-permissive.yaml`) introduced in 7c-1** once openbao + postgres-operator + observability are mesh-enrolled, (f) close the ADR-0010 deferral. ADR-0024 / operator-backlog cross-reference: removal of override is the 7c-2 closeout signal. | 1-2 day operator window | ⬜ Open | Soft for Phase 10 — apps go into STRICT in app ns on day one under cluster.local (already in force from 7c-1); trust-domain unification is a separate concern and does not gate Phase 10 |
| 22 | 2026-05-05 | 7c-1 | CNPG cluster `secforge-app-db` Ready=False since 2026-04-29 (lastTransitionTime: 2026-04-29T22:03:54Z) — `postgres-operator` ns → CNPG pods on `:8000` (status-extraction endpoint) blocked by NetworkPolicy `allow-openbao-to-secforge-app-db` which only permits `openbao→5432`. Data plane is unaffected (Phase 9 E2E passed under this condition; openbao→CNPG dynamic-cred mint works through 7c-1's PERMISSIVE override). What's broken: postgres-operator's status reporting feeds CNPG `Cluster.status.phase=Healthy`, so backups / version-upgrade signal / failover hooks operate blind. Fix: add a NetworkPolicy ALLOW for `postgres-operator → app/secforge-app-db:8000` (mirror the existing pattern but with the postgres-operator ns selector + port 8000). Surfaced during Phase 7c-1 cutover (commit `c409913`); confirmed pre-existing by toggling STRICT off mid-cutover and observing the same i/o timeout. | small (~15 min) | ✅ Closed 2026-05-05 (added `allow-postgres-operator-to-secforge-app-db` at `infrastructure/cloudnativepg/clusters/app-db-netpol-operator-status.yaml`, mirrors the openbao→app-db policy shape with port 8000 + postgres-operator ns selector. `Cluster.status.conditions[Ready]` flipped to True at 2026-05-05T20:08:11Z; postgres-operator log noise stopped. Other CNPG-hosting namespaces (keycloak/spicedb/teleport) didn't surface this because they don't have an explicit per-target ingress policy — the `app` ns's stricter posture was the differentiator) | — |
| 23 | 2026-05-05 | 9 (retro) | Wazuh manager-side filebeat → indexer pipeline broken at TLS + auth. Real OpenBao + Keycloak events flow correctly to the manager (alerts.log shows ~30s-cadence rule matches against `/host/var/log/pods/openbao_*` and Keycloak audit lines), but `wazuh-alerts-*` indices don't exist on the indexer at all — only `wazuh-monitoring-*` (agent-keepalive). Root cause confirmed via `filebeat test output`: handshake fails with `x509: certificate signed by unknown authority`. Inspection of `/etc/filebeat/filebeat.yml` on `wazuh-manager-0` shows the `output.elasticsearch:` block has no `ssl.certificate_authorities`, no `ssl.certificate`, no `ssl.key` (all four ssl directives are commented out), and `password:` is empty. The `wazuh-filebeat-certs` Secret has the right material (`filebeat.pem`, `filebeat-key.pem`, `root-ca.pem` matching the indexer root by SHA-256 fingerprint), but filebeat isn't told to use any of it. **CLOSURE 2026-05-05:** Root cause was NOT in the chart's `inject-filebeat-config` initContainer (that copied the chart's correct ConfigMap version into the EmptyDir as designed). The breakage was UPSTREAM in the Wazuh manager image at `/etc/cont-init.d/1-config-filebeat`, which uses `sed -i "s\|password:.*\|password: '$INDEXER_PASSWORD'\|g" /etc/filebeat/filebeat.yml` — `\|` is the sed delimiter. The randomly-generated `INDEXER_PASSWORD` (`iaol\|QsorzrF&StImeA9kow?V\|qt7za.`) contained two `\|` characters, breaking the sed expression syntactically. Combined with `set -e` at the top of the script, this aborted the substitution mid-way: the `username:` substitution ran (no `\|` in `admin`); the password's first sed (uncomment `#password:` → `password:`) ran but the value-write failed, leaving `password:` empty; the SSL substitution lines (steps 4-7) never ran, leaving the four `ssl.*` directives still commented from the image's stock template. Fix: `infrastructure/wazuh/apply.sh` `gen_pw()` now uses `specials = ".*+?=!"` (no `\|`, no `&` — both are sed-meta) and Wazuh's complexity policy is still satisfied. After rotating the four credential Secrets and restarting indexer + manager + dashboard, `/etc/filebeat/filebeat.yml` now has all four ssl directives populated + the actual indexer admin password substituted. `filebeat test output` reports handshake OK + version 7.10.2; `wazuh-alerts-4.x-2026.05.05` index created on the indexer (~1135 docs within minutes). Pipeline is end-to-end live. Two residual notes: (1) the image's `1-config-filebeat` substitutes username with `INDEXER_USERNAME` (`admin`), not the chart-intended `FILEBEAT_USERNAME` (`filebeat_internal`), so filebeat now auths as the indexer superuser instead of the dedicated filebeat user — over-privileged but functional, internal-only mTLS path; (2) the secforge custom rules (100200-100299) don't fire on real pod-log events because the manager-side decoder's `<prematch>^{"</prematch>` doesn't match content with the kubelet `stdout F ` prefix that survives the syslog pre-decoder strip — opened #24 to track. | medium (~2-4 hr) | ✅ Closed 2026-05-05 | — |
| 24 | 2026-05-05 | 9 (retro) | Manager-side custom decoder for kubelet pod-log wrappers doesn't strip the `stdout F ` middle prefix. Wazuh's syslog pre-decoder strips only the leading RFC3339 timestamp, leaving `stdout F {"...":...}` as the post-pre-decode content. The secforge `kubelet-pod-log` decoder's `<prematch>^{"</prematch>` therefore never matches, JSON_Decoder doesn't engage, `auth.display_name` / `request.path` / `loggerName` fields don't get extracted, and the secforge custom rules (100200-100299) all stay silent on real pod-log lines. Synthetic events injected via `wazuh-logtest` (which skip the agent → manager pipeline) DO match because they're plain JSON without the kubelet wrapper — that's the green path validated at #18 closeout. Real events DO arrive at the manager (alerts.log shows them), but they fall through to the chart's generic rule 100001 (case-insensitive `<match> ERROR </match>` substring) and end up tagged as "Application error detected" instead of OpenBao-/Keycloak-specific descriptions. Fix candidates: (a) add a parent decoder with `<prematch>` matching `^stdout F ` then offset / strip, then call JSON_Decoder; (b) switch agent localfile to a custom log_format with regex pre-extraction; (c) move the strip step from manager-side to agent-side (have the agent emit just the JSON suffix). Approach (a) is cleanest. Until closed, Wazuh's coverage is "events arrive and are searchable in Discover with rule 100001/591/etc." — useful for breakage detection but the secforge-tuned rules and field-level enrichments don't engage on production audit lines. | small (~1 hr — local_decoder.xml change + wazuh-logtest verify with kubelet-wrapped line + verify rule 100200 fires on real probe) | ✅ Closed 2026-05-05 (option α — extended `infrastructure/wazuh/03-decoders-configmap.yaml` with two literal-prefix `<decoder name="json">` blocks: `^F ` for full kubelet lines and `^P ` for partial continuations. Both use `<plugin_decoder offset="after_prematch">JSON_Decoder</plugin_decoder>`. **Subtle finding during the fix:** Wazuh's syslog pre-decoder consumes BOTH the leading RFC3339 timestamp AND the `stdout`/`stderr` token (treating it as the syslog hostname), so the residual content reaching decoders is `F {...}` not `stdout F {...}` — the original problem statement of "stdout F middle prefix" was half-right. Probe-decoder verification (`prematch=stdout F `=NO match, `prematch=F `=match, `prematch={"`=NO match) proved the actual content shape. Decoder name `json` deliberately collides with the chart-shipped JSON decoder — Wazuh allows multiple decoders to share a name, and the rules' `<decoded_as>json</decoded_as>` sentinel keys on that name regardless of which decoder did the parse. End-to-end probe: 12 hits in rule.id 100200-100299 range from a single Keycloak failed-login + OpenBao kv-read/write trigger, across rules 100200 (OpenBao base, 4×) / 100202 (OpenBao sys/ mutation, 3×) / 100221 (Keycloak WARN, 3×). All hits include populated `data.auth.display_name` / `data.request.path` / `data.loggerName` / `data.message` fields. Runbook `wazuh-operations.md` extended with a new "kubelet pod-log prefix handling" section documenting the OSRegex constraints (no `(F\|P)` group alternation, no `[FP]` POSIX char classes, `type="pcre2"` not honored on `<prematch>`).) | — |
| 25 | 2026-05-05 | 7d.7 | Wazuh dashboard OIDC cutover never executed. Phase 7d Item 7 scaffolding has been in place since 2026-05-02 (Keycloak `wazuh-dashboard` client provisioned via `infrastructure/keycloak/clients/wazuh.sh` + OpenBao policy + role + VSO binding `platform/manifests/wazuh/03-vso-binding.yaml` rendering `wazuh-oidc-vso` Secret in wazuh ns + reversible flip script `platform/components/07j-wazuh-oidc-configure.sh`), but the actual cutover from local admin → Keycloak SSO has never been run, so the Wazuh dashboard still requires the chart-generated local admin user (the same admin whose password we fixed in #23). Note: even after the OIDC cutover, the system-internal admin password persists for filebeat → indexer auth (HTTP basic) and the manager API → indexer auth — those layers don't speak OIDC, and #23 already moved that admin password into OpenBao. OIDC only buys the human-facing dashboard login surface. Watch for: the same `realm_access.roles`-claim-plumbing gap that forced Grafana onto `preferred_username` (per the OpenBao 2.5.3 upstream defect noted in PLAN.md Phase 7 carry-ins) — Wazuh's OpenSearch Security Plugin OIDC config might need the same workaround. Procedure: run `platform/components/07j-wazuh-oidc-configure.sh`, watch the dashboard pod restart, navigate to https://wazuh.secforge.dev/ and confirm redirect to Keycloak; verify role-mapping (jaupole → wazuh `admin` backend role); document the cutover in `docs/03-runbooks/wazuh-operations.md`. | small (~30-45 min) | ⬜ Open | Soft — dashboard auth is human-only (no app traffic depends on it); local admin login still works in the interim; doesn't gate Phase 10 |
| 26 | 2026-05-05 | 7d.4 | Prometheus → OpenBao `sys/metrics` scrape failing with `permission denied` at ~30s cadence (Prometheus default `scrape_interval`). Surfaced via Wazuh dashboard noise: every scrape interval, OpenBao's audit device logs the denied request → kubelet pod-log → wazuh-agent → manager → indexer, where chart's generic rule `100001` "Application error detected" fires at level 10 (high). Per Phase 7d Item 4 (2026-05-02), the integration was supposed to use a periodic bearer token (`-period=720h -no-default-policy -policy=metrics-policy`) stored at K8s Secret `openbao/openbao-metrics-token` with the ServiceMonitor consuming via `bearerTokenSecret`. Something has drifted. Diagnose: (a) `kubectl get secret -n openbao openbao-metrics-token -o jsonpath='{.data.token}' \| base64 -d` to retrieve the current token + `bao token lookup` it (does it still exist? does it still have `metrics-policy` attached? is its TTL still alive given the periodic-on-USE renewal pattern?); (b) `kubectl get servicemonitor -n openbao -o yaml` to confirm `bearerTokenSecret.name` + `bearerTokenSecret.key` point at the right Secret + key; (c) `bao policy read metrics-policy` to confirm the policy still grants `read` on `sys/metrics` (it's just a 1-line capability list); (d) check if the periodic token's "renew on USE" pattern is actually renewing — the fact that scrapes are denied suggests the token may have expired and Prometheus is using a cached version. **Net effect today:** OpenBao Prometheus metrics not collected → Grafana OpenBao panels empty (Raft state, lease counters, unseal status, etc.) → no automated health alerting on OpenBao internals. The Wazuh log spam is a *symptom*; fixing this closes it. Verification: after the fix, re-query the indexer for `data.request.path=sys/metrics` events with `data.error=*` populated — count should drop to 0 within 1-2 scrape intervals. | small (~30 min once root cause identified; could grow to medium if the periodic-token-on-USE renewal pattern needs to be revisited) | ✅ Closed 2026-05-05 (Path A — token expired; policy + ServiceMonitor wiring intact. Re-ran `configure-metrics-auth.sh` (idempotent — rotates the periodic token + overwrites the K8s Secret). Subtle diagnostic gotcha: `bao token lookup-self` against the metrics token returns 403 even when the token is fresh because metrics-policy is `-no-default-policy` and doesn't grant `auth/token/lookup-self` — must lookup-by-id from an admin token. Admin-side lookup of the new token shows `policies=[metrics-policy], period=720h, ttl=719h59m`. Prometheus picked up the rotated Secret on next scrape without restart (kubelet auto-reconciles projected Secrets; Prometheus reads bearer-token file per-scrape). Verified: (a) audit log shows `error: null` on three consecutive `sys/metrics` response events at 30s cadence; (b) `vault_core_unsealed=1` for all 3 OpenBao pods via Prometheus query API; (c) Wazuh indexer query for `data.request.path=sys/metrics` AND `data.error` populated since fix-time = 0 hits across 2-min window (vs 10 hits in a 5-min pre-fix window); (d) rule 100001 firings on sys/metrics post-fix = 0 (vs 10 pre-fix). Underlying brittleness — periodic-token-on-USE pattern locks out under sustained scrape failure since USE doesn't refresh — documented as a recovery scenario in `docs/03-runbooks/openbao-recovery.md` § "Restore Prometheus metrics-scrape auth", and Phase 7d Item 4 footnoted in PLAN.md.) | — |
| 27 | 2026-05-05 | 9 (retro) | Secforge OpenBao base rule `100200` (and the broader 100200-range secforge ruleset) doesn't match `read` operations that return `permission_denied` or other auth-failure modes. These events fall through to chart-shipped rule `100001` "Application error detected" at level 10, which is a false-high (auth misconfiguration is operational, not application-error severity). Currently surfaced because of #26's auth-failure spam, but the rule-coverage gap is permanent until 100200 widens. Fix candidates: (a) extend rule `100200` (or add a child rule `100201`/`100203`) matching `data.request.operation=read` AND `data.error=*` populated, mapped to level 5 (medium — diagnostic-useful but not critical); (b) match on `data.response.data.error=*` populated (the audit field that has the HMAC'd error response); (c) write a per-path rule covering `data.request.path=sys/*` since that's mostly an admin-shaped surface where read-failures are noteworthy. Either way, the fix suppresses the false-high from 100001 via Wazuh's rule cascade — Wazuh fires the most-specific matching rule and quiets the catch-all. Verification path: `wazuh-logtest` with a synthetic OpenBao audit-event JSON containing the permission_denied shape; confirm the new secforge rule fires (not 100001). Must NOT regress the existing rule 100200/100202/100221 firings from #24 closure (12 hits across the live-event probe). | small (~30 min — local_rules.xml edit + `wazuh-logtest` verify + reload manager via `wazuh-control restart`) | ✅ Closed 2026-05-05 (added secforge rule 100206 to `infrastructure/wazuh/04-rules-configmap.yaml`. Slot picked: 100201/100203/100204/100205 were all taken (used in #18 + #24 closeouts) — first genuinely free slot in the 100200-299 range was 100206. **Two design decisions surfaced during fix that the prompt didn't anticipate:** (a) parenting on 100200 doesn't work — failed-auth audit events emit only `auth.token_type`, not `auth.display_name`, so 100200's display_name sentinel excludes them; (b) a stand-alone level-5 rule loses to 100001's level 10 because Wazuh fires the highest-level matching rule when multiple match, regardless of specificity. Solution: parent 100206 on `<if_sid>100001</if_sid>` directly, with constraints `request.operation=^read$` + `request.path=\.+` + `error=\.+`. When 100001's `<match> ERROR </match>` matches AND the OpenBao read-denial pattern also matches, 100206 overrides at level 5. **Also surfaced an OSRegex gotcha:** in `<field>` constraints, `\.` means "any char" and bare `.` means "literal period" — inverted from PCRE. The `\.+` convention used throughout this file works for "any non-empty content"; bare `.+` would match only literal periods. Verified via `wazuh-logtest`: synthetic permission_denied event fires rule 100206 at level 5 with description "OpenBao read denied: path=sys/metrics error=1 error occurred:..."; regression-checked 100200 (level 3 on success-shape), 100202 (level 6 on sys/ mutation), 100221 (level 5 on Keycloak WARN) all still fire. Runbook `wazuh-operations.md` extended with a new sub-section "Read-side auth/policy denial — rule 100206" documenting the if_sid=100001 idiom + OSRegex gotcha. **Latent bug surfaced for next pass (not in #27 scope):** rule 100201 (added in #18 closure as the OpenBao auth/policy denial rule) matches on `<field name="response.error">\.+</field>`, but OpenBao's audit shape has the actual error string at *top-level* `error`, not `response.error`. Rule 100201 has therefore never fired. Worth opening a follow-up to fix its field path.) | — |

| 28 | 2026-05-05 | 7d.6 | Secforge rule `100301` (renumbered from `100201` in the 2026-05-20 platform migration; added in #18 closeout as the OpenBao auth/policy-denial rule) has **never fired** — its field constraint `<field name="response.error">\.+</field>` targets a path that doesn't exist in OpenBao's audit-event shape. The actual error field is at *top-level* `error` (or `data.error` post-#24's JSON_Decoder mapping into the Wazuh field tree), not nested under `response`. Surfaced during #27's slot-picking pass (WSL Claude noticed 100201 was taken-but-dormant when scanning for free slots in the 100200-299 range). Fix: change the field path in `platform/manifests/wazuh/local-rules/openbao-keycloak-audit.xml` rule 100301 to match the actual audit-shape field. **Also worth confirming what the rule was originally meant to match** — auth-failure on writes? policy-violation generally? — and whether the level / description / groups still fit, or whether the rule needs broader rework. The #27 closure introduced 100306 (then numbered 100206) specifically for read-side auth failures, so 100301's intended scope likely doesn't overlap (probably write-side or generic policy-denial). Verification: `wazuh-logtest` with a synthetic event matching the corrected field path; confirm 100301 fires; regression-check it doesn't shadow 100306 / 100300 / 100302 / 100321. **✅ Closed 2026-05-22.** Rule 100301's field constraint corrected `response.error` → top-level `error` (commit `f0e8bcd`) — the path now matches the OpenBao audit shape, same as sibling rule 100306. Deployed via `07o-wazuh-audit-rules.sh` (analysisd validated, manager reloaded); `wazuh-logtest` confirms 100301 fires at level 8 with the `error=` description substitution populated. Scope confirmed: 100301 covers authenticated-actor policy/permission errors (100300's `auth.display_name` sentinel) — structurally disjoint from 100306 (anonymous read-side failures carry no `display_name`), so no shadowing. (Incidental: `wazuh-analysisd -t` flagged a pre-existing dangling `if_matched_sid` — rule `100405` references missing `100403` in a different manager rule file — unrelated to #28, worth a future look.) | small (~10-15 min — single field-path edit + wazuh-logtest verify + manager restart) | ✅ Closed 2026-05-22 | — |
| 29 | 2026-05-05 | n/a | 👁️ Watching brief — Wazuh agent on the operator's Windows host (`jason-pc-gaming`) for SIEM coverage of the workstation that holds the SSH signing key, the mkcert root CA private key, the cached OpenBao admin tokens, and the operator's GitHub credentials. Three pre-reqs to fire: (a) ingress-nginx TCP-passthrough or NodePort for manager ports `1514` (ossec) + `1515` (authd) — currently ClusterIP only; (b) pre-registration flow documented (`manage_agents` interactive on manager → `client.keys` → SSH transfer to Windows host → MSI install with key); (c) Windows agent enrolled + verified via a deliberate Windows event (e.g., `Get-EventLog Security` filter for a known-good audit). Trigger to convert from watching brief to active work: threat-model gap analysis identifies the operator workstation as in-scope, OR the host starts holding more sensitive material than today (e.g., production cloud creds), OR an incident response playbook is being drafted that needs Windows-host visibility. | medium (~2-3 hr all-in once triggered) | 👁️ Watching brief | No — operator workstation isn't currently in-scope for SIEM coverage |
| 30 | 2026-05-05 | 10.1.4+ | Add a Project Tracker audit-rule file at `platform/manifests/wazuh/local-rules/project-tracker-audit.xml` with a matching installer component (mirror `07o-wazuh-audit-rules.sh`) once PT is emitting structured JSON audit events from 10.1.4 onward (the BFF-injected-identity step, where audit middleware lands). Allocate a 101xxx block for PT — per `docs/06-reference/wazuh-rule-conventions.md`, the 100200-100999 ranges are now all assigned (Trivy / OpenBao+Keycloak audit / upstream-image / maintenance), and 101000+ is the available per-app range. Rules to write: per-domain mutate-with-permission-denied, per-domain mutate-with-error, login-success-from-new-IP (if BFF logs source IP), dynamic-credential-refresh-success, AuthZEN-deny pattern, etc. Coordinate with PT app code (`server/src/common/audit.ts` is the likely place) to ensure event shape is rule-matchable — recommended envelope: `{ts, severity, event:"audit.<verb>", app, actor:{sub, tenant}, target:{kind, id}, outcome, msg}`. Same pattern applied to PF lands the next 101xxx block in Phase 11. **Trigger:** 10.1.4 commit lands + PT emits its first audit event. **Watch for:** events falling through to chart's catch-all rule 100001 (the same false-high pattern that surfaced in #27); PT events should hit their 101xxx range, not 100001. | small (~30 min once event shape is settled) | ⬜ Open | No — without these rules PT events still arrive in the indexer and are searchable in Discover; they just hit chart's generic rules instead of secforge-tuned ones (cosmetic, same as pre-#27) |
| 31 | 2026-05-05 | 10.1.3+ | Codify the Project Tracker schema-evolution workflow before the first post-cutover Prisma schema change. Today's flow (post-Phase-10.1.3) requires three coordination points per change: (a) PT-repo Prisma schema edit + `npx prisma migrate dev --name X` against local docker-compose, (b) platform-repo copy `prisma/migrations/<TS>_X/migration.sql` → `apps/project-tracker/migrations/00N-X.sql` PLUS write matching RLS policy in `apps/project-tracker/migrations/00N+1-rls-X.sql` for any new multi-tenant tables, (c) cluster apply via `BAO_TOKEN=<admin> bash apps/project-tracker/migrations/apply.sh`. **Three deliverables:** (1) `docs/03-runbooks/project-tracker-schema-evolution.md` documenting the 8-step workflow, the three forget-traps (`tenantId` field on new models, RLS policy on new tables, PT/platform-repo drift), and the recovery procedure if cluster falls behind local; (2) switch `apps/project-tracker/migrations/apply.sh` from hand-rolled `psql <list-of-files>` to `prisma migrate deploy` against the cluster (idempotent via Prisma's `_prisma_migrations` table) + a separate idempotent RLS apply step (`CREATE POLICY ... IF NOT EXISTS` + `ENABLE ROW LEVEL SECURITY` is idempotent on already-applied tables); (3) bash CI lint at `apps/project-tracker/migrations/lint-schema.sh` that fails commit if any `model X` block in `prisma/schema.prisma` lacks `tenantId`, OR any `CREATE TABLE` in the latest migration.sql doesn't have a matching `ENABLE ROW LEVEL SECURITY` line in the platform-repo RLS files. Wire as a pre-commit hook in both repos. **Trigger to act:** first time you want to add a model, column, or relation to PT post-cutover. The pain of doing it manually once will surface what the runbook needs to say; don't pre-build the ergonomics blind. **Watch for:** if a schema change happens without #31's deliverables in place, the operator should still complete it manually and THEN come back to write the runbook from notes — the runbook will be better for having walked through the actual flow once. Same pattern lifts and shifts to Phase 11 (Proposal Forge); PF has more models + real production data, so the discipline matters more there. | small (~1-2 hr total — runbook is the bulk; apply.sh swap is ~20 min; lint script is ~30 min) | ⬜ Open | No — but skipping it sets up a security-gap forget-trap (new multi-tenant table without RLS = open across tenants until manually fixed); should land BEFORE Phase 11 (PF integration) where the discipline matters more |
| 32 | 2026-05-18 | ecosystem | Keycloak → Ecosystem Control user-sync gap. Control's `organization_memberships.user_id` and `organizations.created_by_user_id` are populated with whatever Keycloak `sub` was passed in when the row was created, with no later reconciliation. When the Keycloak user is recreated (realm reset, account re-registered, federation rebound, etc.), the user gets a new `sub` but Control's rows still reference the old one. Result: the new user appears to have zero memberships from Control's perspective, Member Hub's `/auth/callback` correctly chooses not to set the active-org cookie (`pickInitialActiveOrg(emptyList) → null`), and the Member Hub UI shows "Active org: none" even though an org "for them" exists in Control. **Surfaced:** Member Hub dev session 2026-05-18 — `/members` showed "no members yet" for a freshly-signed-in user; investigation found user's Keycloak sub `91c6cee3-…` while Control's only membership row was for `db83f3e1-…` from an earlier Keycloak state. **Manual fix applied today:** direct UPDATE on `organization_memberships.user_id` + `organizations.created_by_user_id` in `control` DB, then direct `WriteRelationships` to SpiceDB via inline node script `kubectl exec`'d into the control api pod (writing `organization:<id>#member@user:<new-sub>` + `organization:<id>#administrator@user:<new-sub>`). Verified end-to-end: `permission check ... view ...` returned `HAS_PERMISSION`. **Fix candidates (pick one):** (a) Keycloak Admin Events SPI listener → Control webhook that fires on user-create / user-delete / user-update, reconciles `organization_memberships` by email or some stable attribute and writes corresponding SpiceDB updates via the existing `spicedb-sync.ts` reconcilers; (b) add a Control admin endpoint (and matching UI) for the operator to remap user_id without going to the DB + SpiceDB directly — narrower scope, manual trigger, but bounds the worst case; (c) store a stable "federation identity" (email or Keycloak federation-id claim) as the canonical key in Control with the sub as a denormalized lookup that auto-refreshes on each `/me` call. **Watch for:** spicedb-sync.ts at `Ecosystem Control/src/api/lib/spicedb-sync.ts:145-167` is the existing reconciler for the member relation — whichever fix lands needs to invoke it so SpiceDB and Postgres don't drift again. **Trigger:** before opening the platform to multiple human users in any environment, OR the next time Keycloak users get reseeded (every reset compounds the orphan pile). | medium (~2-4 hr depending on fix choice; (b) is smallest, (a) is the proper fix) | ✅ Closed 2026-05-19 (option (c) — email-anchored auto-reconcile in /me. Migration 033 adds `user_email`/`created_by_user_email` columns to the 3 active-state tables (organization_memberships, org_user_roles, organizations). INSERT sites in orgs.ts/sub-orgs.ts/invitations.ts/members.ts populate the new columns. /me handler in routes/me.ts self-heals when memberships-by-sub returns 0: looks up by lower(email) → atomic transactional UPDATE of all 3 tables → invokes the 3 spicedb-sync reconcilers per affected org. Audit-log + invited_by + workflow.initiator intentionally untouched (history). One-off backfill at src/scripts/backfill-user-email.ts for existing rows via Keycloak Admin API; orphaned rows whose user no longer exists in Keycloak are left NULL by design. tsc passes.) | — |
| 33 | 2026-05-18 | per-app | Audit log hardening — hash-chained rows + nightly signed checkpoints + git-anchor. Today (post Member Hub B3 minimal tier 2026-05-18) every app's `audit.event` table is append-only at the DB level (REVOKE UPDATE/DELETE + RLS-deny-U/D policies) but rows are NOT hash-chained. Tampering by anyone with DB-owner access (CNPG postgres superuser, the `_owner` role used by migrations) is undetectable. Full skill spec at `Member Hub/.claude/skills/audit-logging/SKILL.md` calls for: (1) two new BYTEA columns `prev_hash` + `full_hash` on `audit.event` populated by `lib/audit.ts`'s record() — acquire `pg_advisory_xact_lock(hashtext('audit_chain'))`, SELECT prev row's full_hash, compute `full_hash = sha256(prev_hash || canonical_json(this row minus full_hash))`, INSERT atomically; (2) optional `signed_root` column populated by a nightly CronJob that recomputes the chain, signs the latest full_hash with an Ed25519 key (OpenBao Transit, not Infisical as the upstream skill spec says), and commits `(date, latest_id, root, signature)` tuple to a public `audit-anchors` git repo so tampering requires git history rewrite + private-key compromise; (3) `workers/audit-verifier/` worker that recomputes the chain, blocks new writes via a feature flag + pages on-call if it finds a mismatch; (4) `audit.read` SECURITY DEFINER function gated by an `auditor` SpiceDB relation (replaces the minimal-tier RLS-by-org SELECT policy with something narrower than "anyone in the org can read"). Forward-compatibility was preserved in the minimal-tier migration: the chain columns can be added NULL-able + back-filled in a single one-off worker run. **Scope:** Member Hub today, Proposal Forge + Project Tracker once they ship audit. Each app gets its own `audit.event` table (no cross-app audit DB — same tenancy boundary as the rest of their data) but the verifier worker pattern is reusable. **Trigger to act:** before any production tenant onboarding where the audit log is load-bearing for compliance / dispute resolution / incident response — currently no app has a real tenant. Member Hub's minimal-tier table is fine for dev iteration. **Watch for:** the `audit-anchors` git repo needs to live somewhere stable and publicly readable (e.g. a dedicated repo in the SecForge GitHub org); auto-commit credentials are themselves a sensitive surface, scope them to push-only on that single repo. | medium-large (~1 day per app for chain + verifier; ~half-day for the cron + git-anchor + Ed25519-via-Transit wiring that's shared across apps) | ✅ Closed 2026-05-19 (all 4 parts for Member Hub. **Part 1**: migration 039 adds prev_hash/full_hash/signed_root + canonical_row_json/compute_full_hash/set_full_hash helpers; lib/audit.ts rewrites record() to chain under pg_advisory_xact_lock + read-then-INSERT-then-set_full_hash; existing rows backfilled in-place. **Part 2**: OpenBao Transit Ed25519 key `audit-signing` + `audit-signer` policy + `member-hub-audit-signer` k8s-auth role; public repo <https://github.com/jaupole/secforge-audit-anchors> with README + per-app folders; nightly CronJob `12-audit-anchor-cronjob.yaml` runs at 00:17 UTC, signs latest full_hash via Transit HTTP API, commits JSON anchor with key version + public key. **Part 3**: migration 040 adds audit.tamper_flag + audit_verifier role; workers/audit-verifier/verify.ts walks the chain in batches and flips the flag on mismatch; CronJob `13-audit-verifier-cronjob.yaml` runs every 5 min; lib/audit.ts reads the flag and refuses writes when set; src/scripts/clear-audit-tamper-flag.ts is the operator escape hatch (records the clearing as a fresh audit.event before resuming writes). **Part 4**: ecosystem.zed adds `auditor: user` relation + `read_audit = auditor + administer` permission on organization; Member Hub /api/v1/audit/* gated on read_audit (superset of prior `administer`, adds auditor tier). Runbook at `docs/03-runbooks/audit-anchors.md`. One-time per-app setup (GitHub PAT + audit_verifier DB password) documented; Proposal Forge + Project Tracker will mirror the pattern from Member Hub.) | — |
| 34 | 2026-05-18 | platform | **🚨 BLOCKING all new-app cluster bootstrap. PRIORITY: bumped 2026-05-18 evening — also gates centralized vendor-credential encryption (Postmark server tokens being added to Control's `organization_email_config` table will land plaintext until Transit can wrap them).** OpenBao has no working admin recovery path on this cluster. Surfaced 2026-05-18 during Member Hub Track C cluster-bootstrap attempt — needed to write a per-app policy + k8s-auth role + JWT-auth role + seed `secret/data/apps/member-hub/runtime`, all of which require admin scope. Tested every documented path: (a) `admin-break-glass` k8s-auth role from `docs/03-runbooks/openbao-recovery.md` § "Kubernetes auth break-glass" → `invalid role name` (was supposedly staged in Phase 5.6, not present in current state); (b) `bao operator generate-root -init` → `405 unsupported operation` (a known OpenBao 2.5.3 + Transit auto-unseal limitation already documented IN the recovery runbook itself); (c) common OIDC role names (admin, break-glass, emergency, platform-admin, openbao-admin, super-admin, operator) → all `invalid role name`; (d) operator's `jason.upole` OIDC login → per-user role `Oidc-jason.upole` with narrow scope (can list secret engines in UI, can't `lookup-self`, can't read its own policies, can't list/write policies or auth/k8s roles); (e) operator's `jaupole` Keycloak realm-admin account → no matching OpenBao OIDC role configured to log in with; (f) operator's stored "s." tokens → all 26-char narrow-scope (presumably auto-issued role tokens, not root). **Net:** there is currently no path to mint an admin/root OpenBao token without external recovery (Velero snapshot of an earlier state when admin existed, OR the original `bao operator init` printed root token from cluster bootstrap). **Workaround in use today:** new apps stay in laptop-hybrid-dev mode — talk to prod Keycloak + Control + SpiceDB over public ingress + tailnet, no cluster footprint. Member Hub is in this state. Works fully for development; blocks production deploy. **Fix candidates:** (1) Velero browse for an earlier openbao-* state that includes a working admin path (per `[[project_2026_05_16_ghcr_cutover_session]]` we've done Velero-based OpenBao recovery before — re-do it). (2) Locate the original `bao operator init` root token from cluster-bootstrap session (Phase 5.3). (3) Build a one-shot admin-mint pod that runs IN the openbao SA's trust context with a manually-attached `admin` policy via direct Postgres write to OpenBao's storage backend (drastic, last-resort, requires understanding OpenBao's storage schema). (4) Recreate the cluster (not realistic). Once admin is restored: immediately (a) create the `admin-break-glass` k8s-auth role per the recovery runbook so this exact situation doesn't recur, (b) update the recovery runbook with whatever NEW invariants we discover, (c) close this entry and the documentation drift in the same PR. **Trigger:** before any new app needs cluster deploy. Member Hub already affected; Proposal Forge will be next. **Severity:** HIGH (multi-week blocker if not addressed; touches every future per-app bootstrap). | medium-large (~half-day if Velero snapshot has good state; multi-day if storage-backend surgery needed) | ✅ Closed 2026-05-19 (Velero+kopia restore from `daily-everything-20260517020048` + manual kopia-CLI Job for the two volumes Velero PVR stalled on (openbao-0 + openbao-seal-0 — same stale-pod-UID issue as `[[topolvm-csi-investigation-2026-05-16]]`). Operator OIDC `admin` role was in the 05-17 backup so login restored cleanly. Then created `auth/kubernetes/role/admin-break-glass` (SA `openbao/openbao` → admin policy, 1h ttl); smoke-tested with `bao write auth/kubernetes/login role=admin-break-glass jwt=$SA_JWT` from inside `openbao-0` — returned a token with policies `[admin, default]` and lease=3600. Folded the role creation into `platform/components/05c-openbao-configure.sh` so cluster rebuilds get it for free (the runbook claim is now true). Velero memory bumped 512Mi→2Gi in `platform/values/velero.yaml` after the controller OOMKilled mid-restore. Lost ~2657 Raft entries (12628→9971) from after 05-17 02:00Z but nothing load-bearing — Member Hub etc. wasn't in cluster yet. Full memo: `[[openbao-admin-recovery-2026-05-19]]`.) | — |
| 35 | 2026-05-18 | platform | Keycloak admin UI breaks with `Failed to fetch dynamically imported module: .../ClientDetails-<hash>.js` (and similar for other admin sub-pages) after Keycloak restarts or theme cache invalidations. Browser holds a cached `index.html` that references hashed JS chunk names which no longer exist on the server; result is a permanent error banner that survives multiple hard refreshes (browser keeps serving cached HTML; only incognito window or Ctrl+Shift+Del clears it). **Surfaced 2026-05-18 twice in one Member Hub dev session** — once when probing OIDC roles, once when adding a redirect URI to the `member-hub` client. **Workaround used today:** open Keycloak admin in private/incognito window every time. **Root cause:** no `Cache-Control: no-cache, must-revalidate` on the admin HTML responses + hashed asset names rotate on theme/version changes. **Fix (two changes, ~30 min total):** (1) ingress-nginx annotation on `platform/manifests/keycloak/ingress.yaml` setting `more_set_headers "Cache-Control: no-cache, must-revalidate"` for `^/admin(/|$)` paths and `Cache-Control: public, max-age=31536000, immutable` for `^/admin/.*\.(js\|css\|woff2\|png\|svg)$` (chunks are content-hashed, safe to cache forever); (2) on the Keycloak StatefulSet env, pin theme cache to a known shape: `KC_SPI_THEME_STATIC_MAX_AGE=2592000`, `KC_SPI_THEME_CACHE_THEMES=true`, `KC_SPI_THEME_CACHE_TEMPLATES=true` — so asset hashes only rotate on actual Keycloak version bumps, not every pod restart. **Trigger to act:** any time the workaround friction outweighs the fix friction (probably soon — this hit twice today). **Watch for:** the ingress configuration-snippet annotation needs the `more_set_headers` module available in the ingress-nginx build (it's the standard NGINX more-headers module, included in ingress-nginx default image); if missing, fall back to a server-snippet approach. Test with a curl after applying: `curl -sI https://auth.secforge.dev/admin/master/console/ \| grep -i cache-control` should show `no-cache, must-revalidate`. | small (~30 min including the post-apply verification + Keycloak pod restart) | ✅ Closed 2026-05-19 (investigation found the prescribed fix mostly redundant against current Keycloak behaviour. **The caching is already correct:** `/admin/master/console/` HTML already returns `Cache-Control: no-cache` — Keycloak emits this itself, so the browser always revalidates the index.html; `/resources/<version>/.../*.js` already returns `max-age=2592000` and the `<version>` path segment is content-versioned, so a Keycloak upgrade serves assets under a *new* path (no stale-name collision). **Prescribed fix #1 (ingress no-cache header) rejected:** it's redundant (Keycloak already sets `no-cache` on the HTML) AND blocked — ingress-nginx 1.15 ships `allow-snippet-annotations: false`; the `configuration-snippet`/`more_set_headers` approach would need that flag flipped cluster-wide, re-opening an arbitrary-nginx-config-injection surface, not worth it for a header already present. **Delivered instead:** (a) created `platform/manifests/keycloak/06-ingress-admin.yaml` codifying the live `keycloak-admin` Ingress (path `/admin`, tailnet+pod-CIDR+host-IP whitelist) that had existed on-cluster since ~2026-05-13 with NO repo manifest — pure drift, now source-of-truth; (b) added prescribed-fix-#2's theme options to `04-keycloak-cr.yaml` `additionalOptions` as `spi-theme-static-max-age=2592000` / `spi-theme-cache-themes=true` / `spi-theme-cache-templates=true` — declarative pins matching Keycloak's production-mode defaults so a future upstream default flip can't silently change asset behaviour. **Not applied live:** the theme options already match effective defaults (zero behaviour change) and `04-keycloak-cr.yaml` carries the known `image: sha256:000…` placeholder drift, so a blind `kubectl apply` would ImagePullBackOff Keycloak; the new Ingress manifest is an exact codification of the live resource (re-apply is a no-op). Both take effect on the next legitimate Keycloak CR apply, gated on resolving the image-digest drift (tracked separately in `04-keycloak-cr.yaml`'s TODO). **Residual (inherent, not closable here):** an admin tab left open ACROSS a Keycloak *image upgrade* breaks on the next lazy-chunk import — the in-memory SPA still references the previous resource-version path the new pod no longer serves. A normal page reload fixes it (the no-cache HTML pulls fresh asset paths). This is SPA-vs-backend-version-skew in Keycloak's own admin console. Operator workaround downgraded from "incognito window every time" to "reload the tab after a Keycloak upgrade".) | — |
| 36 | 2026-05-19 | platform | `wazuh-manager` livenessProbe crash-loop. The #20 fix (closed 2026-05-19) set the probe to `set -e; pgrep -x wazuh-apid >/dev/null; pgrep -x wazuh-remoted; pgrep -x wazuh-analysisd`. `pgrep -x` matches the process `comm` name exactly — but `wazuh-apid` is **not a binary**, it runs as `python3 /var/ossec/api/scripts/wazuh_apid.py`, so its `comm` is `python3` and `pgrep -x wazuh-apid` returns nothing. `set -e` then aborted the probe script on the very first clause, every cycle. Net effect: kubelet killed `wazuh-manager-0` at exactly 120s initialDelay + 3×30s failureThreshold = 3m30s, on a loop — **52 restarts in ~5h**, and the Wazuh dashboard showed "API connections could be down or inaccessible" because the API daemon went down with the pod every few minutes. `wazuh-remoted`/`wazuh-analysisd` are C binaries so their `pgrep -x` clauses worked — `apid` alone broke it. Surfaced 2026-05-19 by the operator reporting the dashboard API error. **Fix (2026-05-19):** probe rewritten to `s=$(/var/ossec/bin/wazuh-control status 2>/dev/null \|\| true)` then `echo "$s" \| grep -q "<daemon> is running" \|\| exit 1` for apid/remoted/analysisd — `wazuh-control status` is Wazuh's own authoritative daemon check and reports apid correctly regardless of launch method. (`\|\| true` because `wazuh-control status` exits 1 whenever optional daemons like clusterd/maild/csyslogd are off — normal on a single-node manager; the grep on captured output is the real signal. `pgrep -f wazuh_apid` was rejected — the probe script's own cmdline contains the daemon string, so `-f` self-matches.) Applied live via `kubectl patch statefulset wazuh-manager` (strategic-merge patch-file) + pod recreate — manager came back `Running` `0` restarts. Mirrored into the vendored chart `platform/manifests/wazuh/vendor-chart/templates/manager/statefulset.yaml`. | small (done) | ✅ Closed 2026-05-19 | — |
| 37 | 2026-05-20 | platform | Commit-signing config has drifted from ADR-0021 (§3 mandates SSH-signed commits: `gpg.format=ssh`, `user.signingkey`, `commit.gpgsign=true`, `tag.gpgsign=true`). Current state of the Security Forge repo: `gpg.format=ssh` + `user.signingkey=/home/jaupo/.ssh/id_ed25519.pub` are set, but `commit.gpgsign` is **unset** — a plain `git commit` lands an **unsigned** commit. And `user.signingkey` is a WSL-filesystem path that does not resolve from Windows git (Git Bash, where the repo physically lives on `C:\`), so an explicit `git commit -S` fails with `error: Couldn't load public key /home/jaupo/.ssh/id_ed25519.pub: No such file or directory`. Net: there is no working signed-commit path from the Windows host; commits since the drift are unsigned — including the 2026-05-20 `infrastructure/wazuh/` retirement commits (`3d48a30`, `f28a3f7`, `e702585`, `1807546`). Related environment quirk: `.git/hooks/pre-commit` was generated by `pre-commit` on Windows (hard-coded Windows Python path + bash-array syntax), so it only runs under Git-for-Windows `sh`, not WSL `dash`; its CRLF line endings were repaired to LF on 2026-05-20 so it at least execs. **Fix (operator, on the Hetzner box):** decide the canonical commit environment, make `user.signingkey` an absolute path that resolves there, set `commit.gpgsign=true` + `tag.gpgsign=true`, and re-run `pre-commit install` from that same environment so the hook matches it. **Trigger:** before commit provenance becomes load-bearing — remote push, multi-actor review, or any supply-chain attestation that consumes commit signatures. | small (~30 min — git config + `pre-commit install`) | ⬜ Open | No — unsigned commits are fully functional; this is a provenance gap and blocks nothing until a remote or supply-chain consumer needs verified signatures |
| 38 | 2026-05-20 | platform | Retire the `infrastructure/` tree (the dead Local Edition) subdir by subdir. `infrastructure/wazuh/` is done — migrated to `platform/components/07o`+`07p` (commit `45b8926`). Full per-subdir audit — superseded / partial / gap / parked — at `docs/06-reference/infrastructure-retirement.md`. **Genuine content gaps to resolve before their dirs can be deleted:** `valkey/` (no production Valkey config exists anywhere in `platform/`), `secrets-guardrails/` (verify suite + weekly CronJobs, no platform home), `grafana/dashboards/` (6 dashboard JSONs not in `platform/`), `spicedb/schema.zed` + `ecosystem-schema.zed` (no `.zed` in `platform/` — confirm the live schema source first). **Landmine:** `infrastructure/istio/authzpol-strict-7c2-draft/` is parked Phase 7c-2 prep (see #21) — must NOT be deleted. Each subdir follows the wazuh playbook: content-diff vs `platform/`, migrate edition-agnostic content, repoint inbound refs (`docs/`, ADRs, live `platform/` files), then `git rm`. **Progress 2026-05-20:** 22 of 23 subdirs retired across 16 commits; every genuine-content item (Wazuh SIEM rules, Grafana dashboards, ADR-0013 Kyverno policies, the Keycloak custom image, the app/security alert rules, the SpiceDB schema) migrated into `platform/`. Only `infrastructure/istio/` remains — deferred, it retires with the Phase 7c-2 closeout (#21). | medium (multi-pass) | 🟦 22/23 done — only `istio` left (deferred to #21) | No — `infrastructure/` is all but gone |
| 39 | 2026-05-20 | platform | Flip the two ADR-0013 outbound-secrets Kyverno policies from Audit to Enforce. `no-secret-shaped-env-vars` + `legacy-secret-env-expiry` (ADR-0013 § Layer 4 — the bright-line "no secret-shaped env vars" admission guardrail) were found **missing from the production cluster** on 2026-05-20 and migrated to `platform/manifests/kyverno/policies/09`+`10` in Audit mode (per the platform Audit→Enforce convention in `12b-kyverno-policies.sh`). Scoped to the SecForge app namespaces `control` + `member-hub`. **Pre-flight finding — resolved in source 2026-05-20:** the scan found `control/TRANSIT_KEY_NAME`, a false positive (it holds the *name* of an OpenBao Transit key, not key material). Renamed to `TRANSIT_KEYNAME` in `platform/manifests/{control,member-hub}/09-backend-deployment.yaml` and in the Control repo (`src/api/config.ts` + `clients/transit.ts`); non-breaking through the transition because the Control zod schema defaults to `pii-encryption`. It takes effect on the live `control` pod at its next deploy — until then the Audit PolicyReport still shows the stale finding. **Before the Enforce flip:** confirm the running `control` pod no longer carries a `TRANSIT_KEY_NAME` env var, review PolicyReports for ~1 week, then `kubectl patch clusterpolicy no-secret-shaped-env-vars` + `legacy-secret-env-expiry --type=merge -p '{"spec":{"validationFailureAction":"Enforce"}}'`. **Also:** once Enforce is live, a weekly guardrail-verification CronJob (LIVE-mode `kubectl apply --dry-run=server` self-test) is worth building — the old `infrastructure/secrets-guardrails/` CronJobs were broken local-edition scaffolding (non-existent `app` ns, undeployed collector, offline-mode-in-a-docker-less-image) and were not ported. **✅ Closed 2026-05-22 (the flip).** Pre-check came back clean: the live `control` + `portal` Deployment templates and every running `control` pod carry no secret-shaped env var (the `TRANSIT_KEYNAME` rename is live), and no Pod uses the `legacy-secret-env` escape-hatch annotation. The only PolicyReport `fail`s were 4 stale 0-replica ReplicaSets (pre-rename revision history) — deleted, since Enforce evaluates admission (create/update) and never re-touches them. Both policies flipped to Enforce in `09`+`10` and applied (commit `0c88da3`); verified `validationFailureAction=Enforce`, `control` pods healthy, outbound-secrets PolicyReports now clean. The weekly guardrail-verification CronJob is spun off as #54. | small (the flip) + small (cronjob rebuild) | ✅ Closed 2026-05-22 | — |
| 40 | 2026-05-20 | platform | Local-Edition docs/apps cleanup — residual of the `infrastructure/` retirement (#38). The subdir deletions leave stale `infrastructure/*` path mentions in prose/comments: architecture docs (`01-architecture/05-secrets-management.md`, `06-workload-identity.md`), several runbooks, and `apps/*/deploy/` manifest comments. Some runbooks (`spire-rotation.md`, `teleport-operations.md`) are themselves wholly Local-Edition and superseded by platform equivalents (`spire-ca-rotation.md`; Teleport is retired). ADR + PLAN + closed-backlog references are point-in-time records — leave them. The pass: repoint live-doc paths to `platform/`, retire the Local-Edition-only runbooks. Note `CLAUDE.md` is itself still the project's "Local Edition" file — the largest doc-tree edition artifact. **🟨 Largely addressed 2026-06-07 (doc drift sweep — see `doc-drift-audit-2026-06-07.md`):** CLAUDE.md de-local-editioned, PLAN.md replaced with prod-status, build-era tree archived to `99-archive/`, architecture docs + runbooks given production banners, Teleport/SPIFFE/hostname drift fixed. **Residual:** **2026-06-07** repointed the cleanly-mappable artifact paths in the *current* reference/architecture docs (SpiceDB schema → `platform/manifests/spicedb/`, OpenBao policies → `platform/manifests/openbao/policies/`, SPIRE manifests/values, the `05c-openbao-configure` policy-load step; de-referenced the retired `helloworld-bff.hcl` example). The remaining ~100 `infrastructure/*` mentions are concentrated in the **already-bannered local-edition runbooks** (`bff-key-rotation`, `openbao-recovery`/`-seal-unseal`, `spire-rotation`, `secrets-guardrails-*`, `keycloak-client-provisioning`, `istio-*`, etc.) where they're entangled with **retired helloworld artifacts** and reorganized scripts — those need the per-runbook rewrite treatment (the keycloak-operations approach, cf. #82), not a path-swap. | medium | 🟨 Partially done 2026-06-07 | No — stale prose mentions mislead but block nothing |
| 41 | 2026-05-21 | platform | Third-party vendor images that ship no cosign signature — the permanent-Audit half of image-signature verification. On 2026-05-21 `verify-image-signature` was split into `verify-image-signature-secforge` (Enforce — SecForge `ghcr.io/{secforge,jaupole}/*` images; our CI signs them keyless via GitHub Actions OIDC; portal + control confirmed verifying) and `verify-image-signature-vendors` (Audit — CloudNativePG + SPIFFE). The vendors policy can never go fully green: of 10 in-scope third-party images, 6 carry NO cosign signature — all 4 CloudNativePG (`cloudnative-pg` operator publishes SLSA *attestations* (`.att`) but no signature; `postgresql`, `plugin-barman-cloud`, `plugin-barman-cloud-sidecar` have nothing attached at all), plus `spiffe/spiffe-helper:0.11.0` and `spiffe/spire-controller-manager:0.6.4`. The other 4 SPIFFE images (`spire-server`, `spire-agent`, `oidc-discovery-provider`, `spiffe-csi-driver`) ARE properly keyless-signed. Confirmed 2026-05-21 via `cosign verify` + `cosign tree` on the box, cross-checked against Kyverno's own admission log (`no signatures found`). So `verify-image-signature-vendors` stays Audit indefinitely — its PolicyReport permanently shows those 6 failing (meaningful signal, not noise) and it cannot be flipped to Enforce as-is (doing so would block every CloudNativePG pod — operator, all Postgres instances, barman backups — plus `spire-controller-manager`, cluster-wide). Note `spiffe-helper` runs as a sidecar inside the SecForge `control` pods, so we ship an unsigned third-party binary next to our own app. Fix candidates: (a) for CloudNativePG, switch the rule from signature verification (`attestors:`) to attestation verification (`attestations:` — CNPG does publish SLSA provenance); (b) digest-pin the 6 unsigned images so the bytes are at least immutable absent a signature; (c) self-build + sign `spiffe-helper` / `spire-controller-manager`, or drop them for signed alternatives; (d) narrow the vendor rules' `imageReferences` to only the images that actually sign, so the policy could eventually Enforce a true subset. **Trigger:** when third-party supply-chain assurance becomes load-bearing — production tenant onboarding, a compliance attestation, or an incident playbook needing provenance. **Cross-ref:** #39 (the separate ADR-0013 secret-shaped-env-var Kyverno policies' own Audit→Enforce flip). | medium | ⬜ Open | No — `verify-image-signature-secforge` (our images) is Enforce and green; the vendor side is Audit-only visibility and gates nothing |
| 42 | 2026-05-21 | platform | helloworld residual prose cleanup. The Phase 9 helloworld integration-reference demo was decommissioned 2026-05-21 (commit `e103f10`): `apps/helloworld-{frontend,backend,bff}/` (53 files — source, deploy manifests, SBOMs), the `helloworld-{backend,bff}.hcl` OpenBao policies, and `docs/01-architecture/10-helloworld-demo.md` were removed; `bff-key-rotator.hcl` trimmed (kept — still serves the 3 real BFFs); the CLAUDE.md "Where to find things" row dropped; the stale OpenBao policy files cleared from the box deploy-copy. The app is fully gone (it was never deployed — there is no `app` namespace on the cluster). **Residual:** ~20 files still name helloworld as a comment, worked-example, or test-fixture — `apps/lib/api-auth/*` + `apps/lib/secrets/*` comments and `*_test.go` fixtures, `apps/security-events-collector/*` (comments + README), `apps/authzen-facade/deploy/05-vso-binding.yaml` comment, the SpiceDB `schema.zed` + `ecosystem-schema.zed` comments and the 4 `tests/*.yaml` files (which use `tenant:helloworld` / `app:helloworld-app` as arbitrary fixture object IDs), and several runbooks that use helloworld as the worked example (`new-app-bootstrap.md`, `secrets-library.md`, `api-auth-library.md`, `migrate-env-to-openbao.md`, `bff-key-rotation.md`). ADRs, the Phase 9 retrospective, and `.claude/audit-history/` are point-in-time records — leave them. The pass: repoint runbook examples to a live app (control / member-hub) or genericise them; the SpiceDB test fixtures can be renamed for clarity or left (arbitrary IDs, functionally inert). Mirrors the #40 stale-prose cleanup from the `infrastructure/` retirement. | medium | ⬜ Open | No — stale comments/examples mislead but block nothing; helloworld is fully removed as a deployable artifact |
| 43 | 2026-05-21 | platform | checkov IaC gate residual findings. `gate_iac` (run-gates.sh) was repaired + triaged 2026-05-21: the merge-slow-gates pipeline's IaC gate had two stacked bugs — an ARG_MAX crash (checkov's multi-MB JSON via `jq --argjson`) and an array-discard (checkov emits a per-framework JSON array the jq threw away) — then a P1/P2/P3 finding triage. It runs clean and green; `gate-iac` went from a crash → 15 (P3 close) → **9** after the digest-pin landed — all non-blocking, surfaced in the run's `diagnose.json`. Everything genuinely fixable on prod manifests was fixed or documented-accepted (`checkov.io/skip` + `.checkov.yaml` skip-check) in the P1/P2/P3 triage. **Renovate digest-pin half — DONE.** Renovate had never actually run (App installed 2026-05-21; `renovate.json` `managerFilePatterns` were bare regex that Renovate reads as globs — fixed in `0d38f79`; `keycloak-image-build`'s build context was repointed off the retired `infrastructure/` tree in `ff88c54`). Digest-pin PR #2 merged 2026-05-21 (`8bdcc12`): 6 platform images digest-pinned, and Renovate now keeps image / GitHub-Action / dependency digests current going forward. **Residual — 9 findings, none prod-blocking:** 5× CKV_K8S_43 — 3 are `member-hub/{08,09,13}` `${MEMBER_HUB_TAG}` envsubst-placeholder images (Renovate can't pin a template var; they pin when Member Hub deploys with a real tag), 2 are on `apps/authzen-facade` + `apps/security-events-collector`; plus 4× CKV_K8S_38 / CKV_K8S_31 on those same two app-tier reference apps. **Trigger:** the member-hub three resolve at Member Hub deploy; the six app-tier when/if authzen-facade + security-events-collector are promoted to deployed. **Cross-ref:** #41 (the image-signature half of the same supply-chain work). | small — deferred to those deploys | ⬜ Open | No — gate is green; residual is member-hub-deploy-time + app-tier |
| 44 | 2026-05-21 | platform | jwx (`github.com/lestrrat-go/jwx`) major-version migration — parked. `apps/lib/api-auth` (`client.go` + `middleware.go`) does JWT signing/parsing, JWS parse/verify, JWKS fetch+cache and DPoP-proof verification on `github.com/lestrrat-go/jwx/v2 v2.1.6`. Renovate PR #15 offered a jump straight to `v4.0.2` — but v4.0.2 was released 2026-05-07 (~2 weeks old at parking time), it is a *double-major* jump skipping the entire v3 API rewrite, and it forces `go 1.25 → 1.26`. Migrating security-critical JWT/DPoP verification code onto a 2-week-old crypto-library major is a security-regression risk that a green test suite would not fully catch; v2.1.6 is current with no open CVE. **Action taken 2026-05-21:** PR #15 closed; a `renovate.json` packageRule now parks jwx *major* updates behind `dependencyDashboardApproval` + a 60-day `minimumReleaseAge` (v2 minor/patch updates still flow normally; vulnerability alerts still override). **Revisit ~2026-07-21:** re-evaluate then — likely target the more-soaked `v3.x` line (`v3.1.1`) rather than v4, since v3 has had longer in the field. The migration is real code work (jwx v3 substantially changed the `jwa`/`jwt`/`jwk` APIs) and must be verified with `go build ./...` + `go test ./...` in `apps/lib` (test files in `api-auth/` + `oidc/` exercise the surface). **Trigger:** the soak elapsing, or a jwx v2 CVE forcing the move sooner. | small (decision) + medium (migration) | ⬜ Open — parked, revisit ~2026-07-21 | No — v2.1.6 is current and unaffected by any open CVE |
| 45 | 2026-05-21 | platform | Wazuh SIEM image upgrade — parked. Renovate's non-major sweep surfaced bumps for all four Wazuh images (`wazuh/wazuh-agent`, `wazuh/wazuh-dashboard`, `wazuh/wazuh-indexer`, `wazuh/wazuh-manager`). Moving the four together is a coordinated SIEM version change — indexer + manager + dashboard must move in lockstep, and the vendored chart at `platform/manifests/wazuh/vendor-chart/` carries local patches (the #36 livenessProbe fix, chart-native OIDC templates, decoder/rule config) that must be re-checked against any new image. It is not a blind dependency merge. **Action taken 2026-05-21:** a `renovate.json` packageRule gives the four Wazuh images their own `wazuh SIEM images` group with `dependencyDashboardApproval`, so they surface as one distinct parked dashboard entry instead of being folded into the all-non-major group. **Procedure when done:** review the target Wazuh release notes, re-verify the vendored-chart patches still apply, bump the four image refs together, deploy in a maintenance window, confirm agent→manager→indexer pipeline + dashboard OIDC still work. **Trigger:** a deliberate maintenance window — handle this BEFORE Postgres v17 (#46). **Done 2026-05-21:** upgraded 4.14.4 → 4.14.5 (security patch — fixes critical CVEs: cluster memory-allocation from a crafted packet, Authd path traversal, Remoted heap overflow, an RBAC privilege-escalation bypass, an Analysisd buffer overflow). Repo bump `028a20f`; deployed to the box as helm revision 18. indexer / manager / dashboard + the standalone agent DaemonSet all verified Running at 4.14.5, the `secforge-prod-k8s` agent reconnected to the manager. The deploy surfaced + fixed stale repo drift in `platform/values/wazuh.yaml` — see #47. | medium (~half-day, coordinated) | ✅ Closed 2026-05-21 | — |
| 46 | 2026-05-21 | platform | CloudNativePG Postgres 16 → 17 major-version upgrade — parked. Renovate flagged `ghcr.io/cloudnative-pg/postgresql` v16 → v17. A Postgres major version is an engine upgrade, not a routine image bump: CNPG performs it via an in-place `pg_upgrade` (or a logical-replication switchover) and it must run as a planned maintenance procedure. **Procedure when done:** confirm the running CNPG operator version supports PG 17; take + verify a fresh backup (Velero/kopia + the barman object-store backup) and rehearse the restore; follow the CNPG major-upgrade path (`imageName`/`imageCatalogRef` bump + `pg_upgrade`); validate every app DB (`control`, `member-hub`, plus PT/PF when they cut over) post-upgrade; keep the restore-from-backup rollback ready. Run in a maintenance window. **Trigger:** a deliberate maintenance window, AFTER the Wazuh upgrade (#45). **Progress 2026-05-21 — SpiceDB canary done.** Mechanism proven on `spicedb/secforge-spicedb-db`: CNPG 1.29's declarative offline `pg_upgrade` took it 16.10 → 17.6 (target `ghcr.io/cloudnative-pg/postgresql:17.6-bookworm` — the latest available, and ≥17.6 which the CNPG `max_slot_wal_keep_size` caveat requires). Triggered by a `kubectl patch` of `.spec.imageName`; the cluster fenced for ~3 min, came back healthy on 17.6, `relation_tuple` data intact, the SpiceDB app reconnected. Pre-upgrade barman backup `secforge-spicedb-db-preupgrade-pg17` was taken first. Repo `platform/manifests/spicedb/02-cnpg-cluster.yaml` bumped to 17.6. **serverName note:** barman archive separation must be set as the `serverName` plugin parameter inside the Cluster `.spec.plugins[].parameters` (NOT on the ObjectStore — CNPG rejects it there); it was skipped for the SpiceDB canary (reconstructible data) but is **required for `control` + `keycloak`** so the pre-upgrade PG16 backup stays restorable across the major bump. **Remaining: `control/control-db` + `keycloak/secforge-keycloak-db`** — same procedure, each in its own short window (DB fenced → app down briefly), with serverName separation. **Cross-ref #47:** the CNPG `Cluster` CRs live in the box deploy-copy — diff repo vs live before applying. **Closed 2026-05-21 — all 3 done.** `control/control-db` (20 public tables) + `keycloak/secforge-keycloak-db` (88 tables) upgraded 16.10 → 17.6 the same way: fresh pre-upgrade barman backup, `serverName` separation (`control-db-pg17` / `secforge-keycloak-db-pg17`, set as the Cluster plugin parameter), `kubectl patch` of `.spec.imageName`, ~3 min fenced each — all returned healthy with data intact and the apps (Control, Keycloak) reconnected. All three `02-cnpg-cluster.yaml` manifests bumped to 17.6 + box deploy-copies synced; the keycloak manifest also gained the `barmanObjectName: minio-backup` plugin parameter that was live but missing from the repo (drift — see #47). | medium (~half-day, planned + backup-gated) | ✅ Closed 2026-05-21 | — |
| 47 | 2026-05-21 | platform | Audit `platform/values/*.yaml` (and other box deploy-copy files) for box-vs-repo drift. Surfaced 2026-05-21 during the Wazuh 4.14.5 upgrade (#45): the repo's `platform/values/wazuh.yaml` was stale — seven production-config items had been hand-applied to the box's deploy-copy and never committed back (dashboard + indexer Keycloak OIDC federation, the indexer JVM truststore opts, the dashboard ingress `whitelist-source-range` IP allowlist, both indexer/manager storage classes, a Velero backup-volumes-exclude annotation). The box's `~/secforge/platform/` is a non-git copy, so hand-edits there drift silently. Syncing the stale repo file to the box clobbered the live config and the helm upgrade failed at templating (`dashboard.security.openid` nil-pointer) — caught before any apply, no outage. The wazuh overlay is now reconciled (reconstructed from the deployed release via `helm get values`, verified to render manifests identical to live bar the image bump), but other overlays may carry the same drift; a redeploy-from-repo of any drifted component would silently drop live config — the wazuh ingress IP allowlist nearly went that way, a real access-control regression. **The pass:** for each `platform/values/*.yaml` and each helm-deployed component, diff the repo source against the live release (`helm get values <release> -o yaml`) and reconcile. **Longer term:** the box deploy-copy should be a git checkout (or the deploy should run from one) so drift cannot accumulate silently. **Done 2026-05-21 — audit complete.** All 22 helm releases that have a `platform/values/*.yaml` overlay were scanned (repo file envsubst-rendered vs `helm get values <release> -o yaml`, semantic deep-diff): 13 clean, 9 drifted. **The dangerous direction — repo stale, so a redeploy would clobber a live fix — applied to exactly one: `spire`** (repo `spiffe-csi-driver` memory 64Mi/256Mi vs the live 2026-05-12 OOM fix 128Mi/512Mi); fixed in `platform/values/spire.yaml` + box-synced. The other 8 are not that danger — `velero`/`kps`/`trivy-operator`/`ingress-nginx` are repo-ahead (repo correct, live behind); `loki`/`tempo` carry a literal un-rendered `${STORAGE_CLASS}` in live values (inert — PVCs already bound to `local-path`); `minio` live value stale-but-inert; `openbao-seal` differs only in an init-container script vs a 2026-05-07 release. The repo-ahead / undeployed items are tracked in #49. | medium | ✅ Closed 2026-05-21 | — |
| 48 | 2026-05-21 | platform | Keycloak CR cutover — deploy the SecForge custom Keycloak image (CVE-refreshed base) and move the IdP off the unsigned in-cluster-built image onto the GHA-signed image. Attempted 2026-05-21 (operator asked for it after the #45 Wazuh upgrade); found **three stacked blockers** that make this a planned task, not a digest swap. **(1) Image-name mismatch.** `keycloak-image-build.yml` sets `IMAGE_NAME` to `${{ github.repository_owner }}/keycloak`, so it builds/pushes/signs `ghcr.io/jaupole/keycloak`. But `04-keycloak-cr.yaml`'s `spec.image` placeholder, the `verify-image-signature-secforge` Kyverno policy, the image `README.md`, and `renovate.json` `ignoreDeps` all name `ghcr.io/secforge/keycloak` — which does not exist (registry 404). This is why the CR placeholder digest was never filled. Fix: pick `ghcr.io/jaupole/keycloak` as canonical and correct all four consumers. **(2) Signature-scheme mismatch.** The workflow installs cosign v3.0.6 (via `cosign-installer`, bumped v3→v4 by PR #16 in the 2026-05-21 Renovate safe wave). The latest build (`da29397`, digest `sha256:f0995cc3...`) has no legacy `sha256-<digest>.sig` tag — older digests do — and Kyverno's `verify-image-signature-secforge` returns "no signatures found" and **blocks the pod at admission (Enforce)**; verified with a throwaway test pod. Cosign v3 defaults to storing signatures via the OCI 1.1 referrers API rather than the `.sig` tag the cluster's Kyverno reads. Fix: either pin cosign back to v2 in `keycloak-image-build.yml`, or upgrade/configure Kyverno to verify referrer signatures — needs a decision + test. **(3) Bidirectional CR drift.** The live keycloak CR has `httpEnabled: false` + `tlsSecret: keycloak-internal-tls` (internal listener HTTPS-only) that the repo CR lacks — the repo still says `httpEnabled: true`, so applying it as-is would regress the IdP listener to plaintext; the repo CR has the #35 theme-cache `additionalOptions` the box lacks. Reconcile both ways. **Context:** the live IdP runs `localhost/secforge/keycloak:26.3.3-optimized`, an older in-cluster kaniko build; the GHA pipeline (`image/` + `keycloak-image-build.yml`) was never completed end-to-end, so the #17 base-digest CVE refresh is built + signed but NOT deployed. **Prep done 2026-05-21:** `ghcr-pull-secret` created in the `keycloak` namespace (copied from the `control` ns — a proper VSO `VaultStaticSecret` binding is a follow-up, since the control copy is VSO-rendered). **Order to fix:** name mismatch → signature scheme → CR reconciliation → set `spec.image` to the verified `ghcr.io/jaupole/keycloak@sha256:...` digest + `imagePullSecrets` → apply (IdP restart). **Sub-task folded in 2026-05-21 — make the keycloak version customManager functional:** it currently captures only the base-image digest; the version (`26.3.3`) is templated via `currentValueTemplate`, not a real string in the Dockerfile, so Renovate errors on any keycloak *version* bump ("update failure") — keycloak version updates are disabled in `renovate.json` (an `enabled:false` packageRule) until this is fixed. While editing the image Dockerfile for the cutover: add `ARG KEYCLOAK_VERSION=26.3.3`, change the FROM to `quay.io/keycloak/keycloak:${KEYCLOAK_VERSION}@${KEYCLOAK_BASE_DIGEST}`, extend the customManager `matchStrings` to capture `KEYCLOAK_VERSION` as `currentValue`, then drop the `enabled:false` keycloak-version rule (keep `dependencyDashboardApproval`). Renovate then resumes surfacing keycloak versions — visible, applicable, dashboard-gated. **✅ Closed 2026-05-22 — cutover landed.** The IdP runs `ghcr.io/jaupole/keycloak@sha256:90bbcd9dd270372f2899c8400dddf779a8c6a440af97bf1cc8dabdf103a8bfa3` (Keycloak 26.3.3, base `6a7217a` CVE-refreshed, cosign-v2-signed, Kyverno-admission-verified). Commits: `1f4002d` (cosign pinned to v2 — Kyverno reads the `.sig` tag; names corrected secforge→jaupole), `dda4733` (KEYCLOAK_VERSION ARG + Renovate customManager — keycloak version bumps re-enabled, dashboard-gated), `c6f62a6`+`337e56b` (CR reconciled — signed digest, internal listener `httpEnabled:false`+`tlsSecret` kept, blacklist ConfigMap mount dropped since the image bakes the HIBP list), `191e25d` (rollback runbook). **Took 3 deploy attempts** — two latent gaps in the never-completed GHA pipeline: `6bbe73c` — the Keycloak Operator does NOT propagate `imagePullSecrets` from `unsupported.podTemplate`, so it was moved to the `keycloak` ServiceAccount; `282a2a1` — the Dockerfile baked only KC_FEATURES+KC_DB, not the `health/metrics/tracing` build-time options the CR enables, so `startOptimized` crash-looped — now baked to match the retired kaniko recipe (`image-build/00-dockerfile-cm.yaml`). Both failed attempts rolled back cleanly via the pre-cutover CR snapshot — the 3-layer recovery mechanism (CR snapshot / CNPG backup / DR) worked as designed. Follow-ups: #52 (`apply-manifest.sh` client-side apply broken for this CR), #53 (post-soak cleanup — delete the `keycloak-password-blacklist` ConfigMap + remove kaniko `image-build/`). Not separately ticketed: `ghcr-pull-secret` is still a hand-copied Secret (a VSO `VaultStaticSecret` would be cleaner); `readOnlyRootFilesystem:false` may be tightenable now the image is genuinely `startOptimized`; `keycloak-operations.md` is Local-Edition-stale outside the new cutover section. | medium-large | ✅ Closed 2026-05-22 | — |
| 49 | 2026-05-21 | platform | Pending `platform/values/*.yaml` changes surfaced by the #47 audit — repo-ahead of the live helm releases. **`velero` — DONE 2026-05-21.** The audit's "live 512Mi" was the helm *release state* (rev 8); the actual velero Deployment already carried 2Gi from a 2026-05-19 `kubectl patch` hotfix, so the cluster was never at OOM risk — only a future plain `helm upgrade` would have reverted the patch. Fixed the release state with `helm upgrade velero --reuse-values --set resources.*` (rev 9, 2Gi) — zero disruption, no pod restart; box `platform/values/velero.yaml` synced. **Re-assessed — the rest need no dedicated deploy:** `kps` (a prometheus config-reloader resources override) and `trivy-operator` (`operator.replicas: 1` — already the default — + `operator.resources`) are marginal resource tweaks; a full `helm upgrade` of those charts (kps especially is large) is disproportionate churn for the value — they apply for free on the next routine Renovate chart bump. `loki` + `tempo` carry a literal un-rendered `${STORAGE_CLASS}` in their release state, but the storage class sits in immutable StatefulSet volumeClaimTemplates with PVCs already bound to `local-path` — a `helm upgrade` cannot change it (it would fail on the immutable field); inert, leave. `minio` likewise (PVC bound to the correct `minio-local`; release-state value cosmetically stale). **`ingress-nginx` — DONE 2026-05-21.** Investigated: the live `secforge-default-headers` ConfigMap is the *correct* deliberate 2-header set (Referrer-Policy + X-Content-Type-Options), scoped down 2026-05-14 because a global CSP clobbers app-set headers (`more_set_headers` overrides, never defers) — the repo's `extraObjects` 5-header block was the stale side. Two real defects found: (a) the ingress-nginx chart has no `extraObjects` support, so that block in `values/ingress-nginx.yaml` was *dead config*, silently ignored; (b) the ConfigMap was live-only (`kubectl apply`, no repo manifest). Fixed: codified the ConfigMap as `platform/manifests/ingress-nginx/02-default-headers-configmap.yaml`, wired its apply into `00c-ingress-nginx.sh`, removed the dead `extraObjects` block. Live state unchanged (2 headers), no controller restart. | small | ✅ Closed 2026-05-21 | — |
| 50 | 2026-05-21 | platform | Box deploy-copy → git checkout — the "longer term" fix recommended in #47, executed 2026-05-21. `~/secforge/` was a non-git, `platform/`-only partial copy; box-vs-repo drift accumulated silently and invisibly. **Done:** (a) `~/secforge/` made a git checkout of `jaupole/Security-Forge` via a read-only ed25519 deploy key (pull-only, repo-scoped — least privilege); ownership normalized to `ops`; full pre-conversion backup taken, then removed once verified. (b) Non-destructive drift audit (mixed reset): 317 files the box never had (materialized), 113 bidirectionally drifted, 12 untracked. (c) Box-ahead live config lifted into the repo (commits `ceb27b5`+): fail2ban Tailscale rewrite, wazuh standalone NodePort + internet-egress NPs, openbao ingress IP-allowlist + bff-rotator helloworld policy, keycloak internal-CA + internal-TLS manifests, kyverno `02b`/`07` policies, in-cluster kaniko image-build manifests, 9 per-ns egress policies. (d) Repo bug surfaced + fixed (`4031445`): all 79 platform shell scripts were committed mode 644 — `install-all.sh` gates components on `[ -x ]` and would silently skip every one; restored to 755. (e) Box snapped to `origin/main`; `git pull` now syncs it and `git status` exposes new drift on sight. Cruft deleted: `08-teleport.sh.deferred`, the superseded `secforge-default-headers.yaml`, the orphaned `wazuh-agent/trivy_rules.xml`. `04-keycloak-cr.yaml` left diverged for #48. | medium | ✅ Closed 2026-05-21 | — |
| 51 | 2026-05-21 | platform | Wire the per-namespace egress baseline allows into the deploy. The 9 per-ns egress NetworkPolicies (for keycloak/spicedb/observability/wazuh/ingress-nginx — the namespaces whose Layer-A default-deny was deferred) were captured into the repo at `platform/manifests/_egress-baseline/per-ns/` during the #50 conversion, but nothing applies them — they were hand-applied to the live cluster by the operator. `apply-egress-baseline.sh` documents that per-ns allows belong at `platform/manifests/<ns>/0X-egress-<purpose>.yaml` applied via `apply-manifest.sh`; components apply explicit per-file lists (not globs), so proper wiring = relocate each file into its namespace dir with a non-colliding number + add an `apply-manifest.sh` line to the owning component (`00c-ingress-nginx.sh`, `03-keycloak.sh`, `04-spicedb.sh`, `07-wazuh.sh`, and whichever component owns the `observability` ns manifests — needs identifying). Completes Layer-A egress filtering for the 5 deferred namespaces. **✅ Closed 2026-05-22.** The 9 files were `git mv`'d into their namespace dirs (keycloak/09+10, spicedb/09+10, wazuh/04+05, observability/11+12, ingress-nginx/03) and an apply line wired into each owning component (`00c-ingress-nginx`, `03-keycloak`, `04-spicedb`, `07-wazuh`, `07b-tempo` — the observability namespace bootstrap). Commit `9f5b152`; a server-side dry-run confirmed all 9 relocated files apply `unchanged` against the live cluster (repo == live, no drift). | small-medium | ✅ Closed 2026-05-22 | — |
| 52 | 2026-05-22 | platform | `platform/lib/apply-manifest.sh` uses client-side `kubectl apply`, which is broken for the `keycloak` Keycloak CR: its `kubectl.kubernetes.io/last-applied-configuration` annotation is corrupted — it carries `metadata.resourceVersion`/`uid`/`generation` (a past apply fed it a raw `kubectl get -o yaml` dump). The client-side 3-way merge then computes a patch that deletes `resourceVersion`, and the apiserver rejects the apply (`metadata.resourceVersion: Invalid value: 0x0: must be specified for an update`). Surfaced during the #48 cutover; worked around there with `kubectl apply --server-side --force-conflicts`. Fix: either (a) strip the stale annotation (`kubectl annotate keycloak keycloak -n keycloak kubectl.kubernetes.io/last-applied-configuration-`) so the next client-side apply rewrites it cleanly — but note a 2-way merge then can't delete removed fields; or (b) move the keycloak component (or apply-manifest.sh generally) to server-side apply. Pick one, apply it, and check whether any other CR carries the same corrupt annotation. **✅ Closed 2026-05-22** — `apply-manifest.sh` gained an opt-in `--server-side` flag (server-side apply + `--force-conflicts`); `03-keycloak.sh` now applies the CR with it (commit `8281b81`). The corrupt annotation was stripped off the live CR; a scan of every Keycloak / CNPG Cluster / ScheduledBackup CR found no other corruption. Verified: `apply-manifest.sh --server-side 04-keycloak-cr.yaml` is a clean no-op re-apply (CR generation unchanged, no pod restart). Client-side apply stays the default for all other components — no blanket behaviour change. | small | ✅ Closed 2026-05-22 | — |
| 54 | 2026-05-22 | platform | Weekly guardrail-verification CronJob for the ADR-0013 outbound-secrets policies — spun off from #39 when those policies went Enforce. A once-a-week LIVE-mode self-test that proves the guardrail still bites: e.g. `kubectl apply --dry-run=server` of a deliberately secret-shaped-env Pod into the `control` namespace, asserting Kyverno's `no-secret-shaped-env-vars` policy rejects it — so a silent policy regression (deleted/disabled/scope-narrowed) is caught instead of going unnoticed. The old `infrastructure/secrets-guardrails/` CronJobs were broken local-edition scaffolding (non-existent `app` ns, undeployed collector, offline-mode-in-a-docker-less image) and were never ported; this is a fresh build for the platform edition. **✅ Closed 2026-05-22.** Built as `platform/manifests/kyverno/07-guardrail-selftest.yaml` — a weekly CronJob (Mondays 07:00 UTC) that server-dry-runs a secret-shaped-env probe Pod into the `control` namespace and asserts `no-secret-shaped-env-vars` rejects it (greps the denial for the policy name; the probe is otherwise PSS-restricted-compliant). SA + a narrow `create pods` + `get namespaces` ClusterRole; skips clean if the `control` ns is absent. Wired into `12b-kyverno-policies.sh`. Commits `b78f57a` + `7f5d767` (the latter fixed an RBAC gap — the guard's `get namespaces` was forbidden, so the first run silently always skipped). Deployed + verified: a one-off run PASSed — the probe was denied by `no-secret-shaped-env-vars`. | small | ✅ Closed 2026-05-22 | — |
| 53 | 2026-05-22 | platform | Post-soak cleanup of the #48 Keycloak signed-image cutover. Once the new image has soaked ~24h (≥2026-05-23) and a real end-to-end login is confirmed: (a) delete the `keycloak-password-blacklist` ConfigMap in the keycloak namespace — the custom image now bakes the HIBP list at `/opt/keycloak/data/password-blacklists/`, the CR no longer mounts it; (b) remove the kaniko `platform/manifests/keycloak/image-build/` manifests — superseded by the `keycloak-image-build` GHA pipeline. Both are retained until then as rollback aids — the ConfigMap plus the on-node `localhost/secforge/keycloak:26.3.3-optimized` image are the Layer-1 rollback target. | small | ⬜ Open | No |
| 55 | 2026-05-22 | platform | MinIO image upgrade — server image from `RELEASE.2024-12-18T13-15-44Z` (~17 months stale) to `RELEASE.2025-09-07T16-13-09Z@sha256:14cea493…`, mc client from `RELEASE.2024-11-21T17-21-54Z` to `RELEASE.2025-08-13T08-35-41Z@sha256:a7fe349e…`. Drives: (a) the in-cluster MinIO is the substrate for every CNPG cluster's barman backups, Velero kopia, Tempo / Loki / Wazuh archive storage — pushing Member Hub's CNPG cluster onto a 17-month-stale build is the wrong starting position (#55 should land before the Member Hub deploy in Phase B); (b) accumulated reliability/bug fixes across ~9 months of releases. Notable non-applicable: GHSA-jjjj-jwhf-8rgr (CVSS 8.1, "Privilege Escalation via Session Policy Bypass in Service Accounts and STS") is patched only in `RELEASE.2025-10-15T17-29-55Z` — that release was **never published as a public OSS image** (see #56), so this upgrade does NOT close that CVE; we don't use STS / restricted Service Accounts on this MinIO (root credentials only) so it doesn't bite, but the gap is the real story. Procedure: `docs/03-runbooks/minio-version-upgrade.md` — filesystem snapshot of `/var/lib/minio/data` before the rolling restart (Velero kopia repo lives **inside** MinIO at `backups/velero/` — circular dependency, the FS snapshot is the only real rollback target). Renovate customManager added (`renovate.json`) so future MinIO chart versions surface on the dashboard — without it the existing generic `CHART_VER` customManager fell through to a dead Google bucket registry URL, which is **why MinIO went 17 months stale unnoticed**. **Upgrade landed 2026-05-22T22:35 UTC, ~3 min downtime.** Pre-upgrade FS snapshot at `/var/lib/minio/data.pre-upgrade-2026-05-22T223136Z` (43G on `/var/lib/minio` xfs partition, 250G total / 86G used post-snapshot). Helm-side fixes folded in: `platform/values/minio.yaml` `persistence.size` 50Gi → 200Gi (matches live PVC; helm refused the upgrade because Kubernetes blocks PVC shrink); `sudo env KUBECONFIG=/etc/rancher/k3s/k3s.yaml bash 07a-minio.sh` is the right invocation under sudo (`install-helm.sh` doesn't inherit KUBECONFIG). Verified end-to-end: SSE-S3 auto-encryption still enabled on `backups` (MINIO_KMS_SECRET_KEY env continues to work in the target release), one-off Velero backup of `keycloak` ns → `Completed`, one-off CNPG plugin-method backup of `control-db` → `completed` in 11 s. **Soak ends 2026-05-29** — close this item then by running `sudo rm -rf /var/lib/minio/data.pre-upgrade-2026-05-22T223136Z` and flipping Status to ✅ Closed. | medium | ⬜ Open (soak) | No — landed clean ahead of Member Hub Phase B |
| 58 | 2026-05-23 | platform | Fold the live-only OpenBao app roles + audit-signing primitives into the bootstrap chain. Three k8s-auth roles (`control-vso`, `member-hub-vso`, `member-hub-audit-signer`), the `audit-signer` policy, and the `transit/keys/audit-signing` Ed25519 key were created via break-glass during the 2026-05-22 Member Hub Phase B deploy and existed only in live OpenBao — cluster rebuild would have lost them silently. **✅ Closed 2026-05-23** in commit `5190b3e`: new HCL `platform/manifests/openbao/policies/audit-signer.hcl` (auto-loaded by 05c's discovery loop), extended `05c-openbao-configure.sh` to create `transit/keys/audit-signing` (ed25519, `deletion_allowed=false` so accidental delete can't break the anchor chain), and new component `platform/components/05j-app-vso-roles.sh` with an `APP_ROLES` table that idempotently upserts the three roles via a single `bao write`. Verified idempotent: dry-run replay of both 05c + 05j against the live cluster left `audit-signing` at `latest_version=1` (no re-key) and the existing role configs byte-identical. Post-replay live auth tests confirmed all 3 SAs (`control/control-vso`, `member-hub/member-hub-vso`, `member-hub/member-hub-audit-signer`) still mint tokens with the expected policies + TTLs. Adding a new app from now on = appending one row to `APP_ROLES`. | small | ✅ Closed 2026-05-23 | — |
| 64 | 2026-05-23 | platform | DR drill Tier 2 — full greenfield rebuild on a fresh Hetzner instance (not local KIND). Validates the parts Tier 1 (#63) deliberately skipped: Cloudflare DNS + public TLS via cert-manager + Let's Encrypt; Tailscale operator mesh; Velero restore-from-MinIO (the realistic DR path); GHCR pull with cosign image-signature verification under Kyverno; SPIFFE/SPIRE workload-identity flow + spiffe-helper sidecars; full observability stack (Wazuh / Loki / Tempo / Prometheus); actual Member Hub backend pod startup with all the above wired. Cost: ~€10-20 for a few hours on a Hetzner CAX instance + 2-3h elapsed. Worth doing once per quarter (or after any major bootstrap-chain change). | medium-large | ⬜ Open | No (cluster operates; only matters for DR confidence) |
| 63 | 2026-05-23 | platform | DR drill Tier 1 — local KIND-based dry-run of the realm-import + secret-injection codification on a truly fresh Keycloak. Validates the codification correctness of #57 / #59 / #60 / #61 without burning a Hetzner instance. **✅ Closed 2026-05-23** — drill ran on Docker Desktop KIND + cnpg + vault-secrets-operator + keycloak-operator + dev-mode OpenBao. **Caught 2 real bugs:** (a) `passwordBlacklist(Pwdb_top-100000.txt)` clause in passwordPolicy is a HARD prereq on the custom Keycloak image baking that file — stock upstream fails to start in import mode. Already documented in 03a but not in platform-realm.yaml; folded into the new DR runbook. (b) **Real bug — fixed inline**: my codified client descriptions across all 9 clients were 250-350+ chars; Keycloak's `CLIENT.DESCRIPTION` column is `varchar(255)`; realm-import failed with `value too long for type character varying(255)`. Commit `59e5bbb` truncated each description to a single-line purpose statement; detailed context preserved in YAML comments. **After both fixes**, realm-import completed in 12s; verified all 9 clients + 3 svc-acct users + custom flow + WebAuthn fields landed correctly; operator-auto-generated 32-char secrets for all 8 confidential clients; drill-adapted 05l publish flowed every secret to its expected OpenBao kv / k8s Secret destination. Runbook with findings + greenfield-rebuild ordering: `docs/03-runbooks/dr-drill-tier1-findings.md`. Out-of-scope for Tier 1 (deferred to #64): Cloudflare DNS, Tailscale, Velero restore, GHCR cosign verification, SPIFFE, observability, actual Member Hub pod startup. | medium | ✅ Closed 2026-05-23 | — |
| 62 | 2026-05-23 | platform | Make the audit-anchor CronJob actually run. The original `mirror.gcr.io/alpine:3.21` + `apk add` approach failed at script start with `Unable to lock database: Permission denied` because the pod runs as non-root. **✅ Closed 2026-05-23** via design (a) — TS worker in the Member Hub image, matching audit-verifier's working pattern. Cross-repo: Member Hub commits `cb15707` + `c026d01` added `workers/audit-anchor/anchor.ts` (393 LOC); Security Forge commits `6b88db0` + `df2f083` swapped the CronJob to `ghcr.io/jaupole/member-hub@sha256:c2afcfdd…`, deleted the inline anchor.sh ConfigMap, added wait-for-db initContainer + ghcr-pull-secret + readOnlyRootFilesystem=true. **Side discovery + fixed inline**: missing `allow-member-hub-to-openbao` NetworkPolicy on openbao ns — Cilium RST'd cross-ns drops as ECONNREFUSED. The Member Hub backend's failure was silent up to now because lib/openbao is lazy-initialized + PII encrypt path hasn't been triggered yet; this fix unblocks both audit-anchor + future backend PII flow (Security Forge commit `bae6182`). End-to-end verified: first run published `member-hub/2026-05-23.json` to secforge-audit-anchors (covers_through_id=9, previous=0, key_version=1); re-run correctly detected `no_new_rows` and skipped commit (idempotent). Git operations now go through GitHub Contents API (`PUT /repos/.../contents/{path}`) — no working directory, no git binary, no isomorphic-git dependency. Uses plain Node fetch + the existing pg pool pattern. | medium | ✅ Closed 2026-05-23 | — |
| 61 | 2026-05-23 | member-hub | Drop the ADR-0013 escape hatch on the audit-anchor CronJob (PGPASSWORD + GH_TOKEN). **✅ Closed 2026-05-23** in commits `c9b4d73` + `fd024de`. The two `secforge.dev/legacy-secret-env*` annotations (expiring 2026-08-20) were the bypass for Kyverno's outbound-secrets policy, since libpq's PGPASSWORD and git/curl's GH_TOKEN are vendor-fixed env names matching the credential-name regex. Refactor: mount the existing Secrets (`member-hub-db-app` key `password` and `audit-anchors-push-token` key `token`) as files at `/run/secrets/db/password` and `/run/secrets/gh/token` (defaultMode 0400, tmpfs-backed). `anchor.sh` reads both into shell-local vars at script start (`export PGPASSWORD=$(cat …)` for libpq; `GH_TOKEN=$(cat …)` used inline in clone URL). Pod-spec env now only carries non-credential vars (`OPENBAO_ADDR`, `PGHOST`, `PGDATABASE`, `PGUSER`). Kyverno admission passes, escape-hatch annotations removed. **Side-effect discovered + fixed**: pre-existing ConfigMap rename gap — the openbao-ca volume referenced CM `openbao-ca` but the actual CM (per `08a-openbao-ca-configmap.yaml`) is `openbao-internal-ca-cert`. CronJob would have failed silently on first scheduled run regardless. **Separately discovered + filed as #62**: the audit-anchor cronjob has NEVER actually run successfully — `apk add` fails because the pod runs as non-root. The ADR-0013 refactor goal (drop the env-var policy violation) IS achieved independent of #62. | small | ✅ Closed 2026-05-23 | — |
| 60 | 2026-05-23 | platform | Greenfield secret-injection refactor for Keycloak clients + custom browser-webauthn-required flow codification + 07i's orphaned OpenBao OIDC-bundle staging step. **✅ Closed 2026-05-23** in commit `17f4b92`: chose design (b) post-import publish, implemented as `platform/components/05l-keycloak-secret-publish.sh`. Two-phase script — Phase 1 seeds the static OIDC-bundle metadata (client_id, issuer, redirect_uri, cookie_password random:32) at `secret/wazuh/oidc` (closes 07i's orphaned side-effect); Phase 2 reconciles `CLIENT.secret` for each codified client to its destination (OpenBao kv for VSO consumers — VSO refreshes within 60s; direct k8s Secret only for the openbao client where VSO would be circular). Idempotent: matches no-op, missing publishes from Keycloak DB, drift warns but does NOT auto-resolve (could break running apps mid-rotation; use `project_keycloak_client_secret_rotation_pattern` to resolve). Verified against live cluster: first run published `member-hub-system` (had been orphaned since #57 — its `system_client_secret` was in Keycloak DB but never made it to OpenBao kv), other 7 no-op'd. Second run: 8/8 match, 0 drift. Also folded 03a stages [04]+[05] into `platform-realm.yaml` `authenticationFlows:` + `browserFlow:`: 5 nested flows declared from scratch (auth-cookie + Organization subflow + forms subflow with Conditional 2FA containing webauthn-authenticator + auth-recovery-authn-code-form alternatives). 03a script remains the day-2 idempotent replay for live drift recovery; both must stay aligned. Remaining (small, future ticket): a true greenfield E2E DR drill to validate the whole bootstrap chain end-to-end against an empty cluster — currently we've only proven idempotence against the live cluster, not first-time creation. | medium-large | ✅ Closed 2026-05-23 | — |
| 59 | 2026-05-23 | platform | Codify the remaining live-only platform-realm clients + neutralize the broken kcadm auth path in per-app bootstrap scripts. Scope: `control`, `control-admin`, `control-portal` (uncodified) plus the clients owned by 05h/07d/07i (openbao, grafana, wazuh-dashboard) whose creating scripts read the deleted `temp-admin` user via `keycloak-initial-admin` Secret. **✅ Closed 2026-05-23** in commit `0ce2ace`: added all 6 missing clients to `platform/manifests/keycloak/realms/platform-realm.yaml` `spec.realm.clients[]` (now 9 total alongside #57's member-hub trio), added `service-account-control-admin` USER_ENTITY with realm-management roles. Also folded 03a hardening stages [02]/[03]/[06]/[07]/[08] into the realm-import: webAuthnPolicy block (RpId=`${DOMAIN}`, UserVerification=required, ES256+RS256), requiredActions (CONFIGURE_TOTP flipped to defaultAction=false, webauthn-register added with defaultAction=true priority=70). Marked 05h/07d/07i DEPRECATED with informative banners that exit immediately + pointer to platform-realm.yaml; historical implementation preserved below the exit for reference. 07i is PARTIALLY DEPRECATED — the OpenBao OIDC-bundle staging side-effect still has no replacement (rolled into #60). 03a's header rewritten to clarify the new split: most fields are declarative in platform-realm.yaml; only the custom browser-webauthn-required flow (stages [04]+[05]) remains 03a-exclusive. 03a is still the source of truth for day-2 drift recovery; both must stay aligned. YAML validated: parses clean (9 clients, 3 svc-acct users, 7 requiredActions, WebAuthn fields). kubectl client-side dry-run accepts the CR. NOT applied to live — realm-import is one-shot by design. Spun off #60 for the secret-injection refactor + custom-flow codification + 07i's OpenBao staging side-effect. | medium | ✅ Closed 2026-05-23 | — |
| 67 | 2026-05-27 | portal | SPA idle-timeout detection in Ecosystem Portal (both `portal.secforge.dev` and `admin.secforge.dev` shells). Today's session-expiry handling (commit landing in this session: `silentRenewError` + visibilitychange + 401 UNAUTHED_EVENT listeners in `AuthProvider`) closes the three reactive gaps — operator now bounces to sign-in as soon as the dead session is *observed* (next focus, next silent-renew, or next 401). What's still missing is **proactive** idle detection: track keyboard/mouse activity on the SPA, and after N minutes of quiescence, call `signoutDirect()` even if the SSO session at Keycloak would still happily renew. Without this, the implicit lifetime of an active tab is the realm's 24h remember-me cap (because `automaticSilentRenew: true` bumps the SSO session's lastAccessTime on every iframe round-trip), so an operator who walks away from a logged-in browser for 8 hours stays logged in. **Recommended shape:** small zustand store fed by a `useIdleTimer` hook on `AppShell` (mousemove/keydown/click/touchstart listeners, debounced to ~5s); on N minutes (config: 30 min default, configurable per-shell) call `signoutDirect()`. Show a 30-second warning toast before tripping. Same logic for Member Hub's `AppShell`. **Trigger:** before paying tenants onboard, or before the first sensitive surface (admin role grants, billing config changes) is regularly used over coffee shops / hot-desks. Cross-ref: `project_session_lifetime_prelaunch` memo. | small-medium (~half day, two repos) | ⬜ Open | No — current dead-session detection covers the observed-symptom case; idle-timeout is the separate, proactive layer |
| 57 | 2026-05-23 | platform | Codify the three Member Hub Keycloak clients (`member-hub` user-login OIDC, `member-hub-admin` admin-API service-account, `member-hub-system` Control-API service-account). All three existed only in live Postgres after the 2026-05-22 Phase B deploy. **✅ Closed 2026-05-23** in commit `4422b1e`: added full client definitions to `platform/manifests/keycloak/realms/platform-realm.yaml`'s `clients:` list (was `[]`) and the two backing service-account `USER_ENTITY` rows to `users:` list (was `[]`). Faithful to live shape: `member-hub` ships standard-flow + 2 audience mappers (member-hub-audience + control-audience-for-getme); `member-hub-admin` has the 5 realm-management roles attached via `service-account-member-hub-admin` user; `member-hub-system` has aud-self mapper and just `default-roles-platform`. Verified: YAML parses clean (3 clients, 2 svc-account users), `kubectl apply --dry-run=client` accepts the CR shape. NOT applied to live (realm-import is one-shot — operator skips re-applies when realm exists, by design). Codification value is purely greenfield rebuild. After greenfield install, run the rotation pattern in `project_keycloak_client_secret_rotation_pattern` to sync OpenBao kv with operator-auto-generated client secrets (or vice versa if Velero restored OpenBao first). Side findings closed inline 2026-05-23: vestigial `/*` redirect URIs + web origins on `-admin` / `-system` cleaned via DB transaction (defense-in-depth smell — never consulted with `standard_flow=false`). Remaining clients (control × 3) + auth-path repair filed as #59. | small | ✅ Closed 2026-05-23 | — |
| 66 | 2026-05-27 | tenants-realm | First-sign-in "password OR passkey" choice page for tenant signups. Today's flow (Control commit `8c83c25` + Portal `44be177`) FORCES password change on first sign-in via Keycloak's `UPDATE_PASSWORD` required action — the user MUST replace the temp password to reach dashboard, THEN they can optionally enrol a passkey from Profile and delete the password. The operator originally asked for a real choice: "set a permanent password OR set up a passkey, your call, before dashboard." Keycloak's required-action chain is mandatory-and-sequential, so an in-IdP either/or screen needs either a custom Account-Console theme override or a Portal-side onboarding gate. **Recommended path:** Portal-side onboarding gate. Server marks new signup with `onboarding_pending=true` user attribute. Portal `/me` exposes `onboardingPending: true`. AppShell intercepts every route and redirects to a new `/onboarding` page when the flag is set. The page shows two big buttons: "Set a permanent password" (triggers `kc_action=UPDATE_PASSWORD`) and "Set up a passkey" (triggers `kc_action=webauthn-register-passwordless`). After return, Portal calls a `POST /api/v1/me/complete-onboarding` endpoint that verifies the user actually has either (a) a recent non-temp password or (b) a `webauthn-passwordless` credential, deletes the temp password if path (b) was chosen, and clears `onboarding_pending`. Re-uses the existing `signup_pending_at` 24h cleanup (the attribute names differ; both can coexist or be unified). **Effort:** medium — 1 new Portal page + AppShell route gate + /me schema bump + new Control endpoint + signup.ts switches `temporary: true` -> `temporary: false` + adds the new attribute. **Trigger:** before tenants who explicitly want password-less from day one start onboarding (e.g. internal SecForge staff invited as tenants). Until then the force-password path is acceptable — they CAN switch to passkey-only after first sign-in, just not during. | medium | ⬜ Open | No — current force-password path is functional |
| 65 | 2026-05-27 | tenants-realm | Keycloak Account Console exposes no UI path to **add** a password when the user has none. On the `secforge-tenants` realm with the flexible `browser-flexible` flow (password OR passkey as first-factor alternatives), a tenant who registered passkey-first and later wants to add a password — or who removed their password and changed their mind — has no in-product control to do it. The "Signing In" page shows existing credentials with a "Remove" button, but no "Set up password" / "Add password" affordance when the password credential is absent; only the unauthenticated "Forgot password" reset-email flow works. Live tested 2026-05-27 by the operator on `portal.secforge.dev` after removing the password from their own account — only "Use lost password" was offered as recovery. **Why it matters at scale:** under our model "users can sign in with username+password OR username+passkey, optional 2FA", tenants are expected to manage both credential types via the Account Console. Forcing a password reset email round-trip every time a tenant wants to add a password back is poor UX and adds an unnecessary email-deliverability dependency. **Fix candidates:** (a) custom Account Console (Keycloak's Account Console is a React app — fork + add "Set up password" button when no password credential exists, calling the same `UPDATE_PASSWORD` required-action endpoint); (b) register `UPDATE_PASSWORD` as a triggerable action via the `kc_action` query param from a Portal-side "Manage your password" link (workaround — keeps stock Account Console but routes users through Portal first); (c) wait for upstream Keycloak to add this — historically slow on Account Console feature requests. **Trigger:** before tenants self-serve at scale (paid customers onboarded, expectation of full self-service). Quietly painful but not blocking — Forgot-password email is functional. **Cross-ref:** `project_keycloak_tenants_passkey_required` memo + commit `7e6c5e4` (recovery codes removed) — this is the next-finer-grained UX issue surfaced by removing recovery codes from the tenants realm. | medium | ⬜ Open | No — Forgot-password email is a working (if clunky) workaround |
| 56 | 2026-05-22 | platform | Strategic: MinIO has effectively stopped publishing public open-source release images after `RELEASE.2025-09-07T16-13-09Z` (Sep 7 2025). The Oct 2025 security advisory `GHSA-jjjj-jwhf-8rgr` explicitly pins patches at `RELEASE.2025-10-15T17-29-55Z`, but that image was never pushed to quay.io or Docker Hub; only `.hotfix.<commit>` backports against earlier release tags are visible on the public registries (e.g. `RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772` from April 2026). Consistent with MinIO's broader 2025 commercialization push (Console deprecated, LDAP/OIDC moved). Our substrate dependency on MinIO is significant (Tempo, Loki, Wazuh archives, Velero kopia, every CNPG cluster's barman backups) so a decision is needed: (a) ride the `.hotfix` train and pin those tags going forward; (b) migrate to a maintained alternative — SeaweedFS, Garage, MinIO Operator under their commercial terms, Hetzner Object Storage, Backblaze B2; (c) bifurcate — keep MinIO for ephemeral observability data, move backups (Velero, CNPG) to an out-of-band store, which also breaks the Velero-kopia-inside-MinIO circular dependency that #55's runbook has to snapshot around. ADR-worthy. Time horizon: ~6 months before the next forced upgrade decision. Sub-issues spinning off later: (i) `renovate.json`'s generic `CHART_VER` customManager points at the dead `kubernetes-charts.storage.googleapis.com` registry — surveying which platform charts (cert-manager, keycloak-operator, spicedb, prometheus, loki, velero, kyverno, trivy, ingress-nginx, cnpg, tailscale-operator, …) are silently stale; (ii) move Velero's BackupStorageLocation `s3Url` off the in-cluster MinIO once an alternative is chosen. | large (research → ADR → migration) | ⬜ Open | No (immediate); increasingly yes over the next ~6 months |
| 68 | 2026-05-28 | platform | Migrate `control-qbo-secrets` from Pattern B (OpenBao KV at `secret/data/apps/control/qbo` → VSO → K8s Secret → `envFrom` → `process.env.QBO_CLIENT_SECRET`) to Pattern A (Transit field-level encryption in a control_db row, decrypted on-demand via `transit.Decrypt()`). Pattern A is what Stripe uses today (`platform_billing_config.stripe_secret_key_enc` + `organization_stripe_config.stripe_secret_key_enc`, per-org `pii-org-<id>` Transit keys, cipher-orphan UI flow, audit-trail per decrypt) and is architecturally cleaner: plaintext lives in process memory for milliseconds per request instead of hours in pod env vars; per-org rotation works without redeploys; OpenBao audit logs the read. **Rationale (2026-05-28 session):** today's exposed sandbox QBO secret in the K8s Secret env var is harmless, but when production Intuit keys land they should not sit in pod memory for the pod's lifetime. **Work:** (a) reuse the existing `ecosystem_quickbooks_app.client_secret_enc` column (already present + Transit-encrypted, currently unwired) OR add `qbo_client_id_enc` / `qbo_client_secret_enc` columns to a platform-singleton table; (b) replace `process.env.QBO_CLIENT_*` reads in control with `transit.Decrypt()` at the OAuth flow entry points; (c) delete the `control-qbo-secrets` VSS, the `envFrom: control-qbo-secrets` block on the control Deployment, and the OpenBao KV path; (d) drop the `QBO_REDIRECT_URI` env var stays (not a secret). **Trigger:** before the first production QBO connection (sandbox keys are fine where they are). **Cross-ref:** `project_per_org_integrations_2026_05_20` memo + `project_stripe_webhook_validated_2026_05_26` memo (Stripe is the reference pattern). | small (~1 hr) | ⬜ Open | No — sandbox keys are acceptable in env-var pattern; trigger is production-key landing |
| 71 | 2026-05-28 | platform | Codify the OpenBao seal transit-token rotation as `platform/components/05x-rotate-transit-token.sh` so the procedure is idempotent + safer than the manual reference. Today's 2026-05-28 rotation was executed manually (documented in `docs/03-runbooks/openbao-recovery.md` § "Rotate the Transit unseal token"); the script should replicate that exact 9-step sequence (validate seal root → mint new periodic token → verify token capabilities → backup current seal-block Secret → patch in new token via sed → identify leader → restart followers-first → revoke old token → sanity-check VSS still synced) but with safety rails: refuse to start if seal-OpenBao is sealed; refuse to start if any main OpenBao pod isn't healthy; per-pod restart-count delta watchdog so pre-existing crashloops don't false-positive; rollback on any step failure by restoring the backed-up Secret + restarting affected pods. **Cross-ref:** `docs/03-runbooks/openbao-recovery.md` § "Codified-script gap" + ADR-0009 § Known local gaps #4. **Effort:** small (~1-2 hr — script is mostly already-tested bash from the manual procedure, just needs error-handling wrappers + the watchdog pattern from the retired `infrastructure/openbao/apply-main.sh` rolled in). **Trigger:** next planned rotation or first time a fresh operator needs to do it. Until then the manual procedure in the runbook is the authoritative reference. | small | ⬜ Open | No — manual procedure is documented and proven |
| 70 | 2026-05-28 | platform | Originally: Deploy MinIO KES with OpenBao-backed keystore + 9-step migration. **✅ Closed 2026-05-28 (KES path superseded — drain-and-rotate executed instead).** Research during the deploy session surfaced that `minio/kes` was archived upstream 2025-06-19, making KES a frozen-codebase commitment. ADR-0031 amended in place to record both the original KES design (retained as fallback if MinIO #56 lands on a continuing-MinIO outcome) and the drain-and-rotate execution. Drain-and-rotate sequence (executed 2026-05-28 23:00–23:15 UTC): drained `member-hub-documents/` (2 objects / 3.6 KiB) to host disk with sha256 verification; wiped `backups/cnpg/`, `backups/velero/`, `backups/sse-verify/`, `member-hub-documents/` (~1.2 GiB across 8836 objects); generated new 32-byte master key `secforge-minio-key-2026-05-28-…`; wrote to OpenBao at `secret/data/platform/minio/sse-master-key` (kv-v2 version 2); VSO re-rendered `minio-kms-creds`; restarted MinIO Deployment; round-trip-verified MinIO writes new objects under new key; re-uploaded `member-hub-documents/` from host stage with sha256 MATCH on both objects; triggered fresh CNPG base backups for all 4 clusters (control, keycloak, member-hub, spicedb) via plugin-method Backup CRs — all completed in <30s; triggered immediate Velero backup. Result: 60 MiB → 356 MiB / 149 objects in `backups/` under the new key within minutes. The OLD master key value historically present in state.db kine MVCC bytes is now functionally dead — no encrypted MinIO data on the box references it. **Cross-ref:** project_encryption_architecture memo, ADR-0031 (drain-and-rotate amendment), commits TBD. | half-day | ✅ Closed 2026-05-28 (drain-and-rotate) | — |
| 72 | 2026-05-28 | platform | **OpenBao UI renders authenticated pages without verifying the session token first.** Surfaced 2026-05-28 during the ecosystem-graphics MinIO svc-account provisioning step: operator opened `https://bao.secforge.dev`, was taken straight into the authenticated UI shell (Secrets / Auth / Policies tabs visible, KV mounts listed in the left nav), and only discovered the session was stale when the first write action (`kv put secret/minio/ecosystem-graphics …`) returned `permission denied`. Expected behavior: a stale/missing OpenBao token should bounce to the OIDC sign-in landing immediately on page load — same posture the rest of the platform's UIs take (Portal/Control/Member Hub all hard-bounce on dead session via the `UNAUTHED_EVENT` listener pattern). **Why it matters:** (a) gives the operator a false sense of "I'm signed in" before any privileged action is attempted, leading to wasted token-mint + page-navigate cycles when the session has already expired; (b) the same UI shell-vs-action mismatch could mask a credential-replay attack staging recon — an attacker with a stale cookie could enumerate visible mount names + policy names before any action is denied; (c) inconsistent with the rest of the platform's "render only after auth verified" pattern, which is itself a CLAUDE.md "authentication ≠ authorization" implication. **Likely cause:** the upstream Web UI (OpenBao 2.5.3, served by the openbao binary) keeps a cached server-state snapshot in browser memory (Ember.js app), and only revalidates the token on explicit API calls rather than on shell-mount. Stale tokens still let the shell render because the shell itself never hit `sys/internal/ui/mounts` or similar with the dead token until the user acted. **Fix candidates:** (a) wrap the OpenBao UI behind ingress-nginx `auth-url` + `auth-signin` annotations pointing at oauth2-proxy (operator pattern — IdP-enforced gate **before** OpenBao's own UI is reachable), trading a hop for hard-rejection on stale session; (b) write a small Ember add-on / patch on the vendored OpenBao image that gates the root route on a live `sys/capabilities-self` ping (upstream patch — preferred long-term, but burns operator hours); (c) push upstream OpenBao to fix it (slow, but correct). Recommendation: (a) for the next maintenance window — small ingress-nginx annotation change + oauth2-proxy already present for Wazuh / Grafana, so the same gate pattern fits. **Trigger:** before the OpenBao UI is exposed to anyone other than the sole operator (currently jaupole only via OIDC), or any time the operator is annoyed by it again. | small-medium (oauth2-proxy gate ~2 hr; upstream patch ~half day) | ⬜ Open | No — UI auth still ultimately enforces on every action; this is a UX + recon-surface hardening item, not a privilege-escalation bug |
| 69 | 2026-05-28 | platform | Add a `kubernetes/role/admin-break-glass` to **openbao-seal** mirroring the one already on main openbao (memory `project_openbao_admin_recovery_2026_05_19`). Surfaced 2026-05-28 during seal-token rotation: openbao-seal's only admin recovery path is the initial root token (which best-practice says to revoke after bootstrap) — `bao operator generate-root` returns `405 unsupported operation` on OpenBao 2.5.3 (already-known per `docs/03-runbooks/openbao-recovery.md`), so if the operator-stored root is lost or revoked, the cluster has no admin path back without Velero restore from a backup that has a valid root, or a destructive re-bootstrap. **Today's rotation worked only because the operator's second offline-stored copy of the root happened to still be valid.** Adding a k8s break-glass to openbao-seal removes that single point of operational fragility. **Implementation sketch:** (a) enable `auth/kubernetes` on openbao-seal (requires admin — use the offline root one more time during the next planned maintenance window); (b) write `auth/kubernetes/role/admin-break-glass` bound to `openbao/openbao-seal` SA (or a dedicated SA) with `admin` policy + 1h TTL, mirroring 05c-openbao-configure.sh's pattern for main openbao; (c) fold the configuration into 05a-openbao-bootstrap-seal.sh so the runbook claim becomes load-bearing on rebuild; (d) extend `docs/03-runbooks/openbao-recovery.md` § "Kubernetes auth break-glass" to mention both main openbao AND openbao-seal. **Trigger:** any time before the next seal-token rotation cycle (recommend within 30 days). **Risk if unfilled:** if the offline root gets lost/revoked and there's no recent Velero backup with a valid root, openbao-seal becomes unrecoverable without re-bootstrap (which would invalidate main openbao's master key wrap → destroys all Transit-encrypted PII unless rebuilt from CNPG/barman backups). | small (~1 hr, requires the offline root token to land changes) | ⬜ Open | Yes (medium) — single point of operational failure on a load-bearing component |
| 73 | 2026-06-02 | platform | Keycloak CR `additionalOptions`: rename deprecated `tracing-service-name` / `tracing-resource-attributes` → `telemetry-*` (Keycloak 26.6 deprecation, surfaced in the keycloak-0 boot log after the 26.3.3→26.6.2 upgrade). Still functional in 26.6.x; slated for removal in a future major. These are RUNTIME options in `platform/manifests/keycloak/04-keycloak-cr.yaml` (additionalOptions ~lines 104-106), so a rename = operator reconcile → keycloak-0 recreate (single-replica StatefulSet ⇒ brief IdP blip) AND crash-loop risk if the new names are wrong (an unknown runtime option fails `kc.sh start` under `--optimized`). Not worth a public-IdP restart just to silence a warning. DO IT at the next planned keycloak restart/upgrade: (a) verify the EXACT `telemetry-*` names against KC 26.6 docs / `Profile.java` first; (b) edit the CR + `kubectl apply` together to avoid repo↔live drift; (c) watch the boot for option-parse errors. Cross-ref: 26.6.2 upgrade PR #65; NIST password-policy + registration cleanup 2026-06-02. | small (~30 min at a maint window) | ⬜ Open | No — cosmetic deprecation warning; options still work in 26.6.x |
| 75 | 2026-06-04 | platform | **OpenBao 2.5.3 → 2.5.4 — watched upgrade.** Held out of Renovate group PR #83 during the 2026-06-04 CVE/Tier-C pass: a secrets-engine version bump should be deliberate, not slipped in with image-digest deps. PR #83 was split — its Go deps landed via #42/#43, its velero-plugin-for-aws 1.14.1 + alpine/k8s 1.36.1 cherry-picked to main (`4049797`), leaving #83 as the OpenBao-only tracking PR (kept OPEN, `do not auto-merge`). The bump touches `platform/values/openbao.yaml` + `platform/values/openbao-seal.yaml` (`tag` + the `server.ha`/seal image refs, 2.5.3 → 2.5.4). **Procedure (watched):** (a) read the 2.5.4 release notes/CHANGELOG for seal/Raft/Transit-affecting changes; (b) Velero/kopia snapshot of the openbao + openbao-seal PVs first (recovery history — see ADR-0009 + `docs/03-runbooks/openbao-recovery.md`); (c) confirm the seal-OpenBao is healthy + unsealed before touching main; (d) `helm upgrade` main OpenBao followers-first, watch each pod re-join the Raft quorum + auto-unseal via Transit before the next; (e) verify `bao status` unsealed on every pod, VSO still syncing, and a round-trip `kv get` works; rollback = restore the snapshot + pin back to 2.5.3. **Trigger:** after the rest of the 2026-06-04 upgrade backlog is caught up (operator's stated sequencing); do it in a window where the IdP/secrets blip is acceptable (auto-unseal means no manual unseal keys, so a stuck pod is a real outage risk). **Cross-ref:** Renovate PR #83, `docs/03-runbooks/openbao-recovery.md`, ADR-0009. **✅ DONE 2026-06-04** — executed end-to-end. Seal is 3-of-5 Shamir/file/no-auto-unseal (its readiness probe uses `sealedcode=204`, so "Ready" ≠ unsealed). Procedure: fresh Velero backup `openbao-pre-2540-20260604-2118` (incl. seal keyring PV) → **rehearsed** the seal restart-and-unseal on 2.5.3 first to validate the operator's keys (passed) → bumped both `platform/values/openbao*.yaml` to 2.5.4 (commit `55525b2`) → `install-helm.sh` upgraded both releases (OnDelete StatefulSets = spec-only, no auto-restart) → pre-pulled the image → rolled main one pod at a time, **followers first, leader (was openbao-2) last** (each auto-unsealed via Transit + rejoined Raft; leadership moved cleanly to openbao-0) → rolled the seal LAST → operator unsealed with 3/5 keys. Verified: all 4 pods on 2.5.4/Ready, Raft healthy, seal unsealed, VSO VaultConnection Healthy + all VaultAuths True + secrets Healthy/Ready (re-reconciled against 2.5.4 at 20:01). | small-medium (~1-2 hr watched) | ✅ Closed 2026-06-04 | — |
| 77 | 2026-06-04 | platform | **VSO (1.4.0) wedges VaultStaticSecrets after an OpenBao restart/restore — remedy = recreate the CR.** Surfaced 2026-06-04 verifying the OpenBao 2.5.4 upgrade. **9** VaultStaticSecrets (cert-manager/cloudflare-api-token, observability/{loki,tempo}-minio + grafana-oidc, spicedb/{cnpg-minio,config}, velero/minio, wazuh/oidc, keycloak/cnpg-minio) had been stuck `SecretSynced=False` **since exactly 2026-05-19** (the day of the OpenBao admin-recovery restore — `docs/03-runbooks/openbao-recovery.md`). Symptom: the condition is FROZEN (identical timestamp + byte-identical error to the nanosecond, pointing at a long-dead pod IP `10.42.0.121` on `auth/kubernetes/login`); VSO had completely stopped reconciling these objects. The VaultConnection is Healthy, all referenced VaultAuths are `VALID=true`, and the destination Secrets keep serving their last-good (pre-05-19) data — so consumers worked, but VSO could not refresh them (rotated creds would NOT propagate). **What does NOT fix it:** a VSO controller restart (which is ALSO what the operator's `vso-watchdog` CronJob does every 5 min — proven ineffective for this state) and a force-sync annotation — neither triggers even a fresh attempt. **What DOES fix it (applied 2026-06-04, all 9 cleared + verified, no consumer disruption):** delete + re-apply the VaultStaticSecret CR (`kubectl delete vaultstaticsecret <n> -n <ns>` then `apply-manifest.sh platform/manifests/<ns>/<binding>.yaml`). The destination Secret is owner-ref'd by the VSS so it's GC'd on delete and recreated by VSO within ~5s — running consumer pods keep their loaded creds, so the gap is harmless; do loki/tempo (continuously-read minio creds) last. **Upgrade is NOT a fix:** VSO **1.4.0 (May 5 2026) IS the latest release** — no newer version to upgrade to; this is current-release behavior (the unreachable-Vault reconcile stall is unfixed upstream). **Recommended durable follow-ups:** (a) pin the VSO chart version in `05d-vso-install.sh` (currently UNPINNED — `install-helm.sh` has no `--version`) + add a renovate.json customManager so the eventual upstream fix surfaces (closes a MinIO-#55-style tracking blind spot); (b) optionally rework `vso-watchdog` to recreate stuck VSS instead of the useless controller bounce (note: auto-deleting prod secrets on a heuristic is itself risky). **Update 2026-06-04: follow-up (a) DONE** — VSO chart pinned `VSO_CHART_VER:-1.4.0` in `05d-vso-install.sh` + renovate.json customManager added (commit `53f0d48`; live release was already 1.4.0 so config-only). New VSO releases now surface as a dashboard PR → when the upstream stall fix ships, we'll see it. Follow-up (b) (smarter watchdog) still optional/open. **Cross-ref:** `vso-watchdog` CronJob in `vault-secrets-operator` ns, `platform/manifests/<ns>/*-vso-binding.yaml`, commit `53f0d48`. | small (remedy proven; pin/track done) | 🟨 Mitigated + pinned/tracked 2026-06-04 (smarter-watchdog optional) | No — secrets serve valid data; only blocks credential ROTATION propagation until recreated |
| 76 | 2026-06-04 | platform | **CNPG Postgres image CVEs — accept + monitor (Tier B decision).** Investigated 2026-06-04 for the "bump CNPG to clear CVEs" task and found the bump is a **no-op**: all 5 single-instance DBs (control/keycloak/member-hub/proposal-forge/spicedb) already run the **latest published `ghcr.io/cloudnative-pg/postgresql:17.6-bookworm` digest** (`f785551…` — identical to the current tag, the repo pin, and every running pod), and `17.7–17.11-bookworm` all 404 (17.6 is the latest minor). The image's 23 CRITICAL split into (a) **reachable-but-image-stale** — `libssl3`/`openssl` 3.0.17→3.0.19 (CVE-2026-31789), `libgnutls30` u5→u7, `libsqlite3` u1→u2, Go `stdlib` v1.18.2 — which **have** Debian fixes that CNPG simply hasn't rebuilt into the image yet (nothing pullable fixes them today), and (b) **dormant/no-fix** — perl + python (verified NONE of the 5 DBs use plperl/plpython, only plpgsql, so those binaries are never executed) + zlib. **Decision (operator, 2026-06-04): accept + monitor** — did NOT roll the DBs (zero CVE benefit, pure single-instance downtime risk). The `minimal-bookworm` variant (exists, functionally safe since no plperl/plpython) would strip perl/python (~half the criticals) but does NOT fix the reachable openssl/gnutls and is a variant-switch on 5 prod DBs — deferred as optional DiD. **Monitor mechanism (set up 2026-06-04):** added a `renovate.json` customManager tracking `spec.imageName` in `platform/manifests/*/02-cnpg-cluster.yaml` (the built-in kubernetes manager only reads `image:` in pod specs, so this CRD field — the ONLY postgres ref for keycloak+spicedb — was a blind spot). It surfaces a **reviewable** PR (not auto-merged) when CNPG republishes the image; `docs/03-runbooks/base-image-cve-cadence.md` gained the CNPG one-at-a-time roll steps. **Trigger to act:** the customManager's digest-bump PR appears (= upstream rebuilt with the libssl3/gnutls fixes) → roll all 5 clusters one at a time per the runbook. **Optional follow-up:** evaluate the `minimal-bookworm` switch if a cleaner Trivy scan / smaller surface is wanted. **Cross-ref:** `security-audit-2026-06/FIX-LOG.md` § Tier B; renovate.json CNPG customManager; base-image-cve-cadence.md. | small (monitor in place; the roll itself is ~30 min one-at-a-time when the PR lands) | 👁️ Watching brief | No — DBs are cluster-internal (NetworkPolicy-restricted, no public ingress); reachable CVEs are in TLS libs awaiting upstream rebuild, dormant ones never execute |
| 74 | 2026-06-03 | platform | Take the Keycloak admin REST API fully off the public plane. After Option B (commit `8fde4ae`, 2026-06-03) the admin **console** is tailnet-only on `kc.secforge.dev` (KC_HOSTNAME_ADMIN), but the admin **REST API** (`/admin/realms/*`) still answers on the public `auth.secforge.dev` because the Control + Member-Hub backends reach it via `KEYCLOAK_URL=https://auth.secforge.dev`. It is bearer-token-gated (verified 2026-06-03: 401 without a valid admin-SA token) and additionally behind the source-IP `auth-admin-tailnet` DENY — but that DENY is weak under CNI hostPort SNAT, so the REST API is effectively reachable on the public gateway. **Work:** (a) split the backend Keycloak config — keep the public issuer `https://auth.secforge.dev` for OIDC token *verification* (the `iss` must stay public so tenant JWTs validate), but add a separate admin base URL `https://keycloak-service.keycloak.svc.cluster.local:8443` for the `@keycloak/keycloak-admin-client` REST client AND the admin-SA `client_credentials` token endpoint; the minted token's `iss` stays `auth.secforge.dev` under KC_HOSTNAME strict, so the in-cluster admin API accepts it. (b) Trust the in-cluster TLS: `keycloak-service:8443` serves the `keycloak-internal-tls` cert (internal cert-manager CA, not the public LE cert) — the backends must trust that CA bundle (NEVER `-k`, per CLAUDE.md); check whether `keycloak/05b-internal-port443-service.yaml` already offers a simpler 443 path. (c) NetworkPolicy: `keycloak/05-network-policies.yaml` → `allow-cluster-oidc-clients-to-keycloak` currently allows `[openbao, observability, wazuh, app, ecosystem, vault-secrets-operator]` → keycloak:8443 — add `control` (+ confirm Member-Hub's ns; it may now be `member-hub`, not `app`) and the ztunnel HBONE `:15008` for the ambient hop. (d) Then tighten `auth-admin-tailnet` from a source-IP DENY into a **blanket DENY of `/admin*` on `secforge-gateway`** (drop `notRemoteIpBlocks` entirely) — nothing pod-sourced needs `/admin` on the public plane anymore. The `kc.secforge.dev` console (tailnet gateway) and the master-realm login (`auth.secforge.dev/realms/*`, not `/admin`) are unaffected. **Verify after:** signup wizard (Control → KC Org/user create) + MH admin-API ops end-to-end; OIDC token verification still green. **Trigger:** next maintenance window touching Control/MH deploys, or before broadening operator/tenant reach. **Cross-ref:** commit `8fde4ae`, ADR-0032, the "drop the public-DNS detour" comment already in `keycloak/05-network-policies.yaml`, `istio-ingress/20-virtualservices.yaml` (keycloak + keycloak-admin VS). | medium (~half day) | ⬜ Open | No — admin REST API is bearer-token-gated today; this removes the public-plane exposure (defense-in-depth) |
| 78 | 2026-06-04 | platform | **REPO FIX — app CA-trust bundles hardcode STALE CAs (systemic; HIGH — next deploy re-breaks Keycloak server-side trust).** Surfaced during the 2026-06-04 k3s-encryption incident recovery (memory `project_k3s_encryption_incident_2026_06_04`): after the reboot+restore, EVERY server-side call to Keycloak (`auth.secforge.dev`) failed `UNABLE_TO_VERIFY_LEAF_SIGNATURE` — control-signup-cleanup CronJob crashed hourly, wazuh-dashboard OIDC crash-looped, control/member-hub/proposal-forge admin calls broke. **Browser login was unaffected** (external LE path). **Root cause:** in-cluster `auth.secforge.dev` is split-horizon — it serves Keycloak's `keycloak-internal-tls` leaf (empty subject, SAN incl `auth.secforge.dev`) signed by the **internal** `SecForge Internal CA`, NOT the public LE cert. The app trust bundles (`NODE_EXTRA_CA_CERTS`) carried a STALE `SecForge Internal CA` (fp `E0:7D…`, from a pre-rotation CA hardcoded in the repo manifests). There are now THREE `SecForge Internal CA` certs live: `E0:7D` (stale-in-bundles), `F6:6F:33` (cert-manager clusterissuer `secforge-internal-ca-tls`), `D2:35:22` (what `keycloak-internal-tls` actually serves). **Live fix applied (drifts from repo):** rebuilt each cm to `D2:35:22 + F6:6F:33 + OpenBao 31:C1` and restarted — `control/openbao-internal-ca-cert`, `member-hub/openbao-internal-ca-cert`, `proposal-forge/openbao-internal-ca-cert`, `wazuh/secforge-internal-ca`. **Repo work owed:** (a) `platform/manifests/control/08a-openbao-ca-configmap.yaml` + the member-hub/proposal-forge equivalents + the wazuh `secforge-internal-ca` cm hardcode the stale `E0:7D` Internal CA + stale `EE:9B` OpenBao CA — replace with the live CAs, or better, source them dynamically (a cert-manager `Certificate`/trust-bundle / reflector of `keycloak-internal-tls` ca.crt + `openbao` ca.crt) so a CA rotation can never re-introduce this. (b) **Reconcile the 3-way Internal-CA split**: `keycloak-internal-tls` (`D2:35:22`) ≠ clusterissuer (`F6:6F:33`) — re-issue the keycloak cert from the CURRENT clusterissuer so they align; otherwise the next keycloak cert renewal re-signs with `F6:6F:33` and re-breaks any bundle that only trusts `D2:35:22` (the live fix includes both, so it survives one renewal). (c) control's live cm has 5 certs (2 harmless stale) — rebuild to the clean 3. **Cross-ref:** memory `project_k3s_encryption_incident_2026_06_04`, item #74 (also touches keycloak-internal-tls trust), CLAUDE.md "never `-k`". **✅ Phase 1 DONE 2026-06-04 (trust-manager):** installed jetstack trust-manager v0.22.1 (cert-manager ns, trust namespace=cert-manager); two `Bundle`s (`openbao-internal-ca-cert` key ca.crt → control/member-hub/proposal-forge; `secforge-internal-ca` key ca.pem → wazuh) now SOURCE the live internal root CA (`secforge-internal-ca-tls`) + reflected OpenBao CA (`openbao-ca-source`) + transitional keycloak-served CA (`keycloak-ca-transitional`), and reconcile each app's trust ConfigMap continuously — a future CA rotation self-heals, no more hardcoded-stale. Adopted the existing hand-patched ConfigMaps cleanly (running pods unaffected, CA already in memory); control re-verified `authorized=true`. Codified: `platform/components/06-trust-manager.sh`, `platform/manifests/trust-manager/01-bundles.yaml`, `platform/values/trust-manager.yaml`; the 3 hardcoded `08a-openbao-ca-configmap.yaml` manifests DELETED. **Phase 2 STILL OPEN (deferred — needs a ~30-60s IdP blip window):** re-issue `keycloak-internal-tls` from the current `secforge-internal-ca` ClusterIssuer (`kubectl delete secret keycloak-internal-tls` → cert-manager re-issues → `rollout restart statefulset keycloak`) so Keycloak serves a cert chaining to the current root (`F6:6F:33`), then DROP the `keycloak-ca-transitional` source from `01-bundles.yaml` + `06-trust-manager.sh` and the reflection. That collapses the 3-way split to one CA. Also: the wazuh-indexer keytool import takes only the FIRST cert of the bundle into the JVM truststore (openid uses pemtrustedcas_content = all certs, so OK, but fragile — fix the init to import each cert). **✅ Phase 2 DONE 2026-06-04:** re-issued `keycloak-internal-tls` from the current `secforge-internal-ca` ClusterIssuer (`kubectl delete secret` → cert-manager re-minted it signed by the current root `F6:6F:33`, SAN `auth.secforge.dev` intact) + `rollout restart statefulset keycloak` (brief IdP blip); verified control `authorized=true` with `issuerCA=F6:6F:33`. Then dropped the `keycloak-ca-transitional` source from both Bundles + deleted the reflected cm; app trust configmaps now carry exactly 2 certs (current internal root + OpenBao), and `authorized=true` still holds. The 3-way Internal-CA split is collapsed to one CA. Codified in commit (Phase 2). **Residual (minor):** the wazuh-indexer keytool single-cert import quirk above. | done | ✅ Closed 2026-06-04 (Phase 1 + 2) | — |
| 79 | 2026-06-04 | platform | **Re-enable k3s secrets-encryption-at-rest PROPERLY (currently OFF) + un-patch OpenBao SPIRE init.** The 2026-06-04 incident (memory `project_k3s_encryption_incident_2026_06_04`) was caused by enabling `secrets-encryption` ONLINE on 2026-05-28 without a clean k3s restart to PERSIST the AES-CBC key — it lived only in apiserver RAM, so the planned reboot lost it and made all 210 Secrets unreadable. Recovery disabled encryption (commented `secrets-encryption: true` in `/etc/rancher/k3s/config.yaml`, backup `.bak-pre-incident-disable-20260604`), so the box is back to the **pre-05-28 posture: Secrets are plaintext in `state.db`**. **Owed:** re-enable correctly — enable → `k3s secrets-encrypt reencrypt` → **CLEAN `systemctl restart k3s` to persist the key on-disk** → VERIFY it survives a *test* reboot before trusting it. Do this in a fresh, planned session (NOT at the tail of other work — repeating the exact trigger is how you get incident #2). Also un-patch the `openbao`/`openbao-seal` StatefulSets (recovery JSON-patched out the `wait-for-spiffe-csi` init + spiffe volume because SPIRE was down) — restore via helm redeploy (OnDelete = applies on next pod restart) in the same calm window. **Cross-ref:** memory, ADR (etcd-at-rest), `project_encryption_architecture`. **⚠️ ATTEMPT 2026-06-04 — BLOCKED + safely reverted:** enabling the flag crash-looped k3s with `aescbc.keys: Required value` — the SAME failure as the incident. ROOT BLOCKER FOUND: the k3s **datastore bootstrap** still holds the corrupt May-28 encryption config `{aescbc:{keys:[]}, identity:{}}` (EMPTY key list). k3s rewrites `cred/encryption-config.json` from that bootstrap on every start, so with the flag ON it reads an empty key and fails; removing the on-disk file does NOT help (k3s regenerates the empty one from the bootstrap). With the flag OFF k3s ignores it (`secrets-encrypt status` = Disabled) — current safe state. So enabling requires **clearing the corrupt empty-key config from the datastore bootstrap FIRST** so k3s generates a fresh real key (or injecting a valid aescbc key into both the file AND the bootstrap). No k3s-native "reset corrupt config" command exists (k3s docs warn corrupt encryption state can permanently corrupt a cluster). Reverted cleanly: flag re-commented, k3s active, api ok, 68 pods Running, all healthy. **Backups in `/root/k3s-reencrypt-20260605/`** (state.db.pre-encrypt 277M, config.yaml.bak, cred.bak, encryption-config.json.stale-may28). **NEXT (dedicated session, tested vs the state.db backup):** (a) find/derive the k3s bootstrap key in kine, decrypt, remove/zero the encryption config entry; (b) flag-enable → confirm k3s generates a real aescbc key on-disk; (c) reencrypt; (d) systemctl-restart-k3s test (clears apiserver RAM = reboot-equivalent) to PROVE persistence before trusting. | medium-large (datastore-bootstrap clear needed first; ~half-day researched) | ✅ Closed 2026-06-05 — datastore-bootstrap cleared, fresh aescbc key minted + **reboot-validated** (survived a power-cycle); pre-existing secrets reencrypted via apiserver re-write (`kubectl replace`, no restart) → **209/211 encrypted at rest**, 2 immutable residual (k3s node-password, vso-cc-storage-hmac-key). OpenBao un-patch: `openbao`/`openbao-seal` `wait-for-spiffe-csi` init + spiffe volume restored via helm (rev 5/3, OnDelete → applies on next reboot; image stayed 2.5.4). Runbook: `k3s-encryption-reenable.md` | — (resolved; at-rest encryption ON) |
| 80 | 2026-06-04 | platform | **VSO restored-secret ownerReferences — re-establish VSO ownership so rotation propagates.** After the 2026-06-04 Velero restore, ~30 destination K8s Secrets exist WITHOUT the VaultStaticSecret ownerRef, so VSO logs `SecretSyncError …has no ownerReferences` and refuses to ADOPT/refresh them. Values are CORRECT (restored), so apps work today — but rotated OpenBao creds would NOT propagate. Remedy: for each affected VSS, delete the destination Secret so VSO recreates it owned (brief ~5s gap; running pods keep loaded creds → harmless; do continuously-read ones like loki/tempo minio last), or add the ownerRef manually. Spans control/keycloak/member-hub/minio/observability/kyverno/cert-manager. Distinct from #77 (that was the 1.4.0 reconcile-stall; this is post-restore ownership). **Cross-ref:** memory `project_k3s_encryption_incident_2026_06_04`, #77. **✅ DONE 2026-06-04.** Turned out to be BOTH #77 (frozen reconcile — conditions stuck at 23:03 with a stale TLS error even though auth was fixed) AND the ownership gap. Fix sequence: (1) `rollout restart` VSO controller → cleared the stalled work queue (lifetimeWatchers restarted for all VSS); (2) for each of the 31 not-synced VSS, `kubectl delete secret <dest>` → VSO recreates it OWNED + `SecretSynced=True`, re-rendering the IDENTICAL value from OpenBao (verified byte-identical via sha256 on `minio-kms-creds` the SSE master key — `e7c1a176…` before==after, zero data loss). ~10s recreate gap per secret is harmless (running consumers already hold the value). Note: a VSS spec patch (overwrite:true) and a plain restart alone did NOT un-stick a frozen VSS — deleting the destination secret is what forces the owned recreate. All 31/31 synced. | small | ✅ Closed 2026-06-04 | — |
| 81 | 2026-06-07 | platform | **Automate `member-hub-documents` MinIO credential provisioning (remove the manual secret-copy).** The MinIO user + bucket-scoped policy for the document-attachments bucket are currently provisioned by `manifests/minio/03-member-hub-documents-credentials-job.yaml` driven by `docs/03-runbooks/member-hub-documents-bucket.md`, which requires a one-time manual copy of the app credential Secret from `member-hub` into the `minio` namespace (a Job pod can only read same-ns Secrets). Fully automate by rendering `secret/minio/member-hub-documents` into the `minio` ns via VSO — needs a `minio`-ns `VaultAuth` + an OpenBao k8s-auth role/policy granting read on that path — then the Job reads it via `secretKeyRef` with no manual step (drops runbook steps 2 + 4). **Root cause it closes:** the credential half of this bucket was never provisioned on prod (OpenBao + the bootstrap Job's bucket existed, but no MinIO user), so every document upload/download 500'd with `InvalidAccessKeyId` until 2026-06-07. **Cross-ref:** memory `project_member_hub_documents_minio_gap`. | medium | ⬜ Open | No (runbook is the working DR-replay path; this only removes the manual step) |
| 82 | 2026-06-07 | docs | **Full production rewrite of two large local-edition docs** flagged by the 2026-06-07 doc drift sweep. Both got a protective production banner but still describe the local edition in the body: (a) `docs/03-runbooks/keycloak-operations.md` (~550 lines — bootstrap-admin flow, `auth-admin.secforge.dev`, mkcert, Docker Desktop bridge IPs, kcadm steps that don't apply now that admin is DB-only + tailnet-only); (b) `docs/04-security/threat-model.md` (~800-line point-in-time analysis — local-edition framing, Audit-mode signing claim that may now be Enforce, Valkey, PERMISSIVE-mesh references). Rewrite each against the live cluster. **Cross-ref:** `doc-drift-audit-2026-06-07.md`. **✅ (a) keycloak-operations.md DONE 2026-06-07** — rewritten production-accurate (DB-only admin model, `kc.secforge.dev` tailnet console, realm-import + 03a hardening replay + 05l secret-publish paths, client-secret-rotation Infinispan-bounce gotcha, Istio-gateway troubleshooting, signed-image cutover kept; 615→282 lines). **(b) threat-model.md — framing/facts reframed 2026-06-07** (added a production-reframe banner; scope set to the public Hetzner node; substrate facts corrected — Istio ingress / no Valkey / passkeys / Tailscale / Let's Encrypt; image-signing verified **secforge=Enforce, vendor=Audit**; out-of-scope inversions flagged as in-scope; broken ADR-0006 links fixed). **Remaining (security exercise, not a doc edit):** the full per-threat STRIDE re-rating — move the inverted out-of-scope items (node compromise, public network attacker, real-DNS) into the threat tables, drop the Valkey/ingress-nginx STRIDE sub-zones, add node-level compromise as a first-class threat. | medium | 🟨 (a) done; (b) reframed — STRIDE re-rating owed | No — banner warns readers; re-rating is hygiene |

**#36 residual (non-blocking):** the live `wazuh-manager` StatefulSet is currently a `kubectl patch` layered over the helm release — run a `helm upgrade` from the corrected vendored chart at the next convenient window to clear the patch-vs-helm drift (the live probe is already correct, so this is housekeeping, not urgent). Also: the stale pre-cutover `infrastructure/wazuh/` deploy tree (vendored chart + values overlay + `apply.sh`) — flagged here as a future mis-deploy risk — was retired 2026-05-20 (commits `3d48a30` + `f28a3f7`); the decoder/rule/ISM config was then migrated to `platform/components/07o`+`07p` and `infrastructure/wazuh/` deleted entirely (commit `45b8926`). See #38 for the wider `infrastructure/` retirement.

## Severity / blocker bar

A backlog item is **blocking Phase 10** if any of the following are true:
- An application integrating in Phase 10 will need the platform capability the item gates
- The item describes a recurring outage or wedged-state pattern that can hit running apps
- The item describes a security-bright-line-rule violation per CLAUDE.md

Items marked **Soft** above are operationally relevant but not gating — Phase 10 can proceed; the operator just shouldn't forget about them.

## What this file is *not*

- It's not the place for ephemeral TODOs in code (use `// TODO(operator):` inline)
- It's not a replacement for ADRs — decisions still get an ADR
- It's not a replacement for the Phase prompt's "deliverables" list — those define done; this tracks what slips past done

## Conventions

- Keep numbering monotonically increasing across phases. Don't reuse numbers when items close.
- When closing, update the Status column with a date. Don't delete the row — it's history.
- The `Blocking?` column answers "should this be cleared before the next phase starts?" — be honest; default to "No" unless the case is clear.
- Cross-link from PLAN.md as `operator-backlog #N`; never duplicate the description.
