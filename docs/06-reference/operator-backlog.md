# Operator Backlog

> Canonical index of post-phase follow-up items the operator must (or may) clear. Phase prompts open these; this file is the place to find them after that phase has closed.
>
> When you open a new backlog item, append a row here AND link the row from the closing phase block in PLAN.md. When you close one, mark it ✅ here AND remove the open reference from PLAN.md prose.
>
> Last updated: 2026-05-18 (opened #34 — 🚨 BLOCKING: OpenBao has no working admin recovery path; admin-break-glass k8s role doesn't exist despite runbook claiming it does, generate-root returns 405 on Transit-unsealed setups, all OIDC roles narrow-scope. Member Hub Track C deferred to laptop-hybrid-dev mode until admin restored via Velero recovery or original init root token located). Previous: 2026-05-18 (opened #33 — audit log hardening (chain + sign + git-anchor) deferred from Member Hub's minimal-tier landing today; spec exists in `Member Hub/.claude/skills/audit-logging/SKILL.md`; trigger is first production tenant onboarding where audit is load-bearing for compliance). Previous: 2026-05-18 (opened #32 — Keycloak → Ecosystem Control user-sync gap; surfaced during Member Hub dev when a freshly-signed-in user saw "Active org: none" despite Control having an org; manual fix today via direct DB UPDATE + inline WriteRelationships to SpiceDB; fix candidates range from admin-events SPI listener to a one-shot operator remap endpoint; blocking once Portal opens to multiple users). Previous: 2026-05-05 (opened #31 — codify the PT schema-evolution workflow (PT-repo Prisma migration → platform-repo migration SQL + RLS policy → cluster apply) before the first post-cutover schema change; three deliverables (runbook, `prisma migrate deploy` cutover, tenant_id+RLS lint), small total effort, opens proactively while the workflow is fresh from 10.1.3.) Previous: 2026-05-05 (opened #29 watching-brief — Wazuh agent on operator's Windows host, conditional on threat-model gap firing; opened #30 — PT-specific secforge audit-rule range 100240-100259 to be added once PT emits audit events from 10.1.4 onward, mirroring the per-app convention used for OpenBao + Keycloak.) Previous: 2026-05-05 (opened #28 — secforge rule 100201 (added in #18 closeout) has never fired. Field constraint `<field name="response.error">\.+</field>` targets the wrong path; OpenBao audit events have the error string at top-level `error`, not nested under `response`. Surfaced during #27's slot-picking pass when 100201 was discovered taken-but-dormant. Fix is small: change the field-name path. Worth confirming WHAT the rule was meant to catch when it was originally written — may need wider rework if the original semantics were different from what the field-name implies.) Previous: 2026-05-05 (closed #27 — added secforge rule 100206 (first free slot in 100200-299 — 100201/100203/100204/100205 all taken from #18 + #24). Two design decisions surfaced beyond the prompt's plan: (a) parenting on 100200 doesn't work because failed-auth events lack `auth.display_name`; (b) stand-alone level-5 rule loses to 100001's level 10 because Wazuh fires the highest-level match. Solution: `<if_sid>100001</if_sid>` parenting overrides the catch-all only for events that also match the OpenBao read-denial pattern. `wazuh-logtest` confirms 100206 fires at level 5; regression-checked 100200/100202/100221 still fire on their respective inputs. **Bonus latent-bug finding** (not in #27 scope): rule 100201 added in #18 closure has never fired — its `<field name="response.error">\.+</field>` constraint targets a field path that doesn't exist in OpenBao's audit shape (actual location: top-level `error`). Worth opening a follow-up.) Previous: 2026-05-05 (closed #26 — Path A confirmed: the periodic metrics token aged out under the renew-on-USE pattern despite Prometheus running, because sustained scrape failure means USE doesn't refresh the period. Policy + ServiceMonitor wiring were intact. Re-ran idempotent `configure-metrics-auth.sh`; Prometheus picked up the rotated Secret on next scrape without restart. Verified end-to-end: audit log shows `error: null`, `vault_core_unsealed=1` flowing into Prometheus, Wazuh data.error count on sys/metrics dropped 10→0 across the equivalent window, rule 100001 firings dropped 10→0. Subtle gotcha codified in `openbao-recovery.md`: `lookup-self` is not a useful liveness probe for `-no-default-policy` tokens — must lookup-by-id from admin perspective.) Previous: 2026-05-05 (opened #26 + #27 — Prometheus → OpenBao `sys/metrics` scrape failing with `permission denied` every 30s, surfaced via Wazuh ingestion of the failed audit events; chart's catch-all rule 100001 fires at level 10 instead of secforge ruleset because secforge rule 100200 doesn't cover read-permission-denied events. Two issues, ordered: #26 fixes the underlying auth fault, #27 widens secforge rule coverage.) Previous: 2026-05-05 (opened #25 — Wazuh dashboard OIDC cutover. Phase 7d Item 7 scaffolding has been in place since 2026-05-02 (Keycloak `wazuh-dashboard` client + OpenBao client_secret path + VSO binding + `configure-wazuh-oidc.sh` flip script) but the actual flip from local admin → Keycloak SSO has never been run. Surfaced 2026-05-05 by operator question "why doesn't Wazuh use Keycloak/OpenBao for login like Grafana?".) Previous: 2026-05-05 (closed #24 — `infrastructure/wazuh/03-decoders-configmap.yaml` extended with two literal-prefix `<decoder name="json">` blocks (`^F ` / `^P `) handling the kubelet-wrapped pod-log shape. Wazuh's syslog pre-decoder consumes both the timestamp AND the `stdout`/`stderr` token, so the residual content reaching decoders is `F {"...":...}` not `stdout F {...}`. Live-event probe shows 12 hits in rule.id 100200-100299 range across rules 100200/100202/100221, with `actor` / `path` / `loggerName` fields populated. Annotated #18 with #24-verified note. Bumped runbook with new "kubelet pod-log prefix handling" section + corrected verification recipe.) Previous: 2026-05-05 (closed #22 — added `allow-postgres-operator-to-secforge-app-db` NetworkPolicy at `infrastructure/cloudnativepg/clusters/app-db-netpol-operator-status.yaml` permitting postgres-operator ns → app/secforge-app-db:8000 (CNPG status-extraction endpoint). `Cluster.status` flipped to Ready=True within seconds; `Cannot extract Pod status` reconcile errors stopped immediately.) Previous: 2026-05-05 (closed #23 — root cause was sed-delimiter conflict between `|` in randomly-generated `INDEXER_PASSWORD` and the image's `/etc/cont-init.d/1-config-filebeat`; with `set -e`, the sed-substitution aborted mid-script leaving SSL directives commented + password empty. Fix: `apply.sh` now generates passwords without `|`/`&` (sed-meta safe). `wazuh-alerts-*` index now populated end-to-end. Annotated #17/#18 closure rows as live-verified. Opened #24 (decoder pre-match gap — `stdout F` prefix not stripped; secforge custom rules don't fire on real pod-log lines))

## Legend

- ⬜ Open — not started
- 🟨 In progress
- ✅ Closed
- ⏸️ Blocked — note the blocker
- 👁️ Watching brief — keep an eye on the trigger condition

## Index

| # | Opened | Phase | Topic | Effort | Status | Blocking? |
|---|--------|-------|-------|--------|--------|-----------|
| 4 | 2026-04-30 | 7d.3 | OpenBao Transit unseal token TTL strategy — switch to `-period=720h` periodic tokens | small | ✅ Closed 2026-05-02 | — |
| 12 | 2026-05-02 | 7b.2 | Promtail verify phase-2 — flip Loki `compactor.retention_enabled` and confirm dashboard panels populate | medium | ⬜ Open | No (panels stay empty until flipped; not blocking apps) |
| 13 | 2026-05-02 | 7b | Migrate `BFF_VALKEY_PASSWORD` from K8s Secret env var to OpenBao via `apps/lib/secrets/`. Currently rides on `secforge.local/legacy-secret-env: OPS-MIGRATE-VALKEY-PW` annotation, expires 2026-07-31 | medium | ✅ Closed 2026-05-05 | — |
| 14 | 2026-05-02 | 7b.5 | Live verification of OTel scrubber sink — blocked on Docker Desktop containerd image-load quirk (`docker save \| ctr import` reports `total: 0.0 B`) | small | ⬜ Open | No (code change compiles + tests pass; live verify deferred) |
| 15 | 2026-05-02 | 7c | Istio Ambient AuthZ-denial observability — original log-level finding closed; ztunnel emits per-policy + per-clause RBAC trace at `RUST_LOG=trace` | n/a | ✅ Closed 2026-05-03 | — |
| 17 | 2026-05-02 | 7d.5 | Wazuh agent `client.keys` doesn't persist across pod restarts (enrollment-loop quirk) | ~30–60 min | ✅ Closed 2026-05-05 (live end-to-end verified 2026-05-05 after #23 closeout — agent → manager pipeline confirmed delivering events at ~30s cadence to `alerts.log`, surviving the 2026-05-05 indexer + manager rotation without re-enrollment) | — |
| 18 | 2026-05-02 | 7d.6 | Wazuh manager-side custom decoders for OpenBao + Keycloak JSON formats not loaded | ~1 hr | ✅ Closed 2026-05-05 (synthetic-event rule firing via `wazuh-logtest` re-verified 2026-05-05 after #23 closeout — rule 100200 fires on a synthetic OpenBao audit JSON. Real pod-log format coverage verified 2026-05-05 via #24 — 12 hits in rule.id 100200-100299 range across rules 100200/100202/100221 from live OpenBao + Keycloak probes, with `actor`/`path`/`loggerName` fields populated. End-to-end SIEM coverage for the secforge ruleset is now complete) | — |
| 19 | 2026-05-04 | 10.1.1 | MCP server reachability after Project Tracker moves to cluster — PT's Week-5 MCP server runs over stdio for Jason's local Claude Code → PT data; cluster deployment breaks the stdio model. Decide between (a) keep a local-only MCP shim that proxies to the cluster API, or (b) expose a bounded set of MCP-over-HTTP endpoints. Surface a small ADR. | small (decision) + medium (impl) | ⬜ Open | Soft — only blocks Week-5 PT work, not the Phase-10 cutover |
| 20 | 2026-05-05 | 9 (retro) | Wazuh-apid daemon stops after `wazuh-manager` pod restart and is not auto-recovered; Wazuh dashboard's "wait for dependencies" init then blocks indefinitely. Manual fix today: `kubectl exec -n wazuh wazuh-manager-0 -- /var/ossec/bin/wazuh-control restart`. Durable fix likely a sidecar/liveness/probe pattern that watches `wazuh-apid` PID. See `docs/05-claude-code-prompts/phase-09-retrospective.md` § "Wazuh-apid recovery (operational gotcha)". | medium (~2-4 hr) | ⬜ Open | Soft — only matters when manager pod restarts; Phase 10 cutover should NOT depend on Wazuh real-time event flow until this + #17/#18 are all clear |
| 21 | 2026-05-05 | 7c-2 | Phase 7c-2 — SPIRE-as-CA cutover + multi-ns STRICT expansion + trust-domain unification cluster.local → secforge.local. Pre-req: validate the helm-values diff in `infrastructure/istio/authzpol-strict-7c2-draft/helm-values-spire-ca.draft.diff` against the upstream Istio + SPIRE compatibility matrix at https://istio.io/latest/docs/ops/integrations/spire/. Existing 7c-2 prep (AuthorizationPolicy templates, baseline capture, draft diff) parked at `infrastructure/istio/authzpol-strict-7c2-draft/`. Scope: (a) flip Istio to use SPIRE as external CA (disable Citadel), (b) ambient-enroll keycloak / spicedb / openbao / observability / teleport namespaces, (c) rewrite `app`-ns AuthorizationPolicy principals from `cluster.local/...` to `secforge.local/...`, (d) flip mesh-wide PeerAuthentication PERMISSIVE → STRICT staged ns-by-ns, (e) **removes the CNPG-PERMISSIVE workload-scoped override (`infrastructure/istio/05-peer-auth-app-cnpg-permissive.yaml`) introduced in 7c-1** once openbao + postgres-operator + observability are mesh-enrolled, (f) close the ADR-0010 deferral. ADR-0024 / operator-backlog cross-reference: removal of override is the 7c-2 closeout signal. | 1-2 day operator window | ⬜ Open | Soft for Phase 10 — apps go into STRICT in app ns on day one under cluster.local (already in force from 7c-1); trust-domain unification is a separate concern and does not gate Phase 10 |
| 22 | 2026-05-05 | 7c-1 | CNPG cluster `secforge-app-db` Ready=False since 2026-04-29 (lastTransitionTime: 2026-04-29T22:03:54Z) — `postgres-operator` ns → CNPG pods on `:8000` (status-extraction endpoint) blocked by NetworkPolicy `allow-openbao-to-secforge-app-db` which only permits `openbao→5432`. Data plane is unaffected (Phase 9 E2E passed under this condition; openbao→CNPG dynamic-cred mint works through 7c-1's PERMISSIVE override). What's broken: postgres-operator's status reporting feeds CNPG `Cluster.status.phase=Healthy`, so backups / version-upgrade signal / failover hooks operate blind. Fix: add a NetworkPolicy ALLOW for `postgres-operator → app/secforge-app-db:8000` (mirror the existing pattern but with the postgres-operator ns selector + port 8000). Surfaced during Phase 7c-1 cutover (commit `c409913`); confirmed pre-existing by toggling STRICT off mid-cutover and observing the same i/o timeout. | small (~15 min) | ✅ Closed 2026-05-05 (added `allow-postgres-operator-to-secforge-app-db` at `infrastructure/cloudnativepg/clusters/app-db-netpol-operator-status.yaml`, mirrors the openbao→app-db policy shape with port 8000 + postgres-operator ns selector. `Cluster.status.conditions[Ready]` flipped to True at 2026-05-05T20:08:11Z; postgres-operator log noise stopped. Other CNPG-hosting namespaces (keycloak/spicedb/teleport) didn't surface this because they don't have an explicit per-target ingress policy — the `app` ns's stricter posture was the differentiator) | — |
| 23 | 2026-05-05 | 9 (retro) | Wazuh manager-side filebeat → indexer pipeline broken at TLS + auth. Real OpenBao + Keycloak events flow correctly to the manager (alerts.log shows ~30s-cadence rule matches against `/host/var/log/pods/openbao_*` and Keycloak audit lines), but `wazuh-alerts-*` indices don't exist on the indexer at all — only `wazuh-monitoring-*` (agent-keepalive). Root cause confirmed via `filebeat test output`: handshake fails with `x509: certificate signed by unknown authority`. Inspection of `/etc/filebeat/filebeat.yml` on `wazuh-manager-0` shows the `output.elasticsearch:` block has no `ssl.certificate_authorities`, no `ssl.certificate`, no `ssl.key` (all four ssl directives are commented out), and `password:` is empty. The `wazuh-filebeat-certs` Secret has the right material (`filebeat.pem`, `filebeat-key.pem`, `root-ca.pem` matching the indexer root by SHA-256 fingerprint), but filebeat isn't told to use any of it. **CLOSURE 2026-05-05:** Root cause was NOT in the chart's `inject-filebeat-config` initContainer (that copied the chart's correct ConfigMap version into the EmptyDir as designed). The breakage was UPSTREAM in the Wazuh manager image at `/etc/cont-init.d/1-config-filebeat`, which uses `sed -i "s\|password:.*\|password: '$INDEXER_PASSWORD'\|g" /etc/filebeat/filebeat.yml` — `\|` is the sed delimiter. The randomly-generated `INDEXER_PASSWORD` (`iaol\|QsorzrF&StImeA9kow?V\|qt7za.`) contained two `\|` characters, breaking the sed expression syntactically. Combined with `set -e` at the top of the script, this aborted the substitution mid-way: the `username:` substitution ran (no `\|` in `admin`); the password's first sed (uncomment `#password:` → `password:`) ran but the value-write failed, leaving `password:` empty; the SSL substitution lines (steps 4-7) never ran, leaving the four `ssl.*` directives still commented from the image's stock template. Fix: `infrastructure/wazuh/apply.sh` `gen_pw()` now uses `specials = ".*+?=!"` (no `\|`, no `&` — both are sed-meta) and Wazuh's complexity policy is still satisfied. After rotating the four credential Secrets and restarting indexer + manager + dashboard, `/etc/filebeat/filebeat.yml` now has all four ssl directives populated + the actual indexer admin password substituted. `filebeat test output` reports handshake OK + version 7.10.2; `wazuh-alerts-4.x-2026.05.05` index created on the indexer (~1135 docs within minutes). Pipeline is end-to-end live. Two residual notes: (1) the image's `1-config-filebeat` substitutes username with `INDEXER_USERNAME` (`admin`), not the chart-intended `FILEBEAT_USERNAME` (`filebeat_internal`), so filebeat now auths as the indexer superuser instead of the dedicated filebeat user — over-privileged but functional, internal-only mTLS path; (2) the secforge custom rules (100200-100299) don't fire on real pod-log events because the manager-side decoder's `<prematch>^{"</prematch>` doesn't match content with the kubelet `stdout F ` prefix that survives the syslog pre-decoder strip — opened #24 to track. | medium (~2-4 hr) | ✅ Closed 2026-05-05 | — |
| 24 | 2026-05-05 | 9 (retro) | Manager-side custom decoder for kubelet pod-log wrappers doesn't strip the `stdout F ` middle prefix. Wazuh's syslog pre-decoder strips only the leading RFC3339 timestamp, leaving `stdout F {"...":...}` as the post-pre-decode content. The secforge `kubelet-pod-log` decoder's `<prematch>^{"</prematch>` therefore never matches, JSON_Decoder doesn't engage, `auth.display_name` / `request.path` / `loggerName` fields don't get extracted, and the secforge custom rules (100200-100299) all stay silent on real pod-log lines. Synthetic events injected via `wazuh-logtest` (which skip the agent → manager pipeline) DO match because they're plain JSON without the kubelet wrapper — that's the green path validated at #18 closeout. Real events DO arrive at the manager (alerts.log shows them), but they fall through to the chart's generic rule 100001 (case-insensitive `<match> ERROR </match>` substring) and end up tagged as "Application error detected" instead of OpenBao-/Keycloak-specific descriptions. Fix candidates: (a) add a parent decoder with `<prematch>` matching `^stdout F ` then offset / strip, then call JSON_Decoder; (b) switch agent localfile to a custom log_format with regex pre-extraction; (c) move the strip step from manager-side to agent-side (have the agent emit just the JSON suffix). Approach (a) is cleanest. Until closed, Wazuh's coverage is "events arrive and are searchable in Discover with rule 100001/591/etc." — useful for breakage detection but the secforge-tuned rules and field-level enrichments don't engage on production audit lines. | small (~1 hr — local_decoder.xml change + wazuh-logtest verify with kubelet-wrapped line + verify rule 100200 fires on real probe) | ✅ Closed 2026-05-05 (option α — extended `infrastructure/wazuh/03-decoders-configmap.yaml` with two literal-prefix `<decoder name="json">` blocks: `^F ` for full kubelet lines and `^P ` for partial continuations. Both use `<plugin_decoder offset="after_prematch">JSON_Decoder</plugin_decoder>`. **Subtle finding during the fix:** Wazuh's syslog pre-decoder consumes BOTH the leading RFC3339 timestamp AND the `stdout`/`stderr` token (treating it as the syslog hostname), so the residual content reaching decoders is `F {...}` not `stdout F {...}` — the original problem statement of "stdout F middle prefix" was half-right. Probe-decoder verification (`prematch=stdout F `=NO match, `prematch=F `=match, `prematch={"`=NO match) proved the actual content shape. Decoder name `json` deliberately collides with the chart-shipped JSON decoder — Wazuh allows multiple decoders to share a name, and the rules' `<decoded_as>json</decoded_as>` sentinel keys on that name regardless of which decoder did the parse. End-to-end probe: 12 hits in rule.id 100200-100299 range from a single Keycloak failed-login + OpenBao kv-read/write trigger, across rules 100200 (OpenBao base, 4×) / 100202 (OpenBao sys/ mutation, 3×) / 100221 (Keycloak WARN, 3×). All hits include populated `data.auth.display_name` / `data.request.path` / `data.loggerName` / `data.message` fields. Runbook `wazuh-operations.md` extended with a new "kubelet pod-log prefix handling" section documenting the OSRegex constraints (no `(F\|P)` group alternation, no `[FP]` POSIX char classes, `type="pcre2"` not honored on `<prematch>`).) | — |
| 25 | 2026-05-05 | 7d.7 | Wazuh dashboard OIDC cutover never executed. Phase 7d Item 7 scaffolding has been in place since 2026-05-02 (Keycloak `wazuh-dashboard` client provisioned via `infrastructure/keycloak/clients/wazuh.sh` + OpenBao policy + role + VSO binding `infrastructure/wazuh/03-vso-binding.yaml` rendering `wazuh-oidc-vso` Secret in wazuh ns + reversible flip script `configure-wazuh-oidc.sh`), but the actual cutover from local admin → Keycloak SSO has never been run, so the Wazuh dashboard still requires the chart-generated local admin user (the same admin whose password we fixed in #23). Note: even after the OIDC cutover, the system-internal admin password persists for filebeat → indexer auth (HTTP basic) and the manager API → indexer auth — those layers don't speak OIDC, and #23 already moved that admin password into OpenBao. OIDC only buys the human-facing dashboard login surface. Watch for: the same `realm_access.roles`-claim-plumbing gap that forced Grafana onto `preferred_username` (per the OpenBao 2.5.3 upstream defect noted in PLAN.md Phase 7 carry-ins) — Wazuh's OpenSearch Security Plugin OIDC config might need the same workaround. Procedure: run `infrastructure/wazuh/configure-wazuh-oidc.sh`, watch the dashboard pod restart, navigate to https://wazuh.secforge.local/ and confirm redirect to Keycloak; verify role-mapping (jaupole → wazuh `admin` backend role); document the cutover in `docs/03-runbooks/wazuh-operations.md`. | small (~30-45 min) | ⬜ Open | Soft — dashboard auth is human-only (no app traffic depends on it); local admin login still works in the interim; doesn't gate Phase 10 |
| 26 | 2026-05-05 | 7d.4 | Prometheus → OpenBao `sys/metrics` scrape failing with `permission denied` at ~30s cadence (Prometheus default `scrape_interval`). Surfaced via Wazuh dashboard noise: every scrape interval, OpenBao's audit device logs the denied request → kubelet pod-log → wazuh-agent → manager → indexer, where chart's generic rule `100001` "Application error detected" fires at level 10 (high). Per Phase 7d Item 4 (2026-05-02), the integration was supposed to use a periodic bearer token (`-period=720h -no-default-policy -policy=metrics-policy`) stored at K8s Secret `openbao/openbao-metrics-token` with the ServiceMonitor consuming via `bearerTokenSecret`. Something has drifted. Diagnose: (a) `kubectl get secret -n openbao openbao-metrics-token -o jsonpath='{.data.token}' \| base64 -d` to retrieve the current token + `bao token lookup` it (does it still exist? does it still have `metrics-policy` attached? is its TTL still alive given the periodic-on-USE renewal pattern?); (b) `kubectl get servicemonitor -n openbao -o yaml` to confirm `bearerTokenSecret.name` + `bearerTokenSecret.key` point at the right Secret + key; (c) `bao policy read metrics-policy` to confirm the policy still grants `read` on `sys/metrics` (it's just a 1-line capability list); (d) check if the periodic token's "renew on USE" pattern is actually renewing — the fact that scrapes are denied suggests the token may have expired and Prometheus is using a cached version. **Net effect today:** OpenBao Prometheus metrics not collected → Grafana OpenBao panels empty (Raft state, lease counters, unseal status, etc.) → no automated health alerting on OpenBao internals. The Wazuh log spam is a *symptom*; fixing this closes it. Verification: after the fix, re-query the indexer for `data.request.path=sys/metrics` events with `data.error=*` populated — count should drop to 0 within 1-2 scrape intervals. | small (~30 min once root cause identified; could grow to medium if the periodic-token-on-USE renewal pattern needs to be revisited) | ✅ Closed 2026-05-05 (Path A — token expired; policy + ServiceMonitor wiring intact. Re-ran `configure-metrics-auth.sh` (idempotent — rotates the periodic token + overwrites the K8s Secret). Subtle diagnostic gotcha: `bao token lookup-self` against the metrics token returns 403 even when the token is fresh because metrics-policy is `-no-default-policy` and doesn't grant `auth/token/lookup-self` — must lookup-by-id from an admin token. Admin-side lookup of the new token shows `policies=[metrics-policy], period=720h, ttl=719h59m`. Prometheus picked up the rotated Secret on next scrape without restart (kubelet auto-reconciles projected Secrets; Prometheus reads bearer-token file per-scrape). Verified: (a) audit log shows `error: null` on three consecutive `sys/metrics` response events at 30s cadence; (b) `vault_core_unsealed=1` for all 3 OpenBao pods via Prometheus query API; (c) Wazuh indexer query for `data.request.path=sys/metrics` AND `data.error` populated since fix-time = 0 hits across 2-min window (vs 10 hits in a 5-min pre-fix window); (d) rule 100001 firings on sys/metrics post-fix = 0 (vs 10 pre-fix). Underlying brittleness — periodic-token-on-USE pattern locks out under sustained scrape failure since USE doesn't refresh — documented as a recovery scenario in `docs/03-runbooks/openbao-recovery.md` § "Restore Prometheus metrics-scrape auth", and Phase 7d Item 4 footnoted in PLAN.md.) | — |
| 27 | 2026-05-05 | 9 (retro) | Secforge OpenBao base rule `100200` (and the broader 100200-range secforge ruleset) doesn't match `read` operations that return `permission_denied` or other auth-failure modes. These events fall through to chart-shipped rule `100001` "Application error detected" at level 10, which is a false-high (auth misconfiguration is operational, not application-error severity). Currently surfaced because of #26's auth-failure spam, but the rule-coverage gap is permanent until 100200 widens. Fix candidates: (a) extend rule `100200` (or add a child rule `100201`/`100203`) matching `data.request.operation=read` AND `data.error=*` populated, mapped to level 5 (medium — diagnostic-useful but not critical); (b) match on `data.response.data.error=*` populated (the audit field that has the HMAC'd error response); (c) write a per-path rule covering `data.request.path=sys/*` since that's mostly an admin-shaped surface where read-failures are noteworthy. Either way, the fix suppresses the false-high from 100001 via Wazuh's rule cascade — Wazuh fires the most-specific matching rule and quiets the catch-all. Verification path: `wazuh-logtest` with a synthetic OpenBao audit-event JSON containing the permission_denied shape; confirm the new secforge rule fires (not 100001). Must NOT regress the existing rule 100200/100202/100221 firings from #24 closure (12 hits across the live-event probe). | small (~30 min — local_rules.xml edit + `wazuh-logtest` verify + reload manager via `wazuh-control restart`) | ✅ Closed 2026-05-05 (added secforge rule 100206 to `infrastructure/wazuh/04-rules-configmap.yaml`. Slot picked: 100201/100203/100204/100205 were all taken (used in #18 + #24 closeouts) — first genuinely free slot in the 100200-299 range was 100206. **Two design decisions surfaced during fix that the prompt didn't anticipate:** (a) parenting on 100200 doesn't work — failed-auth audit events emit only `auth.token_type`, not `auth.display_name`, so 100200's display_name sentinel excludes them; (b) a stand-alone level-5 rule loses to 100001's level 10 because Wazuh fires the highest-level matching rule when multiple match, regardless of specificity. Solution: parent 100206 on `<if_sid>100001</if_sid>` directly, with constraints `request.operation=^read$` + `request.path=\.+` + `error=\.+`. When 100001's `<match> ERROR </match>` matches AND the OpenBao read-denial pattern also matches, 100206 overrides at level 5. **Also surfaced an OSRegex gotcha:** in `<field>` constraints, `\.` means "any char" and bare `.` means "literal period" — inverted from PCRE. The `\.+` convention used throughout this file works for "any non-empty content"; bare `.+` would match only literal periods. Verified via `wazuh-logtest`: synthetic permission_denied event fires rule 100206 at level 5 with description "OpenBao read denied: path=sys/metrics error=1 error occurred:..."; regression-checked 100200 (level 3 on success-shape), 100202 (level 6 on sys/ mutation), 100221 (level 5 on Keycloak WARN) all still fire. Runbook `wazuh-operations.md` extended with a new sub-section "Read-side auth/policy denial — rule 100206" documenting the if_sid=100001 idiom + OSRegex gotcha. **Latent bug surfaced for next pass (not in #27 scope):** rule 100201 (added in #18 closure as the OpenBao auth/policy denial rule) matches on `<field name="response.error">\.+</field>`, but OpenBao's audit shape has the actual error string at *top-level* `error`, not `response.error`. Rule 100201 has therefore never fired. Worth opening a follow-up to fix its field path.) | — |

| 28 | 2026-05-05 | 7d.6 | Secforge rule `100201` (added in #18 closeout as the OpenBao auth/policy-denial rule) has **never fired** — its field constraint `<field name="response.error">\.+</field>` targets a path that doesn't exist in OpenBao's audit-event shape. The actual error field is at *top-level* `error` (or `data.error` post-#24's JSON_Decoder mapping into the Wazuh field tree), not nested under `response`. Surfaced during #27's slot-picking pass (WSL Claude noticed 100201 was taken-but-dormant when scanning for free slots in the 100200-299 range). Fix: change the field path in `infrastructure/wazuh/04-rules-configmap.yaml` rule 100201 to match the actual audit-shape field. **Also worth confirming what the rule was originally meant to match** — auth-failure on writes? policy-violation generally? — and whether the level / description / groups still fit, or whether the rule needs broader rework. The #27 closure introduced 100206 specifically for read-side auth failures, so 100201's intended scope likely doesn't overlap (probably write-side or generic policy-denial). Verification: `wazuh-logtest` with a synthetic event matching the corrected field path; confirm 100201 fires; regression-check it doesn't shadow 100206 / 100200 / 100202 / 100221. | small (~10-15 min — single field-path edit + wazuh-logtest verify + manager restart) | ⬜ Open | No for Phase 10 — the SIEM coverage is otherwise complete (100200 generic OpenBao + 100202 sys/ mutation + 100206 read-side auth failure + 100221 Keycloak WARN); 100201's intended coverage is a refinement, not a load-bearing gap |
| 29 | 2026-05-05 | n/a | 👁️ Watching brief — Wazuh agent on the operator's Windows host (`jason-pc-gaming`) for SIEM coverage of the workstation that holds the SSH signing key, the mkcert root CA private key, the cached OpenBao admin tokens, and the operator's GitHub credentials. Three pre-reqs to fire: (a) ingress-nginx TCP-passthrough or NodePort for manager ports `1514` (ossec) + `1515` (authd) — currently ClusterIP only; (b) pre-registration flow documented (`manage_agents` interactive on manager → `client.keys` → SSH transfer to Windows host → MSI install with key); (c) Windows agent enrolled + verified via a deliberate Windows event (e.g., `Get-EventLog Security` filter for a known-good audit). Trigger to convert from watching brief to active work: threat-model gap analysis identifies the operator workstation as in-scope, OR the host starts holding more sensitive material than today (e.g., production cloud creds), OR an incident response playbook is being drafted that needs Windows-host visibility. | medium (~2-3 hr all-in once triggered) | 👁️ Watching brief | No — operator workstation isn't currently in-scope for SIEM coverage |
| 30 | 2026-05-05 | 10.1.4+ | Add Project Tracker audit-rule range to `infrastructure/wazuh/04-rules-configmap.yaml` once PT is emitting structured JSON audit events from 10.1.4 onward (the BFF-injected-identity step, where audit middleware lands). Allocate 100240-100259 for PT (per the convention: 100200-100229 OpenBao, 100221 Keycloak, 100240+ per-app). Rules to write: per-domain mutate-with-permission-denied, per-domain mutate-with-error, login-success-from-new-IP (if BFF logs source IP), dynamic-credential-refresh-success, AuthZEN-deny pattern, etc. Coordinate with PT app code (`server/src/common/audit.ts` is the likely place) to ensure event shape is rule-matchable — recommended envelope: `{ts, severity, event:"audit.<verb>", app, actor:{sub, tenant}, target:{kind, id}, outcome, msg}`. Same pattern applied to PF will land 100260-100279 in Phase 11. **Trigger:** 10.1.4 commit lands + PT emits its first audit event. **Watch for:** events falling through to chart's catch-all rule 100001 (the same false-high pattern that surfaced in #27); PT events should hit 100240-range, not 100001. | small (~30 min once event shape is settled) | ⬜ Open | No — without these rules PT events still arrive in the indexer and are searchable in Discover; they just hit chart's generic rules instead of secforge-tuned ones (cosmetic, same as pre-#27) |
| 31 | 2026-05-05 | 10.1.3+ | Codify the Project Tracker schema-evolution workflow before the first post-cutover Prisma schema change. Today's flow (post-Phase-10.1.3) requires three coordination points per change: (a) PT-repo Prisma schema edit + `npx prisma migrate dev --name X` against local docker-compose, (b) platform-repo copy `prisma/migrations/<TS>_X/migration.sql` → `apps/project-tracker/migrations/00N-X.sql` PLUS write matching RLS policy in `apps/project-tracker/migrations/00N+1-rls-X.sql` for any new multi-tenant tables, (c) cluster apply via `BAO_TOKEN=<admin> bash apps/project-tracker/migrations/apply.sh`. **Three deliverables:** (1) `docs/03-runbooks/project-tracker-schema-evolution.md` documenting the 8-step workflow, the three forget-traps (`tenantId` field on new models, RLS policy on new tables, PT/platform-repo drift), and the recovery procedure if cluster falls behind local; (2) switch `apps/project-tracker/migrations/apply.sh` from hand-rolled `psql <list-of-files>` to `prisma migrate deploy` against the cluster (idempotent via Prisma's `_prisma_migrations` table) + a separate idempotent RLS apply step (`CREATE POLICY ... IF NOT EXISTS` + `ENABLE ROW LEVEL SECURITY` is idempotent on already-applied tables); (3) bash CI lint at `apps/project-tracker/migrations/lint-schema.sh` that fails commit if any `model X` block in `prisma/schema.prisma` lacks `tenantId`, OR any `CREATE TABLE` in the latest migration.sql doesn't have a matching `ENABLE ROW LEVEL SECURITY` line in the platform-repo RLS files. Wire as a pre-commit hook in both repos. **Trigger to act:** first time you want to add a model, column, or relation to PT post-cutover. The pain of doing it manually once will surface what the runbook needs to say; don't pre-build the ergonomics blind. **Watch for:** if a schema change happens without #31's deliverables in place, the operator should still complete it manually and THEN come back to write the runbook from notes — the runbook will be better for having walked through the actual flow once. Same pattern lifts and shifts to Phase 11 (Proposal Forge); PF has more models + real production data, so the discipline matters more there. | small (~1-2 hr total — runbook is the bulk; apply.sh swap is ~20 min; lint script is ~30 min) | ⬜ Open | No — but skipping it sets up a security-gap forget-trap (new multi-tenant table without RLS = open across tenants until manually fixed); should land BEFORE Phase 11 (PF integration) where the discipline matters more |
| 32 | 2026-05-18 | ecosystem | Keycloak → Ecosystem Control user-sync gap. Control's `organization_memberships.user_id` and `organizations.created_by_user_id` are populated with whatever Keycloak `sub` was passed in when the row was created, with no later reconciliation. When the Keycloak user is recreated (realm reset, account re-registered, federation rebound, etc.), the user gets a new `sub` but Control's rows still reference the old one. Result: the new user appears to have zero memberships from Control's perspective, Member Hub's `/auth/callback` correctly chooses not to set the active-org cookie (`pickInitialActiveOrg(emptyList) → null`), and the Member Hub UI shows "Active org: none" even though an org "for them" exists in Control. **Surfaced:** Member Hub dev session 2026-05-18 — `/members` showed "no members yet" for a freshly-signed-in user; investigation found user's Keycloak sub `91c6cee3-…` while Control's only membership row was for `db83f3e1-…` from an earlier Keycloak state. **Manual fix applied today:** direct UPDATE on `organization_memberships.user_id` + `organizations.created_by_user_id` in `control` DB, then direct `WriteRelationships` to SpiceDB via inline node script `kubectl exec`'d into the control api pod (writing `organization:<id>#member@user:<new-sub>` + `organization:<id>#administrator@user:<new-sub>`). Verified end-to-end: `permission check ... view ...` returned `HAS_PERMISSION`. **Fix candidates (pick one):** (a) Keycloak Admin Events SPI listener → Control webhook that fires on user-create / user-delete / user-update, reconciles `organization_memberships` by email or some stable attribute and writes corresponding SpiceDB updates via the existing `spicedb-sync.ts` reconcilers; (b) add a Control admin endpoint (and matching UI) for the operator to remap user_id without going to the DB + SpiceDB directly — narrower scope, manual trigger, but bounds the worst case; (c) store a stable "federation identity" (email or Keycloak federation-id claim) as the canonical key in Control with the sub as a denormalized lookup that auto-refreshes on each `/me` call. **Watch for:** spicedb-sync.ts at `Ecosystem Control/src/api/lib/spicedb-sync.ts:145-167` is the existing reconciler for the member relation — whichever fix lands needs to invoke it so SpiceDB and Postgres don't drift again. **Trigger:** before opening the platform to multiple human users in any environment, OR the next time Keycloak users get reseeded (every reset compounds the orphan pile). | medium (~2-4 hr depending on fix choice; (b) is smallest, (a) is the proper fix) | ⬜ Open | No for Phase 10 — workaround is documented in this entry; doesn't gate any current phase. Will become blocking once Portal opens to multiple users. |
| 33 | 2026-05-18 | per-app | Audit log hardening — hash-chained rows + nightly signed checkpoints + git-anchor. Today (post Member Hub B3 minimal tier 2026-05-18) every app's `audit.event` table is append-only at the DB level (REVOKE UPDATE/DELETE + RLS-deny-U/D policies) but rows are NOT hash-chained. Tampering by anyone with DB-owner access (CNPG postgres superuser, the `_owner` role used by migrations) is undetectable. Full skill spec at `Member Hub/.claude/skills/audit-logging/SKILL.md` calls for: (1) two new BYTEA columns `prev_hash` + `full_hash` on `audit.event` populated by `lib/audit.ts`'s record() — acquire `pg_advisory_xact_lock(hashtext('audit_chain'))`, SELECT prev row's full_hash, compute `full_hash = sha256(prev_hash || canonical_json(this row minus full_hash))`, INSERT atomically; (2) optional `signed_root` column populated by a nightly CronJob that recomputes the chain, signs the latest full_hash with an Ed25519 key (OpenBao Transit, not Infisical as the upstream skill spec says), and commits `(date, latest_id, root, signature)` tuple to a public `audit-anchors` git repo so tampering requires git history rewrite + private-key compromise; (3) `workers/audit-verifier/` worker that recomputes the chain, blocks new writes via a feature flag + pages on-call if it finds a mismatch; (4) `audit.read` SECURITY DEFINER function gated by an `auditor` SpiceDB relation (replaces the minimal-tier RLS-by-org SELECT policy with something narrower than "anyone in the org can read"). Forward-compatibility was preserved in the minimal-tier migration: the chain columns can be added NULL-able + back-filled in a single one-off worker run. **Scope:** Member Hub today, Proposal Forge + Project Tracker once they ship audit. Each app gets its own `audit.event` table (no cross-app audit DB — same tenancy boundary as the rest of their data) but the verifier worker pattern is reusable. **Trigger to act:** before any production tenant onboarding where the audit log is load-bearing for compliance / dispute resolution / incident response — currently no app has a real tenant. Member Hub's minimal-tier table is fine for dev iteration. **Watch for:** the `audit-anchors` git repo needs to live somewhere stable and publicly readable (e.g. a dedicated repo in the SecForge GitHub org); auto-commit credentials are themselves a sensitive surface, scope them to push-only on that single repo. | medium-large (~1 day per app for chain + verifier; ~half-day for the cron + git-anchor + Ed25519-via-Transit wiring that's shared across apps) | ⬜ Open | No for current dev phases — append-only is enough to satisfy the CLAUDE.md mandate. Will become blocking before first real-tenant compliance review. |
| 34 | 2026-05-18 | platform | **🚨 BLOCKING all new-app cluster bootstrap.** OpenBao has no working admin recovery path on this cluster. Surfaced 2026-05-18 during Member Hub Track C cluster-bootstrap attempt — needed to write a per-app policy + k8s-auth role + JWT-auth role + seed `secret/data/apps/member-hub/runtime`, all of which require admin scope. Tested every documented path: (a) `admin-break-glass` k8s-auth role from `docs/03-runbooks/openbao-recovery.md` § "Kubernetes auth break-glass" → `invalid role name` (was supposedly staged in Phase 5.6, not present in current state); (b) `bao operator generate-root -init` → `405 unsupported operation` (a known OpenBao 2.5.3 + Transit auto-unseal limitation already documented IN the recovery runbook itself); (c) common OIDC role names (admin, break-glass, emergency, platform-admin, openbao-admin, super-admin, operator) → all `invalid role name`; (d) operator's `jason.upole` OIDC login → per-user role `Oidc-jason.upole` with narrow scope (can list secret engines in UI, can't `lookup-self`, can't read its own policies, can't list/write policies or auth/k8s roles); (e) operator's `jaupole` Keycloak realm-admin account → no matching OpenBao OIDC role configured to log in with; (f) operator's stored "s." tokens → all 26-char narrow-scope (presumably auto-issued role tokens, not root). **Net:** there is currently no path to mint an admin/root OpenBao token without external recovery (Velero snapshot of an earlier state when admin existed, OR the original `bao operator init` printed root token from cluster bootstrap). **Workaround in use today:** new apps stay in laptop-hybrid-dev mode — talk to prod Keycloak + Control + SpiceDB over public ingress + tailnet, no cluster footprint. Member Hub is in this state. Works fully for development; blocks production deploy. **Fix candidates:** (1) Velero browse for an earlier openbao-* state that includes a working admin path (per `[[project_2026_05_16_ghcr_cutover_session]]` we've done Velero-based OpenBao recovery before — re-do it). (2) Locate the original `bao operator init` root token from cluster-bootstrap session (Phase 5.3). (3) Build a one-shot admin-mint pod that runs IN the openbao SA's trust context with a manually-attached `admin` policy via direct Postgres write to OpenBao's storage backend (drastic, last-resort, requires understanding OpenBao's storage schema). (4) Recreate the cluster (not realistic). Once admin is restored: immediately (a) create the `admin-break-glass` k8s-auth role per the recovery runbook so this exact situation doesn't recur, (b) update the recovery runbook with whatever NEW invariants we discover, (c) close this entry and the documentation drift in the same PR. **Trigger:** before any new app needs cluster deploy. Member Hub already affected; Proposal Forge will be next. **Severity:** HIGH (multi-week blocker if not addressed; touches every future per-app bootstrap). | medium-large (~half-day if Velero snapshot has good state; multi-day if storage-backend surgery needed) | 🟥 Open / BLOCKING | YES for any new-app cluster bootstrap; not blocking for apps already deployed (control, keycloak, spicedb, etc. — their VSO bindings exist and work). |

## Severity / blocker bar

A backlog item is **blocking Phase 10** if any of the following are true:
- An application integrating in Phase 10 will need the platform capability the item gates
- The item describes a recurring outage or wedged-state pattern that can hit running apps
- The item describes a security-bright-line-rule violation per CLAUDE.md

Items marked **Soft** above are operationally relevant but not gating — Phase 10 can proceed; the operator just shouldn't forget about them.

## What this file is *not*

- It's not the place for ephemeral TODOs in code (use `// TODO(operator):` inline)
- It's not a replacement for ADRs — decisions still get an ADR
- It's not a replacement for the Phase prompt's "deliverables" list — those define done; this tracks what slips past done

## Conventions

- Keep numbering monotonically increasing across phases. Don't reuse numbers when items close.
- When closing, update the Status column with a date. Don't delete the row — it's history.
- The `Blocking?` column answers "should this be cleared before the next phase starts?" — be honest; default to "No" unless the case is clear.
- Cross-link from PLAN.md as `operator-backlog #N`; never duplicate the description.
